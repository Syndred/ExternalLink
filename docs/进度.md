# ExternalLink 项目进度

- **2026-09-26 21:09 / 已定位捕捉层阻塞，定时暂停**：下午 Ego 侧栏/Neon 曾正常；随后 Chrome/Ego CUA 原生绑定间歇转为 `-3811`，普通 Chrome 标签仍能读，Finder 也能捕捉。Apple 定义该码为 ScreenCaptureKit `internalError`，最近日志没有具体底层原因或权限拒绝，故不能归因插件崩溃/权限。Neon Cloudflare 是独立回读阻塞。用户要求后已暂停 heartbeat。详情见验收交接。

- **2026-09-26 21:04 / Chrome CUA 预检继续，原生捕捉仍 `-3811`**：实际读取 NoAdsGames 发布规则；需可 iframe 的游戏直链及真实权利/规则确认，Jev 兼容性未知，未填、未提交，不入固定十站。Finder 捕捉成功，Chrome/Ego 原生绑定失败；Neon Cloudflare 验证阻止实时账本读取。已关候选页并回读，保留 Neon 与 Jev 续办页。本轮插件尝试/独立成功 0，目标 0/10。详见十站验收和交接。

- **2026-09-26 20:59 / Chrome 普通页面可用，工具栏捕捉待恢复**：按用户要求用 CUA Chrome 打开并读取 Jev Code Breaker，标记续办；Chrome/Ego 原生捕捉本轮均 `-3811`，Finder 成功。日志只见捕捉区域坐标异常、未见精确矩形校验/权限根因；无法检查扩展侧栏或实载版本，未重启。无插件提交/Neon 实时回读，目标仍 0/10。详见 Jev 验收交接。

- **2026-09-26 20:53 / CUA 原生捕捉对照与 Jev 候选**：Computer Use 本轮操作 Ego 普通页成功；原生 Chrome 窗口捕捉成功，Ego 捕捉 `-3811` 间歇出现并一度恢复。没有稳定 Ego 控制句柄，未重启。PLAXZY 免费但要登录；PLIX.GG 免费且无 SDK，但要求 iframe，Jev 兼容性及 Neon 精确去重未验，均未投稿。已关搜索/预检页并保留 CAPTCHA、登录、条款现场。插件尝试 0、新独立闭环 0/10。详见 `Jev插件独立十站验收-2026-09-26.md`。

- **2026-09-26 20:40 / CUA 直控与云账本回读**：Computer Use 成功打开 Ego 的 ExternalLink 侧栏，回读 JevPlay Profile 与 2 个云媒体；Neon 生产 `default/submissionRecords` revision 123 回读到 26 条 Jev `success` 键。随后 Ego 原生捕捉复现 `-3812`，Finder 对照成功；普通 CUA 标签仍能操作，本轮没有重启 Ego、也没有开始插件填表或提交。Google/DuckDuckGo 的 CAPTCHA 与登录/条款现场保留。候选仍不够冻结十站，独立闭环 0/10，详见 Jev 验收与交接文档。

- **2026-09-26 / 五分钟续办只读门槛**：Ego 原生控制仍 `-3811`；Neon 官方控制台遇 Cloudflare 安全验证，未通过或绕过，实时账本未读到。Computer Use 已关闭本轮 Neon 页并回读清理，无新增投稿或独立成功。

- **2026-09-26 / Computer Use 与续办规则**：现有五分钟 heartbeat 加入每轮 Computer Use 使用、中断原因、Ego 普通重启和本任务页签/分组清理检查，回读 ACTIVE。公开预检发现 SlowDen/Rando 两个待进一步核验的游戏候选，排除 Valeri 及 Press Kit/商店资料不适配站，Find Next Play 停在登录；本轮页签清理完成，无新增 Jev 插件独立成功。

- **2026-09-26 19:54 / Ego 普通控制恢复**：新建独立空间后直接核实 TechBaseDirectory 已公开 JevPlay 产品页和官网外链，加入防重投范围。本轮两张核验页已关闭，无新投稿；恢复普通控制不等于解除扩展内部页安全拒绝，实载与 Neon 未验。

- **2026-09-26 19:49 / 本地审计误报修复**：修复 Jev 旧种子/灾备为空仍返回 ok；明确来源与未验证云端标记，无证据返回失败。另确认 AIToolnet 当前投稿付费、ToolScout 登录有条款门槛，未新增投稿。Ego 控制仍 -3811。

- **2026-09-26 19:44 / 新任务接手**：fetch 核对 `06df6fe`；原有五分钟 heartbeat 已迁入新任务。Ego 控制仍 `-3811`，未重载插件。四站现场预检校正登录、AI 文案禁用及条款门槛，预检页已清理；无新提交，6/10 目标未达。详见 `Jev插件独立十站验收-2026-09-26.md`。

- **2026-09-26 / 3.7.106 Jev 单独验收**：按最新要求只测试 Jev，其他产品暂停；续办任务已保存普通故障重开 Ego、恢复前查回执去重及关页流程。修自定义下拉 Free 被先出现的 Freemium 抢匹配，复用原生下拉精确优先函数；54/54 测试通过，尚未实载。Ego 已启动但原生窗口捕捉 -3811，未取得退出/重开控制句柄，无新增提交。新 Jev 十站需独立闭环至少 6/10，历史 8/30 不混算。

- **2026-09-26 / 3.7.105 两条辅助投稿**：Alieradox OldPhoto、Graffiti 由插件填文字和最终提交，人工纠正 Radix 定价；两次站方成功提示及 Neon `destinationKey::profileId` 精确记录已在待上传 0、冲突 0 后回读。Graffiti 邮箱为本人 Gmail。站方收到矩阵组合至少 14 个，完全独立闭环仍 **8/30（26.7%）**。源码参照 Radix 官方鼠标 `pointerup` 实现修定价选择，并补 There Is An AI Tool 受众/明确起价映射；54/54 测试通过，3.7.105 尚未实载复验。新站只预检未发送；原生窗口捕捉再次失败，Alieradox 和设置页待清理。

- **2026-09-26 / 3.7.104 Alieradox 回执识别**：新 Chrome 窗口中，插件独立填写和提交 JevPlay，站方明确显示 `Tool submitted successfully! Our team will review it shortly.`；旧版侧栏漏识别，Neon 精确账本未确认，不计独立闭环且不重投。OldPhoto 定价仍误为 Free，未发送。源码补 Alieradox 短暂 AJAX 成功提示的来源限定识别及回归；实载待安全允许的重载方式。当前独立闭环仍 8/30，站方收到至少 12 个组合；本轮已用页签关闭。

- **2026-09-26 / 3.7.103 新站字段路由**：There Is An AI Tool 免费表单现场要求功能标签、准确起价、受众和至少三条功能；插件新增对应映射，付费起价无明确 Profile 数字时留空防止编造。定向测试通过，尚未实载或发送，独立成功仍为 8/30。

- **2026-09-26 / 3.7.102 站点标记切页竞态**：Ignlab 标验证码后切 ListAI 曾在新页短暂显示错误标记；代码固定点击时的 URL/标签/Profile，迟到的注解回读不得渲染另一页，补异步切页回归。源码待推送、浏览器实载未验收；新十站独立成功仍是 8/30。

- **2026-09-26 17:37 / 3.7.101 新十站续验**：插件独立填表、发送、站方回执与 Neon 精确回读为 **8/30（26.7%）**，离超过 50% 尚差 8 个组合。新增闭环来自 AI Tools Directory.site 与 Credible AI Tools 各 2 个；PromptHive OldPhoto 六步草稿和 Alieradox OldPhoto 定价错误草稿均未发送。3.7.101 Radix 定价选择修复已推送、54/54 测试通过但未实载；浏览器安全策略拒绝扩展管理页，原生窗口捕捉故障，继续时不得绕过或声称现场成功。详细逐站证据见 `docs/插件自动投稿开源对照与十站验收-2026-09-26.md`。

- **2026-09-26 16:12 / 3.7.90 涂鸦站邮箱云端验收**：Ego 扩展管理页自行重载 3.7.90，设置页对 Graffiti Name AI 显示联系邮箱、`Business mail`、`Feedback mail` 均为本人 `syndredyoung@gmail.com`。插件启动修复后，`default` 工作区轻量核对和手动云端回读均成功，版本一致、待上传 0、冲突 0；回读后邮箱仍正确。本轮未新增第三方投稿，已用插件页签关闭。

- **2026-09-26 / 插件 3.7.77 源码**：对照 Bitwarden、Automa、Playwright 和 HTML 表单归属规则，改为优先选投稿表单、按活动表单定位最终按钮，兼容通用 `role=button` 与 `form` 属性外部按钮；排除订阅/账户控件，预检错误按活动表单收集，点击前重取按钮。53/53 Node 回归通过。Chrome/Ego 窗口接管被 ScreenCaptureKit `-10005/-3811` 阻断，**尚未重载和实站复验**；本轮未产生新提交或云端记录。多数站插件全流程标准仍未达到。见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 14:13 / 云端同步完成、页签清理**：Chrome 3.7.76 设置页重试上传后保存提交账本和提交时间线 2 类，再回读 1 类新修订，最终 `default` 工作区版本一致、待上传 0、冲突 0。设置页重载后 AI Generation `/add/` 的 JevPlay、OldPhotoLive AI、Graffiti Name AI 三款各有精确已提交状态和带站方回执的动态；没有公开产品页。已关闭本轮 Chrome 25 个已用页签及 Ego Come AI 核查分组。三款各十站站方收件目标已达，插件完成多数站全流程仍未达。详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 / 插件 3.7.76**：代理自行逐版重载至 3.7.76 并回读 Chrome 扩展版本。AI Generation 三产品表单均显示 `Thanks for contributing to the directory!`：OldPhoto 最终按钮由代理点击，JevPlay/Graffiti 均由插件完成填写与最终点击；后两者自动写本地精确账本。修复 Google 表单按钮标签重复导致的漏点击，以及未找到按钮误报可提交。Linkrena Graffiti 插件填完并点击后跳登录/条款页，无回执；新增 `needs_login` 判断，Jev/OldPhoto 未试。53/53 测试通过。云端连接可用，但设置页 5 类本机修改仍待确认，三条 AI Generation 记录未完成 Neon 精确回读；插件多数站全程闭环目标仍未达到。详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 / 插件 3.7.72**：YAATD OldPhoto 实站由 3.7.71 插件填写 9 栏、上传实图并点击提交；站方建立待发布记录，随后免费候补确认连续两次报错，未完成排期、未公开、未付款，Neon 未回读，不计完成。3.7.72 将同页免费候补与付费精选区分为需人工/站方回执路径；代理已自行重载并在扩展详情页回读 3.7.72 与“已重新加载”，YAATD 侧栏的“需人工”已开启、“付费”已关闭。Verified Tools 免费档遇 Cloudflare 安全质询；Best AI Brands 无免费档且至少 $10，均未提交。53 个测试文件通过；详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 / 插件 3.7.70**：Tools.so 嵌入 Tally 表单中 OldPhoto 官网、邮箱由插件填写，3.7.70 修复透明 radio 漏识别并自行重载，现场确认插件选中“我是工具作者”。`@tool_twitter_handle` 为必填，现有 Profile 未提供，未提交、未计成功；已向用户一次性确认三款真实 handle。详见 `三产品十站插件实测-2026-09-25.md`。


- **2026-09-26 / 插件 3.7.68**：AI Tool Claw 免费表单付款误判已修，3.7.67 自行重载后 OldPhoto 六栏插件填对；插件最终点击后站方返回可见错误，无成功回执。3.7.68 增加失败提示即时识别，代理已自行重载并回读版本，未二次提交；站点三产品均不计成功。详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 / 插件 3.7.65**：代理自行重载并回读最终版本。修复 Stripe iframe 抢答检测、后台自动填表绕过付款门槛和页面水合期漏判；按真实可填字段选择嵌入式表单 frame，检测失败时停止后台自动填表。本地 iframe 实机识别并填写 OldPhoto 三栏，ToolDirectory.AI 实页仍识别 25 字段和 $9.99/月门槛，名称/网址为空，未付款、未提交。Come AI 免费表单插件填写 OldPhoto 五栏并最终点击后站方 API HTTP 500，无成功回执，不重试、不计成功。53/53 测试通过。历史站方回执已超过三产品各十站，插件六站全流程及 Neon 精确回读仍未达标；已有回执不重投。

- **2026-09-26 / 插件 3.7.59**：代理自行重载 3.7.58 后，Graffiti Name AI 在 AI Tools Ratings 由插件提交并获站方 `?submit=sent` 收到消息回执，侧栏显示本地动态等待云端保存，尚无 Neon 回读；同站 Jev、OldPhoto 也已有回执，均不得重投。Come AI Graffiti 插件填写/最终点击后站方 API HTTP 500，无回执；修正该站分类把关键词当类目的问题，53/53 测试通过，并自行重载 3.7.59，实机核对 OldPhoto 五栏及正确分类，未提交。后续先解决精确云账本，再继续十站矩阵。详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 / 插件 3.7.58**：Graffiti Name AI 投稿前发现插件把涂鸦作品图填入可选 `Screenshot URL`；已限制为明确产品界面截图 URL，找不到时留空，53/53 测试通过。当前 Graffiti 仅填表未提交，待自行重载、重填与完整提交验收。详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 / 插件 3.7.57**：OldPhotoLive AI 在 3.7.56 由插件提交并获 AI Tools Ratings 收到消息回执，但跨页补读遗漏证据信号，账本硬闸门拒绝自动登记。已补可信页面回执信号与回归；53/53 测试文件通过。JevPlay 和 OldPhoto 都不得重投，待精确补记/云端回读；自行重载 3.7.57 后用 Graffiti 验收整站闭环。详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 / 插件 3.7.56**：自行重载 3.7.55 后，AI Tools Ratings 的 JevPlay 表单由插件提交成功，站方跳至 `?submit=sent` 并显示收到消息回执。旧插件漏识别，未自动入账；已补站点专用回执识别并通过 53/53 测试，待自行重载，用 OldPhoto/Graffiti 现场检验账本与 Neon。JevPlay 不再重投，依据已见回执补精确记录。详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-26 / 插件 3.7.55**：AI Tools Ratings 实机确认 3.7.54 把目录表单误识别为个人资料页，因而只填表、未提交。已收紧资料页判定并加回归，53/53 测试文件通过；下一步由代理自行重载，复验 JevPlay 实际回执和云端账本，再逐一验 OldPhotoLive AI / Graffiti Name AI。详见 `三产品十站插件实测-2026-09-25.md`。

- **2026-09-25 / OldPhotoLive AI 新增十站**：本轮 10 个不同新站均取得站方回执；3 个公开、1 个 FoundrList 免费排期、6 个待审核。新增 Insidr AI、FoundrList、Forward Future 的免费路径至 3.7.43 分组。实站主要用 Ego 手工操作，扩展未能实机重载，Neon 未回读；已完成页签关闭。Firsto 未登录或提交。详见 `外链续办与自动关页-2026-09-25.md`。

- **2026-09-25 / 插件 3.7.43**：免费组补入 AINave、SoMuch、Offo、Insidr AI、FoundrList、Forward Future 的实际免费路径；OldPhoto 内置坏图地址改为逐一访问成功的官方截图。浏览器安全策略仍阻止 Ego 扩展重载，Neon 未回读。详见 `外链续办与自动关页-2026-09-25.md`。

- **2026-09-25 / 3.7.42 自动关页、后置验证与路径去重**：本轮 OldPhotoLive AI 在 FutureTools、SmartBizTools、AI Tool Finder、AllToolsDirectory、AINave、SoMuch、Offo 获得 7 个新站回执，其中 SmartBizTools、AINave、Offo 已公开，另四站待审核；距再交十站还差三站。SoMuch 邮箱确认已完成；AIWebsiteDirectory 待官网徽章，不计完成。四个 9 月 24 日已有回执的站点被重复投递，不计新增；新版统一其主页/投稿路径，防止同 Profile 再次入队。源码绑定侧栏窗口、精确账本后关队列自建标签、修正游标，并阻止带徽章/邮箱确认门槛的伪成功回执。ToolsCourt 报错无回执，Zearches 与 NeuroToolsHub 待条款授权。Ego 未重载，Neon 未回读；详见 `docs/外链续办与自动关页-2026-09-25.md`。

- **2026-09-25 / 3.7.39 高质量与免费分组**：外链库新增两组候选视图、单站手动覆盖和按组建批次入口；高质量组用有依据的权威/流量指标，免费组用近期实站免费路径或真实回执+免费标记，均保留原安全闸门。旧库 2,905 行只有 44 行带 DR/DA，不把无指标站伪称高质量。最近五站主要由 Ego 人工提交，插件自动最终提交比例无全量统计。代码已合入本机 `main`；Ego 新空间已建立，但浏览器 URL 策略拒绝扩展管理页重载，实机显示与 Neon 手工组回读待验收。见 `docs/外链库分组与插件能力-2026-09-25.md`。

- **2026-09-25 / 五个三产品免费申请站点**：Ego 实站完成 The Next AI、ListAi、Launching Next、Free AI Tools Directory、BestOfAI 的 JevPlay / OldPhotoLive AI / Graffiti Name AI 申请或既有待审核核验，五站分别按 Profile 登记 `待审核` 并从来源侧栏回读云端最新。The Next AI 三份 `Tool Submitted!`；ListAi 两份 `Submission Received!`，OldPhoto 已有申请且 `pending review`；Launching Next 免费排队号 153624–153626；Free AI Tools Directory 三份 `Thanks for contacting us!`；BestOfAI 登录后三份 `has been submitted and is awaiting approval`，均 `Not Sponsored`。Viesearch 的 JevPlay 与 OldPhoto 邮件确认后选 BASIC 免费等待队列，Graffiti 确认信仍不在当前 Gmail。TAIFY 验证失败，仅记笔记不计成功。`aitoolsdirectory.com`、Aitoolnet、BestOfAI 的插件标记分别为 `跳过`、`付费`、`需登录`，对应页面重载后保留；未付款。9 月 24 日历史基线为 13 个完整站点，本轮新增五站后按基线可超过 15 站目标，但缺少完整 Neon 全账本，当前精确总数未核实。详见 `docs/外链提交验收与误填保护-2026-09-25.md`。

- **2026-09-25 / Viesearch 与当前账本复核**：远端 `origin/main` 无新改动；Ego 侧栏强制刷新 Viesearch 后仅显示 1 个 Profile、2 条 2026-08-26 的 VideoToArticleAI 历史动态，未显示三款目标的本轮成功记录，站点标记是「可提交」而非「需人工」。另对 AITools Inc、TipSeason、Startup Stash 执行了单站云端回读，三站均显示 JevPlay、OldPhotoLive AI、Graffiti Name AI 已提交，不是可新增站点。全局 Neon 账本本轮无法安全读取，因此 9 月 24 日的 42 个组合、15 个站点、13 个完整站点只作为历史快照，当前完整数未知。Gmail 全文件夹命中 9 月 24 日 JevPlay 与 OldPhotoLive AI 两封 Viesearch 确认信；Graffiti Name AI 未命中。未点击一次性确认链接；公共 `/submit` 没有站内确认区，页面虽被自动填入 Graffiti 资料但没有提交。确认邮件搜索页已留给用户，AIGCLIST 与四个验证码页仍开着。详见 `docs/外链提交验收与误填保护-2026-09-25.md`。

- **2026-09-24 / 3.7.37 实机标记与云同步验收**：Ego 已重载 3.7.37。按真实回执、站点主机和 Profile 去重，三目标累计 42 个产品×站点组合、15 个有至少一款回执的站点，其中 13 站三款齐全，距 30 个完整站还差 17 站。15 个成功来源站均显示 `可提交`；The Next AI 作为自定义站入库并标记验证码，另保留 Launching Next、Free AI Tools Directory、Taify 验证码页；Saaspo 当前提交按钮禁用，标为 `无法提交`。两次云端回读分别只更新 1 类和 5 类，标记仍在；短暂的 `linkMonitorResults` 待上传经重试清零，最终待上传 0、冲突 0。自动填表、提交和记账仍有人工校对/补记实例，不能判定插件可独立完成 30 站验收。已关闭已用页签及无关空白 Chrome 浏览器。详见 `docs/外链提交验收与误填保护-2026-09-24.md`。

- **2026-09-24 / 3.7.28 Launchpedia 回执与自动记账**：Launchpedia 的 JevPlay、OldPhotoLive AI、Graffiti Name AI 均获官方嵌入 Tally 最终 `Form submitted / Thanks for completing this form!` 回执；JevPlay 在旧版人工补记，后两款由新版自动保存。来源侧栏为 3 个 Profile、3 条动态，刷新后回读无待云端保存提示。三份 Affiliate Link 均留空。累计 35 个真实产品×站点回执、13 站、11 站三产品齐全，距 30 个完整站还差 19 站。3.7.29 修正切换 Profile 后保留旧邮箱的问题，定向测试通过，待 Ego 重载实站复验。

- **2026-09-24 / 3.7.27 嵌入 Google 表单回执**：Startup Collections 官方嵌入式 Google Form 对 JevPlay、OldPhotoLive AI、Graffiti Name AI 均显示 `您的回复已记录。`；三份选择免费排队，不买 $10 加速。插件填对三组品牌/官网/描述/联系人，但未自动记账；三份按现场证据在来源站人工补记，侧栏 3 个 Profile、3 条动态，刷新确认云端最新。累计 33 个真实产品×站点回执、12 站、10 站三产品齐全，距离目标 30 个完整站点还差 20 站。3.7.27 将提交监听扩展到来源绑定的嵌入 Google Form，要求匹配当前 Profile 官网域名与最终确认链接；代码回归通过，待重载实站复验。

- **2026-09-24 / 3.7.26 三产品提交与填表修复**：AI Tools Inc 的 JevPlay 官方 Typeform 最终回执由 3.7.24 自动记账，三产品齐全。AI SuperHub 免费审核表单的 JevPlay、OldPhotoLive AI、Graffiti Name AI 都取得官方 `Submission Received!` 成功页，插件提示已保存；来源站侧栏显示 3 个 Profile、5 条动态，Graffiti 人工核验时多追加了动态；点击刷新后回读 `云端动态已核对，当前记录是最新的`。累计 30 个真实产品×站点回执、11 站、9 站三产品齐全，距离用户指定的 30 个完整站点还差 21 站。3.7.25 修正旧批次任务跨站抢占表单来源、单词用户名误填姓氏、短描述最低词数回退和 AI SuperHub 的必填检测；3.7.26 再修词数计数器被误作字符限制、打开的自定义选单掩盖表单必填项。52 个测试文件通过；3.7.26 待重载实站复验。

- **2026-09-24 / 3.7.24 Typeform 最终回执上下文**：OldPhotoLive AI 在 AI Tools Inc 官方免费 Typeform 收到最终 Thanks 回执；插件填对名称与官网，错填姓氏且留空邮箱，提交前人工修正。3.7.23 点击最终按钮后清空内容脚本监听状态，回执二次检查未携带目标目录 URL，故虽然页面有回执却未自动归属；OldPhoto 已按现场证据在来源站人工补记。3.7.24 在回执轮询时传入可信来源目录，定向测试覆盖最终轮询保留来源；待重载用 JevPlay 实站复验。累计 26 个真实产品×站点回执、10 站、7 站三产品齐全；OldPhoto 补记的云端回读待确认。

- **2026-09-24 / 3.7.23 AI Tools Inc 修复待实站复验**：Graffiti Name AI 在 `aitools.inc/submit` 免费官方 Typeform 收到最终 Thanks 回执；插件一度误把联系信息题中的 `We will be in touch` 当作预存回执，并把自动账本归属给旧任务站点。现场按 AI Tools Inc 来源页和 Graffiti Profile 人工补记，侧栏刷新后从 Neon 回读 1 个 Profile、1 条动态。3.7.23 移除该泛化回执，改为已知 Typeform 的完整最终文案；独立表单优先可信浏览器来源，AI Tools Inc 首页来源归一到 `/submit`。定向回归通过，JevPlay/OldPhoto 待重载实站复验。累计 25 个真实产品×站点提交、10 站、7 站三产品齐全。

- **2026-09-24 / 3.7.22 Tally 回执验收**：旧库曾成功的 AI Marketing Directory 免费 Tally 表单，本轮 JevPlay、OldPhotoLive AI、Graffiti Name AI 各自收到 `Form submitted` 与 `Page 2 of 2`，三次均未购买推广。JevPlay 在旧版漏识别后人工补记；3.7.22 仅对该站已知 Tally 表单识别最终回执，OldPhoto、Graffiti 由插件自动按各自 Profile 保存动态，Ego 实站复验通过。三份提交前均检查产品网址和文案；插件在 Tally 的自动填表仍会漏 Tool Name/联系人，需要人工补齐。当前 24 个真实产品×站点提交、9 个站点、7 站三产品齐全；侧栏手动「刷新」后云端回读确认 5 个 Profile、5 条动态均为最新（其中本轮三条）。Launching Next 免费提交表单要求回答 Quick Check 人工验证题，按用户要求留页，尚未提交。

- **2026-09-24 / 3.7.20 AI Infinity 实站验收**：Google Form 的最终提交控件是 `div[role=button]`，旧监听器漏掉点击，致 Graffiti 已收到站点回执却没有自动记账；已按官方目录页和 Graffiti Profile 人工补记。3.7.20 只对 AI Infinity 的已知 Google Form ID 接受该控件，并要求表单内产品 URL 精确匹配当前 Profile，未知表单与错产品均不自动归属。Ego 重载后用 JevPlay 实站提交，收到原文 `Thank you for submitting a new AI tool...`，插件自动保存来源目录的 JevPlay 记录并从队列移除；原目录侧栏显示 JevPlay、OldPhoto、Graffiti 三个 Profile 各一条提交动态，点「刷新」回读提示云端记录最新。当前累计 21 个真实产品×站点提交、8 个站点，其中 6 站三产品齐全；均为已提交/待审核，不等于上线。另修顶层回执的证据链接应指向成功页，而非提交前表单页；此小修尚待重载实站复验。

- **2026-09-24 / 3.7.19 Google 跳转来源**：Ego 实站确认 AI Infinity 表单的 `document.referrer` 为 `forms.gle`，而浏览器标签未保留 `openerTabId`，3.7.18 因来源无法核验而正确停止自动归属。3.7.19 监听浏览器 `onCreatedNavigationTarget`，只对从 AI Infinity 来源页打开的已知 `forms.gle` 链接临时保存来源标签映射，10 分钟内仍核对原标签 URL；未知表单继续拒绝自动记账。定向回归已补，待重载实测。

- **2026-09-24 / 3.7.18 Google Form 来源归属**：AI Infinity 的官方 `forms.gle` 跳转把最终 Google Form 的 referrer 改为 `forms.gle`，已增加浏览器 `openerTabId` 回查来源目录，且仅对实际表单 ID 归属；无可信来源仍拒绝自动记账。OldPhoto 的历史现场回执已按来源目录/Profile 人工补记；Graffiti 与 JevPlay 待修复版实站验收。相关定向测试通过。

- **2026-09-24 / 3.7.17 真实回执与路径账本**：3.7.15 已修复跨 frame 填写、同 URL 标签错填保护、不同产品旧值覆盖、外部表单来源归属、Startup Stash 路径键显示与无证据人工记账；52/52 自动测试通过。3.7.16 又在切换标签或 Profile 时清除旧页填表状态，并在人工登记动态后刷新待提交队列，避免卡片沿用旧状态。TipSeason JevPlay 已收到免费审核回执并由插件自动写入精确路径账本；本轮 19 个产品×站点有真实回执、8 个不同站点、5 站三产品齐全。3.7.17 将 TipSeason、Phygital 的路径与主域名归一显示，收到记账事件后刷新侧栏队列和时间线。19:32 云端修订号一致，待上传 0、冲突 0；精确 Profile 站点回显仍待新版实站核验。详见 `docs/外链提交验收与误填保护-2026-09-24.md`。

- **2026-09-24 / 3.7.14 跨域 Typeform 回执**：content script 开启 `all_frames`，侧栏「填表」arm 时强制刷新当前所有可用 frame 的新版脚本，各 iframe 通过当前页 watch 请求建立独立回执基线；StartupStash 只接受精确结束文案，避免广告页泛化 `Thank you` 误记。手动最终动作按点击时 Profile 归属，以 `confirmedBy=agent` 自动记录；独立 Typeform 仍使用原目录任务 URL 记账。相关 iframe、旧 token 串 Profile、Standalone Typeform 与来源字段回归已补齐，待 Ego 重载后实站验收。

- **2026-09-24 / 迁移成功闸门**：提交队列只跳过 `confirmedBy: agent/manual` 且带有真实证据的 `submissionRecords`；`confirmedBy: migration`、`Table.xlsx submitted seed`、空证据和旧站点勾选记录继续保留为可提交状态。真实回执的成功跳过语义保持不变，相关队列回归已补齐。

- **2026-09-24 / 外链提交恢复与验收**：DeepSeek 新 `outlink` Key 已写入 Worker Secret 并以直接请求 HTTP 200 验证；旧 `.env` 泄露 Key 已清除。Ego 已启用 3.7.12；Insidr、Human or Not、NextPedia 各完成 JevPlay、OldPhotoLive AI、Graffiti Name AI 三产品真实提交与云端回读，其中 7 次新版自动记账通过。Once Tools 的 JevPlay 另有待审核回执并人工补记。3.7.12 修复回执漏识别、跨 Profile 旧值误填、长短描述路由及时间线重复/Excel 日期异常；50/50 回归通过。10015 需登录停放。旧批次冲突及三产品各 30 站目标仍待完成。详见 `docs/外链提交验收与误填保护-2026-09-24.md`。

- **2026-09-23 / 3.7.3 云同步状态可见性**：设置页现用轻量修订号查询对比本机与云端，明确提示待上传、冲突、云端更新和未保存编辑。按钮与零更新文案已澄清；45/45 测试通过。Ego 扩展重载后的实机显示待验收。详见 `docs/云同步状态可见性-2026-09-23.md`。

- **2026-09-23 / 两站资料与 Product Hunt**：AISpeakLearn 与 JevPlay 已在云端及 Ego 插件设置页回读；当前共 8 个 Profile。JevPlay PH 排期 9 月 23 日、AISpeakLearn PH 排期 9 月 24 日，均为北京时间 15:01。两站提交账本分别记录为 `submitted`，尚未记为正式上线。AISpeakLearn 的 4 张英文截图与 PNG Logo 已上传 R2 并回读到插件。详见 `docs/两站资料与PH提交-2026-09-23.md`。

- **2026-09-23 / JevPlay Product Hunt 初次排期**：以 `@syndred` 创建 JevPlay Launch，Product Hunt 预发布后台显示 `Scheduled`，计划北京时间 15:01 上线。后续已新增独立 Profile 并补记账本；尚未核验正式上线或公开展示。见 `docs/JevPlay-Product-Hunt-2026-09-23.md`。

- **2026-09-21 / 3.7.2 多设备强制云端回读**：复制过浏览器 Profile 的电脑若只有本地 pending 标记，“从云端回读”现会先下载 JSON 备份并确认，再用 Neon 完整覆盖本机缓存并清空 pending/conflict/patch。并发编辑时仍拒绝覆盖；44/44 回归通过。详见 `docs/多设备强制云端回读-2026-09-21.md`。

- **2026-09-21 / Neon 新项目迁移完成**：以本机 Ego Profile 为最新数据源，新建 Neon 项目 `dark-night-20420985`、执行表结构、轮换 Worker 数据库密钥并完成明确替换。生产回读为 20 类文档、7 个 Profile、2,975 条外链、30 条提交记录、77 组/222 条时间线；43 个 R2 媒体 43/43 下载哈希和大小一致，缺失引用为 0。Worker 健康检查恢复 200，44/44 测试通过。详见 `docs/Neon新项目迁移-2026-09-21.md`。

- **2026-09-21 / 3.7.1 Neon 流量优化**：确认旧项目 HTTP 402 来自 Neon 当月 Network transfer 5.62 GB 用尽。Worker 新增轻量修订列表与单文档读取；插件改为首次全量、后续只下载修订变化文档，侧边栏 60 秒内复用结果，配额错误 60 分钟退避。旧项目阻塞已由上方新项目迁移解决。详见 `docs/Neon流量优化-2026-09-21.md`。

- **2026-09-17 / 3.5.2 ego 多浏览器同步修复**：确认 ego 整套导入 Chrome Profile 时会复制 ExternalLink 的云端配置和旧修订。已将 AISpeakLearn 与完善后的 VideoToArticleAI 合并上云，`siteProfiles` 38 → 39，回读为 7 个 Profile。手动保存现只上传真实待保存的非冲突文档，冲突回读只替换冲突键并保留其他待保存数据。详见 `docs/Profile资料完善-2026-09-17.md`。

- **2026-09-17 / 3.5.1 外链库数量与分类提交**：云端回读仍为 2,975 行；旧界面显示 3,065 是额外混入打包旧目录。现改为云端主数据 + 3 条独有自定义补充，并分别显示云端行数和去重目标数。侧栏选择分类后可直接启动该分类批次，原有 Profile、成功去重、付费、登录、验证码等闸门全部保留。42/42 回归通过；Chrome 重载待验收。详见 `docs/外链库数量与分类提交-2026-09-17.md`。

- **2026-09-17 / 3.5.0 Submify 对标外链库**：侧栏新增「外链」页和十类分类、费用、语言、DR/流量、Profile 状态筛选；Settings 同步复用。对比 Submify 当前公开 100 条后，云端去重补入 70 条，总数 2,975；29 条社区/投稿类只读跳过自动队列，2 条收费站标记付费。42/42 回归通过；Chrome 重载与侧栏真实回读待现场验收。详见 `docs/Submify外链库分类与补库-2026-09-17.md`。

- **2026-09-11 / 多设备云端合并恢复**：生产default已恢复30条提交账本，合并196条历史后扩展派生为214条时间线；6产品媒体引用恢复，30张R2图片正常。本机回读一致且无冲突，VideoToArticleAI四张截图实机加载正常。PC待以云端回读，详见 `docs/多设备云端合并恢复-2026-09-11.md`。

- **2026-09-11 / 3.4.7 外链库界面联动**：补齐设置页存储监听，使侧栏修改后自动更新外链列表与动态；合并刷新并保护未保存编辑。源码回归与实机验收边界见 `docs/外链库界面联动-2026-09-11.md`。

- **2026-09-10 / 3.4.6 全项目审计**：修复批次暂停/继续持久化与调度异常恢复、云同步并发/中断/冲突、Profile 队列切换、资料草稿保护、登记动态与评论识别/回执。40/40 回归通过；扩展内部页面工具访问受阻，不能报告全部功能已实机通过。逐项证据及待验收范围见 `docs/全项目审计与验收-2026-09-10.md`。

- **2026-09-10 / 3.4.5 动态云端回读**：修复侧栏只读旧缓存、云端补录不显示。先展示本机动态，再后台回读提交账本/时间线；保护本地待保存内容、并发改动、已前进版本和切换工作区，不覆盖当前 Profile。动态「刷新」可立即检查云端。云端 RspAi DevPages 记录仍在（3/5），旧本机为 2/4；源码 34/34 测试通过，Chrome 重载后显示待验收。顺带修正 GitHub URL 不再误填产品官网。详见 `docs/DevPages填表与动态修复-2026-09-10.md`。

- **2026-09-10 / 3.4.4 DevPages 填表与外链动态修复**：检测后立即按字段填写，AI 只补剩余字段；侧栏普通检测/填表只填写并留在当前页。新增「登记动态」入口，手动点击提交后核验新回执并按固定 Profile 入账；非评论页隐藏评论工作室。补充超时、重复点击、切站/切项目保护。31/31 自动测试通过；RspAi DevPages 已按用户确认补录云端并回读。3.4.4 实站验收受浏览器连接/安全策略阻塞，尚未提交 Comparison Text。详见 `docs/DevPages填表与动态修复-2026-09-10.md`。

- **2026-09-10 / 3.4.3 站点标记多选**：侧栏与外链库支持多选、单独取消、多个标签展示及按任一标记筛选；兼容旧标记，后台串行切换避免双页面覆盖，自动观察不覆盖人工选择。队列与机会评分检查全部标记。源码及 27 组 Node 回归通过；本轮已在 Chrome 管理页回读 3.4.3，多选实际点选仍待验收。Google 辅助登录可实现，本次仅核查方案。见 `docs/站点多选与谷歌登录-2026-09-10.md`。

- **2026-09-10 / 3.4.2 表单语义与站点经验**：先让现有云端模型理解表单再填写，区分产品定价与目录收录费；手动可提交标记不被自动判断覆盖。站点字段语义、选项结构和阶段跨 Profile 保存，复用时读取当前项目资料；旧学习兼容迁移不复制旧答案。代码及动态回归已完成，Chrome 重载和实站 A/B 复测尚未完成。详见 `docs/表单语义与站点经验-2026-09-10.md`。

> 最后更新：2026-09-24
> 扩展版本：3.7.37（源码；Ego 重载待验收，Manifest V3）
> 当前路线：Neon + Cloudflare Worker + R2 单一数据源；本地字段优先，AI 补剩余项，复杂表单升级多模态

- **2026-09-24 3.7.36 云同步运行态隔离**：`activeBatchRun` 旧冲突来自不同浏览器批次状态的并发写入；改为只在拥有标签页的本机持久化，正常业务文档仍按修订号增量回读、字段级 PATCH 上传。52/52 回归通过；Ego 重载后的实际冲突数待核对。三产品累计 41 个真实组合、15 站、13 站齐全，30 站目标未完成。
- **2026-09-24 3.7.37 真实回执站点标记**：账本写入和云端回读后，为有精确键及真实回执、无现存状态的站点补 `可提交`；保留验证码/登录/付费/跳过人工标记及表单经验。52/52 回归通过，待 Ego 实机和云端验收。3.7.36 已在 Ego 回读启用，轻量核对显示待上传 0、冲突 0、版本一致。

- **2026-09-10 3.4.1 人工页面保护**：撤回超额自动关闭人工页签；默认容量 20，可配置 1–100，达到容量只等待人工释放页面。已填好待验证码的页面跨批次保留，待办记录与页面现场分别保护。源码与本地测试已验收，用户重载后已在 Chrome 管理页回读 3.4.1 且已启用；扩展工作台工具访问仍受限，实机批次行为待验收。

- **2026-09-10 3.4.0 无人值守**：新增时长／组合／模型调用上限、持久巡检及本轮报告；中断任务保留核验状态，修正停止控制按钮显示。最初的人工页签清理策略已由 3.4.1 撤回。详细故障测试与实机边界见 `docs/无人值守验收-2026-09-10.md`。

- **2026-09-10 3.2.5 确定性优先 + 截图升级**：普通表单继续使用原有扫描、填写、DataTransfer 媒体注入和提交，不因页面打开就调用模型。只有找不到提交入口、填写后仍缺必填/校验、多步骤、自定义组件、富文本或上传预览需要核验时，才写入 `visual_escalation` 并交给 `deepseek-v4-flash-vision-exp` 做“截图—小动作—新截图”的监督循环。上传成功也会升级一次视觉核验预览；`fillOnly` 场景会过滤最终提交/发布/创建草稿动作。验证码、OTP、登录/OAuth、付费/订阅、法律和破坏性动作仍停给人工。Product Hunt 在既有资料准备完毕后按同一升级路径由视觉智能体负责最终创建草稿与回执核验。

- **2026-09-10 3.3.13–3.3.19 通用实站修复**：ListAi 同 URL 文档替换后的 `Submission Received!` 回执现在会二次读取并入账；Hype Star 已由 3.3.18 在真实页面自动完成 URL、Review Draft、类别和跳过徽章五步，返回 `submitted for review / no account created` 并自动记为待审核。3.3.19 合并同一次自动化并发识别出的重复回执记录。全套 Node 测试通过。

- **2026-09-09 3.2.4 通用截图智能体（已被 3.2.5 调整）**：首次接入每轮新截图、稳定 DOM、动作记录和结果回读；其“所有站点先走视觉”的入口已撤回，保留为复杂/失败场景的能力层。

- **2026-09-09 3.2.0 可信自动化**：新增 run/attempt/step 正规化账本、MV3 持久 outbox、R2 不可变截图、复杂组件的标注截图视觉兜底、安全多步骤导航和提交前后证据差分。模型没有 click/submit 权限；无可信回执只进 `submitted_unconfirmed`。真实仅填表批次确认 Neil Patel 不再误点 `Try Free`，停止保留人工页签并关闭自动页签。

- **2026-09-08 校验失败再补填**：规则填完后 AI 只补一轮；提交后仍报漏填再补一轮，不再空转。
- **2026-09-08 防串站**：自动填表只绑定侧栏当前 Profile，且该 Profile 必须在本页有待提交任务；全局评论模板不再覆盖产品文案；提交前核对品牌/域名，对不上就停。
- **2026-09-08 未知栏回填 + 无验证码提交**：可复用未知栏写入 Profile 并同步 Neon；本站一次性选项不入库。目录站无验证码代点提交，有验证码页签留下；成功回执才写账本。
- **2026-09-08 设置页去提示、时间线可滚动**：去掉设置页顶部说明和同类无关提示，只留核心操作。时间线长笔记在条目内滚动，编辑/删除图标不再被挤出；再点一次当前站点标记可取消为未分类；「最近动态」改为显示最新一条内容而不是时间。
- **2026-09-07 外链库标记与卡片修正**：记录字段改为自动换行；置顶/编辑/删除去掉按钮外框，只保留图标。卡片点击只选中当前站，只有入口链接会打开网页。右侧补上与侧边栏相同的站点标记，写入 `siteAnnotations` 并同步 Neon。设置页顶部「填表请用侧边栏」提示已去掉。
- **2026-09-07 外链库卡片密度**：外链卡片改为横排摘要（状态、质量、进度同一行），置顶/删除与时间线编辑/删除改为图标按钮，减少空白以多展示条目。外链库、时间线和网站资料一样会自动写入 Neon。
- **2026-09-07 Settings 站点列表与外链时间线**：网站资料左侧改为可拖动排序的站点列表，点击后右侧显示对应资料，支持增删改。外链库右侧改为当前站时间线（增删改），云端/备份/监控折叠到“配置后一般不用打开”；连接并完成首次迁移后，本地变更会自动写回 Neon。外链卡片不再展示已合并的表格原始字段。

- **2026-09-06 3.0.0 云端数据中心**：Neon 状态首迁移与 R2 媒体首迁移均已完成。线上回读为 17 类有值文档（接口支持 21 类）、2,905 条外链、29 条提交记录、76 组/136 条当前时间线；续传补齐 105 条时间线修订。30 个 R2 媒体（6 Logo、24 截图）已逐个重新下载，SHA-256、大小、Profile 引用和迁移字段引用全部一致。
- Settings 在检测到 `migratedAt` 后自动禁用“首次迁移到云端”，避免日常误点；云端回读保留作手动恢复，立即保存只作自动同步失败时的重试。
- 设置页“网站资料”已改为左侧站点操作栏 + 右侧基本信息/内容描述/锚文本规则三栏，“全局配置”为横向三栏；已修复网格样式让非激活面板无法隐藏、导致顶部三个标签按钮看似失效的回归。Profile 资料区显示云端 R2 Logo/4 张截图预览，原始表格字段改为默认折叠的兼容区。
- 蓝色悬浮 `EL` 是手动补填按钮；已收紧为只在评论/目录提交表单出现。设置「全局配置」可关掉。
- Settings 2.9.0：外链卡片展示表格原始字段、提交网站和最近动态；时间线可按 Profile 追加提交、审核、上线和跟进记录；网站资料保留全部 Field / Content / Notes。
- `Link Submit` 现表头为 Link / SubmitProject / Submit / Time / Record / Detail；同步已按此读取。
- 2.9.2 已内置 2026-09-05 Sheet 快照：6 个项目、2,905 个外链站、29 条核验提交记录；重载扩展即可离线使用，不需要先启动 Agent 或点“应用同步”。
- 2.9.3：固定图标右键可直接打开设置；侧边栏按当前外链站展示 Profile 区分的最新提交、审核、上线和跟进动态；外链库卡片改为全宽单列。

## 1. 已确认工作流

用户先勾选一个或多个自家网站 Profile，再按外链站分组执行。每个外链站只打开一次，组内按勾选顺序逐个提交尚未成功的 Profile。

示例：

- 本轮选择 B、C。
- D 已有 `D × B = success`，因此 D 只提交 C；C 成功后写入账本。
- E 没有历史记录，因此同一页签依次提交 B、C。
- 下一轮 D 的 B、C 均已有成功记录，整个 D 分组跳过。

## 2. 当前架构与真相源

| 数据           | 真相源                              | 说明                                                       |
| -------------- | ----------------------------------- | ---------------------------------------------------------- |
| 自家网站资料   | `siteProfiles`                      | 使用稳定 Profile ID                                        |
| 本轮多选       | `selectedSiteIds`                   | 记住最近选择                                               |
| 永久成功       | `submissionRecords` v2              | 唯一键 `destinationKey + profileId`                        |
| 外链站状态     | `siteAnnotations`                   | `paid/broken/skip/deleted` 永久排除；登录/验证码仅临时闸门 |
| 运行批次       | `activeBatchRun`                    | 只恢复 running / waiting_manual / paused                   |
| 人工维护源     | Neon 云端工作区                     | 网站资料、外链库、分类、账本、时间线；扩展自动同步           |
| 离线安装种子   | `Table.xlsx` / `table-library.json` | 仅首次安装和回滚，不要求日常维护                            |

永久成功只允许两种来源：

1. Agent 看见成功页、成功文案等明确证据。
2. 用户人工点击“确认成功”。

## 3. 本轮完成

### 2026-09-05 2.9.1 离线查看、跟进筛选与卡片信息密度

- [x] Settings 初始化只读本地缓存，不再自动探测 `127.0.0.1:8790`；点击“检查连接”才探测 Agent。
- [x] 已应用的 `sheetTableData` 在 Agent 离线或断开 Google 时继续作为完整外链库缓存。
- [x] 新增表格提交动作（未核验）、已提交、待确认收录、待审核、待跟进、已收录、被拒绝、疑似丢链和未提交筛选。
- [x] “Link Submit.Submit 历史动作”和“有明确证据的提交成功”分开，避免把旧勾选伪造成已收录。
- [x] 多 Profile 卡片不会因其中一个已收录而隐藏另一个仍待跟进的项目。
- [x] 卡片首屏展示入口 URL、提交项目、当前进度、提交时间、最近动态、Record、Detail 和独立备注。
- [x] 自动检查 Sheet 默认关闭；只有主动拉取、回写或启用自动检查时需要本机 Agent。

### 2026-09-05 表格全字段与外链动态时间线

- [x] `Link Submit` 的每个原始列、Record、Detail 和原始行号完整进入同步快照与外链卡片。
- [x] Profile 子表全部 Field / Content / Notes 保留到 `siteProfiles`，网站资料页可统一查看和编辑。
- [x] 新增 `submissionTimeline` 追加式事件模型；同一外链站可分别记录多个 Profile 的提交、待审核、上线、拒绝、需跟进、链接失效与笔记。
- [x] 旧 `submissionRecords` 和 `Link Submit` 历史幂等迁移，不把旧 Submit 标记伪造成明确成功证据。
- [x] 外链卡片展示最近动态、状态标签、完整时间线和表格原始字段；2,905 条记录支持分批加载。
- [x] 备份导入导出覆盖时间线和 Sheet 快照；时间线合并只追加、不覆盖既有历史。
- [x] 继续使用 `chrome.storage.local` 作为运行缓存，无需新增数据库；Google Sheet 仍是人工维护源。

### 2026-09-05 发布分级、标准评论可选代点、熟站 playbook

- [x] `submissionRecords` 增加 `publicationStatus`（已提交 / 待审核 / 已上线）；成功跳过只认带可信证据且由 `agent/manual` 确认的 `status: success`。
- [x] Settings 增加标准 WP 评论代点开关，默认关闭；验证码、iframe、缺字段不代点。
- [x] 10 个高转化目录 playbook，侧栏和外链库可见，回执文本用于分级。
- [x] 链接监控 live 只升级为已上线；Sheet 增加 `PublicationStatus` 列。

### 2026-08-28 竞品能力补齐与 Sheet 回填

- [x] 新增 0–100 外链机会质量分、质量 Tag、筛选/排序与最低分队列闸门；评分保留 Sheet 的 DR/DA/Traffic/Spam/Dofollow/Indexable 等可选字段。
- [x] 新增已发布外链存活监控；只检查成功账本中的公开结果页，不把提交入口当成成果页，不自动撤销成功记录。
- [x] 评论工作室升级为 3 条候选、独立填入、可编辑、字段长度提示与上一版恢复。
- [x] 媒体预检展示当前 Profile 的本地 Logo/截图，并回显文件字段实际上传或跳过原因。
- [x] Google Sheet 增加定时变化检查与通知；仅生成待应用预览，仍保留人工应用门槛。
- [x] Google Sheet 36 个媒体路径单元格已改为真实绝对路径；本机 Agent 回读 6 个 Profile、2,905 条外链、29 条记录、0 冲突，全部本地媒体目录指向统一 `media/` 根目录。
- [x] 本机 Agent 已重启并验证 OAuth、媒体清单（6 个 Profile、31 个文件）和 Sheet 快照。
- [x] 全部 Node 测试、Python 42 项、脚本语法与 diff 检查通过；Settings 和窄侧栏完成截图验收。
- [ ] Chrome 重载 2.9.1 后检查本地缓存数量、进度筛选和卡片首屏字段；只有需要拉取 Sheet 最新改动时才执行预览与应用。

### 2026-08-25 Google Sheet 单一维护源

- [x] Settings 增加 Google 连接、变更预览、应用、账本重试回写和断开入口。
- [x] 本机 Agent 持有 OAuth，强制单工作簿白名单；扩展不保存 refresh token。
- [x] Sheet Profile、外链和分类同步到运行缓存；本地强成功证据、上传 Logo 和学习映射不会被空值覆盖。
- [x] 成功先写本地 `submissionRecords`，再通过 `sheetSyncOutbox` 幂等回写 `Submission Records`。
- [x] 旧 `Submit=1` 只保留为迁移痕迹，不再整站跳过；永久成功只认组合账本。
- [x] XLSX 回滚导入补齐 VideoToArticleAI，JSON 灾备改为证据优先合并。
- [x] 真实工作簿已新增 `Submission Records`、`SyncMeta`，现有 `VideoToArticle` 子表已核验。
- [x] 创建 Google Cloud OAuth 客户端并完成首次真实授权、预览、应用、回写验收；2,902 个 destination 与 6 个 Profile 已落盘，自动同步已启用且无待回写记录。

### 数据模型与迁移

- [x] 新增版本化 `submissionRecords` v2。
- [x] 成功记录包含 `submittedAt`、`confirmedBy`、证据摘要和 Profile 信息。
- [x] 旧 `submittedProjects` 幂等迁移，旧字段暂不删除。
- [x] Table 历史记录按“外链站 × Profile”展开并幂等导入账本。
- [x] Table 项目初始化为稳定 Profile ID。
- [x] 提交账本 JSON 导入/导出；校验 Profile ID 并合并现有数据。

### 队列与调度

- [x] 每次开始都从 Profile、账本、分类和外链库重建，不复用旧 `tasks`。
- [x] 队列模型改为“外链站分组 → Profile 子任务”。
- [x] 已成功组合自动排除；空分组自动跳过。
- [x] 同站下一 Profile 复用页签并重新进入原提交入口。
- [x] 无法重置表单时进入待人工，不写成功。
- [x] 登录、验证码和待人工任务停放并释放并发位。
- [x] `paid/broken/skip/deleted` 终止该站剩余子任务。
- [x] 单项目失败不把整个外链站判坏。
- [x] 游标按稳定 destination key 推进，过滤当前项后不跳过下一站。

### UI 与安全

- [x] Side Panel 分为“执行 / 批量 / 待人工”。
- [x] 手动填表 Profile 单选与批量 Profile 多选分离。
- [x] 批量开始前显示外链站数、待提交组合数和历史成功数。
- [x] 运行状态显示“外链站 / 当前项目 / 本站进度 / 总进度”。
- [x] 日志与任务行带 Profile 名称。
- [x] 待人工列表支持继续处理与人工确认成功。
- [x] 外链库移至 Settings，支持查看、删除、置顶、分类筛选和各 Profile 状态。
- [x] Screenshot 1–4 统一为媒体列表。
- [x] 按钮和输入最小触控高度 44px；补齐键盘焦点和空状态。
- [x] 新增 `DESIGN.md`。
- [x] 网页、AI、Profile、URL、域名、备注和日志不拼入 `innerHTML`。

### 2026-07-30 表格与媒体同步

- [x] 当前 Google Sheet 与仓库 `Table.xlsx` 同步完成。
- [x] 新增并填完整 RainbowPetAI 子表；RspAi、OldPhotoLive 的无效占位邮箱已清理。
- [x] 5 个 Profile、59 条 canonical 表格外链和 12 个历史成功组合可由导入脚本稳定展开。
- [x] RainbowPetAI、RspAi、OldPhotoLive 已同步到 Chrome 扩展，保留稳定 Profile ID。
- [x] 3 个网站均补齐 Logo 与 Screenshot 1–4；本地 Logo 同时嵌入备份，扩展重装后可恢复。
- [x] 表单文件字段会区分 Logo 与 Screenshot 1–4，不再把同一张 Logo 填入所有截图位置。
- [x] 外链库历史状态使用完整账本键查询；SourceForge、SideProjectors 等实机显示正确。

### 2026-08-02 TheJOAI 表单兼容性修复

- [x] 普通字段扫描覆盖 `contenteditable` 富文本编辑器，并兼容 Quill 的非空状态与输入事件。
- [x] 主图和 Logo 支持跨域代理下载、格式转换、16:9 裁剪及站点最大尺寸限制。
- [x] 日期字段缺少资料时使用当天日期并在侧栏明确提醒核对。
- [x] 必选分类按 checkbox/radio 组处理，自动根据 Profile 标签选择匹配分类；选填空字段不再阻塞。
- [x] TheJOAI 首次 DOM 检查确认主图、描述、Launch Date、Categories 的必填约束，并在文件为空时阻止提交。
- [x] 首次预审发现文件输入为空并阻止提交；重新处理后 Main Image 与 Logo 均使用真实文件上传并完成最终提交。

### 2026-08-02 RainbowPetAI 外链提交与交接

- [x] TheJOAI：账户显示 `Submitted for Review`。
- [x] Launching Next：`SUBMITTED / In Queue`，回执 `i=143041`。
- [x] StartupBase：产品页显示 `Pending Review`，进入标准免费队列。
- [x] AITools.inc：Typeform 显示完成回执。
- [x] Uneed：免费排期至 2027-01-14。
- [x] FutureTools：用户人工确认已提交，按 `confirmedBy: manual` 入账。
- [x] SideProjectors：项目 ID `87446`，确认页显示审核中；根入口别名归并到 `/submit`。
- [x] PitchWall：账户显示 `Product Status: Under Review`，免费方案预计超过 30 天发布。
- [x] 新增可合并导入的增量账本 `data/submission-handoff-2026-08-02.json`。
- [x] 新增专用外链提交交接表，集中免费入口、今日记录、阻塞状态和 AI 操作规则。
- [x] 新增并安装 `$external-link-operator` Skill；自动从各项目 `public` 解析媒体，固化 Luna 分工和提交闸门。
- [x] GitHub：公开 `Syndred/pet-memorial-resources` 资源库，README 和 Public 页面已核验。
- [x] Hacker News：已登录 `syndred`，完成首条真实社区评论并回读公开评论 `item?id=49192618`；尚未提交 RainbowPetAI。
- [x] Indie Hackers：账号已登录并具备评论权限，完成首条真实社区评论；发帖权限仍待社区解锁。
- [x] Product Hunt：已创建 RainbowPetAI 发布草稿，必填检查 100%，Logo、三张图库、Maker、Free Options、首评和 `Pets / Social Impact` 标签已核验；尚未排期或上线。
- [ ] FiveTaco 缺少明确回执，保持待复核，不写成功账本。

### 2026-08-06 社区参与流程试跑

- [x] Luna 只读筛选候选和草稿，主代理复核实时页面并负责发布。
- [x] Hacker News：在产品取舍讨论中发布不带链接、不含品牌植入的实质性建议；公开评论为 `https://news.ycombinator.com/item?id=49192618`。
- [x] Indie Hackers：在 AI 工作流语义修复讨论中发布安全边界建议；公开评论含 `commentId=-OzKSLXOfRwBN3JXQJFm`。
- [x] Product Hunt：在 Kiro Crew 发布页提出生产变更人工批准、断点恢复和跨渠道密钥隔离问题；页面显示 `Syndred / 1m ago`，未点赞、未带自链。
- [x] 社区互动写入独立日志 `data/community-participation-log.json`，不计入 `submissionRecords`，也不写入外链成功表。

### 2026-08-06 Product Hunt 草稿

- [x] Cloudflare 验证后确认登录账号为 `@syndred`。
- [x] 草稿页面明确显示 `This product is a draft and is not scheduled for a launch yet.`。
- [x] 草稿产品页：`https://www.producthunt.com/products/rainbowpetai?launch=rainbowpetai`。
- [x] 未宣传 coming soon 的 AI 视频、肖像或讣告功能；Luna 复核后移除 `Artificial Intelligence` 标签。
- [x] 已正式上线；2026-08-10 产品页显示 `Launched this week` 和 `Launched in 2026`，并已写入成功账本。

## 4. 自动测试

### 2026-08-09 社区参与巡检

- [x] Hacker News：现有会话显示 `login`；结合最近两条评论均 `[flagged]`，当天暂停互动，未发布。
- [x] Indie Hackers：个人页回到社区首页且显示 `Join`；未重复登录、未评论，正式发帖权限无法核验。
- [x] Product Hunt：账号页触发 Cloudflare 安全验证；按闸门停止，未点击验证、未评论、未点赞。
- [x] 三个平台均无新的正式发布权限证据；普通互动成功账本和 RainbowPetAI 外链账本均未改动。

### 2026-08-10 社区参与巡检与 Product Hunt 上线核验

- [x] Hacker News：账号已登录，但最近两条评论仍 `[flagged]`；整个平台继续暂停，未发布。
- [x] Indie Hackers：完成外链 gap 帖全文与 11 条回复复核，但评论入口仍指向 `/sign-up`；未强行互动。
- [x] Product Hunt：在 Argos 页面发布一条关于本地运行、远程触发和数据边界的真实问题，公开显示 `Syndred / 1m ago`；未点赞、未带自链。
- [x] RainbowPetAI Product Hunt 产品页已公开上线，并按正式资源页写入 `submissionRecords`；这条正式上线不与普通社区评论混记。
- [ ] Google Sheet 与 `外链提交交接表.xlsx`：当前没有连接的表格会话，工作簿依赖加载超时，保持待同步，不伪造完成。

### 2026-08-11 社区参与巡检

- [x] Hacker News：账号已登录，但最近 24 小时已有一条用户侧评论，且更早两条评论仍为 `[flagged]`；当天未新增互动。基础 `/submit` 表单可见，但未测试 RainbowPetAI 域名级限制，正式提交权限不标记为恢复。
- [x] Indie Hackers：`/new-post` 已显示编辑器、`SUBMIT POST`、`SAVE AS DRAFT` 和 `PREVIEW`，正式发帖权限视为已解锁并仅作记录；候选帖评论和回复入口仍指向 `/sign-up`，当天未评论。
- [x] Product Hunt：在 oqoqo 讨论页发布一条关于远端权威 before/after 状态、代理自报成功和 `unable to verify` 独立计分的真实评论；公开显示 `Syndred / 1m ago`，未点赞、未带自链。
- [x] 普通社区互动继续只写 `data/community-participation-log.json`，未改 RainbowPetAI 外链成功账本、Google Sheet 或工作簿。

### 2026-08-12 社区参与巡检

- [x] Hacker News：账号已登录，8 月 10 日用户侧评论仍公开，但更早两条评论继续 `[flagged]`；整个平台继续暂停，当天未新增互动。
- [x] Indie Hackers：唯一相关候选无法稳定完成全文、回复和评论入口核验；Newest 页面被大量短时 IPTV 推广帖占据，当天选择不发。
- [x] Product Hunt：账号正常且无 CAPTCHA；Tines 3B 虽与代理安全相关，但已有 48 条讨论覆盖相近角度，分页回复无法完整去重，当天未评论、未点赞。
- [x] 三个平台正式发布权限均无新的可确认变化；普通社区日志之外的成功账本、Google Sheet 和工作簿未改动。

### 2026-08-13 社区参与巡检

- [x] Hacker News：账号已登录，但两条历史评论继续 `[flagged]`；整个平台暂停，当天未新增互动。
- [x] Indie Hackers：`/new-post` 正常显示 Syndred 发帖编辑器，正式发帖权限无变化；相关候选页普通评论入口仍指向 `/sign-up`，当天未评论。
- [x] Product Hunt：在 Unsloth Desktop 产品页发布一条关于 8GB GPU 下载前内存估算、KV cache/上下文开销、安全上下文长度和量化回退的真实问题；公开回读节点为 `comment-5782086`，未点赞、未带自链。
- [x] 普通社区互动只写 `data/community-participation-log.json`；RainbowPetAI 外链成功账本仍为 10 条，Google Sheet 和工作簿未改动。

### 2026-08-14 社区参与巡检

- [x] Hacker News：账号已登录，但两条历史评论继续 `[flagged]`；整个平台暂停，当天未新增互动。
- [x] Indie Hackers：Google Ads 复盘候选全文与 6 条回复已核验，但具体页面的评论、点赞和回复入口仍指向 `/sign-up`；当天未互动。
- [x] Product Hunt：账号和当日榜单正常；前一日刚完成真实评论，今天没有明显新增价值的非重复角度，因此未评论、未点赞；昨日页面附带的非必要 Unsloth 自动关注已恢复为未关注。
- [x] 三个平台正式发布权限无变化；普通社区日志之外的 RainbowPetAI 外链成功账本仍为 10 条，Google Sheet 和工作簿未改动。

### 2026-08-16 社区参与巡检

- [x] Hacker News：账号已登录，但两条历史评论继续 `[flagged]`；整个平台暂停，当天未新增互动。
- [x] Indie Hackers：Pickle 浏览器 agent 帖全文与 17 条回复已核验，但具体页面的评论、点赞和回复入口仍指向 `/sign-up`；当天未互动。
- [x] Product Hunt：账号页触发 Cloudflare 安全验证；按闸门立即停止，未点击验证、未评论、未点赞，当前正式发布权限无法重新确认。
- [x] 8 月 15 日没有执行证据，未伪造补写；普通社区日志之外的 RainbowPetAI 外链成功账本仍为 10 条，Google Sheet 和工作簿未改动。

### 2026-08-20 社区参与与 Product Hunt 免费宣传

- [x] Product Hunt：在官方 `p/self-promotion` 的 “What are you building?” 讨论发布 RainbowPetAI 真实介绍，公开回读为 `Syndred / comment-5799679`；未点赞、未索要 upvote。
- [x] Product Hunt：补全 `@syndred` About 与 `https://rainbowpetai.com/` 链接，公开个人页回读成功；站内付费 Promote 起步预算为 $5,000–$10,000，未购买。
- [x] Indie Hackers：页头显示已登录头像，但候选帖的评论入口仍真实跳转 `/sign-up`；未重复登录、未评论。
- [x] Hacker News：8 月 17 日评论仍公开且未 `dead/deleted`；新候选完成正文、README 和回复去重，等待最终发布确认。
- [x] 普通社区互动只写 `data/community-participation-log.json`；RainbowPetAI 外链成功账本仍为 10 条，Google Sheet 和工作簿未改动。

### 2026-08-24 VideoToArticleAI 外链提交

- [x] 新增稳定 Profile `VideoToArticleAI`（`https://videotoarticleai.com`），种子见 `data/videotoarticleai-profile.json`。
- [x] Launching Next：免费提交成功，回执 `i=146539`，`In Queue` 约 4 个月；未购买 $99 Fast-Track。
- [x] StartupBase：加入标准免费队列，产品页 `https://startupbase.io/products/videotoarticleai` 显示 `Pending Review`；未购买 Launch now $39。
- [x] TheJOAI：Excellent submission +100 points；账户显示 Submitted for Review，Updated 2026-08-24 11:35:05。
- [x] SideProjectors：showcase 提交成功，项目 ID `90519`，确认页 under review；未挂出售、未勾选 ComingUp。
- [x] PitchWall：My Products 显示 Under Review；免费 Launch，未买 Premium/Pro。
- [x] StartupStash：Typeform 结束页 Thank you for applying；广告选 No。
- [x] AITools.inc：免费 Typeform Thanks we'll be in touch；未走 Fast Lane $129。
- [x] Uneed：免费 waiting line 已关闭，记为付费闸门，未购买。不算成功。
- [x] 账本对账 `audit-state.mjs --profile VideoToArticleAI` 通过（7 条）。日报：`docs/外链提交报告-2026-08-24.md`。
- [x] 2026-08-25：用本机已登录 Chrome 把 7 条成功按原 6 列格式写入 Google Sheet `Link Submit`。先前 H–J 乱入已清除。新子表已按 OldPhotoLive AI 结构写入 Profile；标签目前是 `工作表3`，需改名为 `VideoToArticleAI`。StartupBase 未写入 IndexPage。
- [ ] 当天目标 10 个免费成功，还差 3 个。SoMuch 卡验证码。
- [ ] 将 `data/submission-handoff-2026-08-02.json` 导入 Chrome 插件合并；本轮未点 Side Panel「开始提交」。

| 测试                                          | 状态                                                        |
| --------------------------------------------- | ----------------------------------------------------------- |
| `tests/queue-workflow.test.mjs`               | ✅ B/C → D/E、成功过滤、死路排除、迁移幂等                  |
| `tests/profile-migration.test.mjs`            | ✅ 旧 Profile 归并到稳定 ID、用户资料优先、重复迁移无副作用 |
| `tests/table-import.test.mjs`                 | ✅ 5 个项目、57 条外链、媒体 URL 与历史记录展开             |
| `tests/scheduler-workflow.test.mjs`           | ✅ 分组顺序、停放释放并发、稳定游标                         |
| `tests/backup-workflow.test.mjs`              | ✅ 导出/导入数据保持、Profile ID 校验                       |
| `tests/ui-workflow.test.mjs`                  | ✅ 三工作区、安全渲染、44px、Settings 功能入口              |
| `tests/extension-content.test.mjs`            | ✅ 扩展静态回归断言                                         |
| `tests/local-agent.test.mjs`                  | ✅ local_agent 静态契约                                     |
| `tests/local-agent-unit.test.py`              | ✅ 15 个单元测试                                            |
| `tests/external-link-operator-skill.test.mjs` | ✅ Skill 对账、媒体发现、alias 和幂等写入                   |

## 5. 尚未完成的真实验收

- [x] Chrome 已安装 ExternalLink，并从旧版 2.3.1 重新加载为 2.5.0。
- [x] Computer Use 实机检查“执行 / 批量 / 待人工”三工作区。
- [x] 旧 Profile 已归并到稳定 ID；批量页只显示 5 个唯一资料且保留 RspAi 勾选。
- [x] 扩展重新加载后 9 个历史成功组合仍在，外链库状态显示正确。
- [x] RainbowPetAI、RspAi、OldPhotoLive 的 Logo 与 4 张截图在 Settings 实机可见。
- [ ] Chrome 手工复现 B/C → D/E。
- [ ] 检查侧边栏日志和待人工恢复。
- [ ] 检查浏览器重启后成功账本、分类和 Profile ID 不变。
- [ ] 在 TheJOAI 页面确认 ExternalLink 内容脚本已注入后，点击插件“填入网站资料”，人工确认主图、富文本计数器和分类显示；浏览器控制扩展目前未注入 ExternalLink，未代点最终提交。

为避免对 227 个真实外链项启动提交，本轮未点击“开始提交”。因此表格同步、资料媒体、账本导入、状态显示和版本重载已实机验证，但 B/C → D/E 的真实提交闭环仍不能标记完成。

## 6. 后续

1. 真实目录站验收：无验证码代点后必须看到回执才入账；有验证码页签留下且不占并发。
2. 使用隔离测试外链或明确授权的真实目标完成 §5 剩余项。
3. 真实提交闭环通过后再启动 P2 难站视觉 Agent。
4. 之后再做博客评论观察/仿写；不改变当前账本和调度边界。

## 7. 提交记录

| Commit    | 内容                                            |
| --------- | ----------------------------------------------- |
| `a315936` | submissionRecords、分组队列和迁移               |
| `32ad4a2` | 同站顺序续跑、人工停放和稳定游标                |
| `0a31398` | 三工作区 UI、外链库、账本备份                   |
| `10f32fb` | 测试、DESIGN、版本和交接                        |
| `5724d14` | 旧 Profile 去重、稳定 ID 与账本同步迁移         |
| `14c83ce` | Google Sheet / Table 多项目、媒体和历史记录同步 |
| `6a67b79` | Logo / Screenshot 表单映射与外链库成功状态修复  |
| `f684e98` | 表格同步和 Chrome 实机验收交接                  |

# 2026-08-25 外链库扩充与视觉优化

- 从 Chrome 收藏夹已打开的聚合源中读取 Notion `100+ DIR / FREE` 清单；源表 144 个免费站点、218 个可见链接。
- 与 Google Sheet `Link Submit` 现有 57 个 URL 做 canonical/主机名去重，排除根页、通用表单、已知付费/失效入口和已有别名，保留 78 条明确 HTTPS 提交入口。
- 已追加到 `Link Submit` 第 59–136 行，指定 `VideoToArticleAI`，保持 `Submit=FALSE`，备注来源与“提交前复核当前免费状态”；回读 78 行 URL 完全一致。
- 已生成 `outputs/external-link-import-2026-08-25/免费外链候选导入.json`。该包仅合并 `urlList`，不含成功账本与 Profile，不会覆盖现有成功证据。
- Settings 外链库新增语义状态色、Profile tag、卡片层级、按钮间距、focus/hover、空状态与窄屏适配；UI、队列、Google 同步、备份测试通过。
- 待人工一步：在 ExternalLink Settings 的“导入账本”选择上述 JSON。原因是浏览器安全策略禁止 Agent 操作扩展内部页面，且本机 Google OAuth 客户端尚未配置，无法执行 Sheet → runtime 自动应用。

## 全量纠偏

- 首轮 78 条只是带明显提交路径的保守子集，不代表收藏夹聚合库总量。
- “大罗 SEO”已逐一读取 21 个子表，共 3,224 行；ViggoZ GitHub 仓库已解析 9 CSV + 9 Markdown，共 2,555 行、3,695 个有效 URL 出现记录；另合并 Web.Cafe 和 Notion FREE。
- 规范化并跨源去重后为 2,881 个免费候选：首轮 78 条已作为正式输入合并，其中 77 条与全量来源重合，`toolbattles.com/submit-product/` 作为唯一增量保留；Profile 1,480、社区/论坛 861、目录 452、文章/文件 88；源表标记即时通过 1,895 条。
- Google Sheet `Link Submit` 已扩容并追加至第 2,903 行；最终 CSV 回读 2,902 条数据记录，主清单对账缺失数为 0。
- 本批新增候选的 `SubmitProject` 不再默认写入 `VideoToArticleAI`；第 59–2,903 行已清空误填值并保留项目下拉框，由实际提交时选择对应 Profile。
- 2026-08-26 Google 同步已完成真实端到端验收：创建独立 Desktop OAuth 客户端、启用 Sheets API、完成账号授权；OAuth 启动不再使用 `include_granted_scopes`，避免 Google 附带既有 `openid/email/profile` scope 时被 oauthlib 拒绝。通过 Computer Use 重载 Chrome 扩展至 2.6.0，在真实 Settings 预览并应用同步：2,902 个 destination、更新 6 个 Profile、移除 0、待回写 0、分类变化 0、保护本地强证据 0；最终界面显示“自动同步已启用 · 无待回写记录 · 最近同步 2026/8/26 07:25:23”。
- 2026-08-26 合并 8 月 5 日遗留 Popup 改版并完成浏览器验收。首次实测发现“刷新合并队列”因后台响应重复携带完整 Profile 配置而报 `Could not serialize message.`；现已改为轻量队列摘要，并让 Popup 使用当前 `selectedSiteIds` 批量协议。修复后实机刷新为表 2,844 + 库 141 = 2,985 个可切换目标，网站资料、配置、日志标签均可用；未点击“开始提交”。
- 2026-08-26 完善提交媒体：新增 `media/submission-assets/`，按 6 个 Profile 保存源码 Logo 和各 4 张 1440×900 的真实线上功能截图；VideoToArticleAI 另生成透明 PNG Logo。本地文件均通过尺寸、文件大小和联系图视觉检查。Google Sheet 同步增加公开 `LOGO / Featured image / Screenshot 1–4` 与本地相对路径字段；OldPhotoLive 的 404 媒体改为可访问的 favicon/CDN 图片，GraffitiName 的二选一伪 URL 改为单一图片直链，VideoToArticleAI 的 HTML 页面地址改为两张已部署图片并保留完整本地四图。真实扩展预览为更新 6 个 Profile、移除 0、证据覆盖 0，应用后自动同步时间为 08:39:09。
- 插件全量合并包更新为 `outputs/external-link-import-2026-08-25/full/全量外链候选导入.json`。包内含 2,881 条 URL，成功账本和 Profile 均为空，不覆盖现有证据；861 个社区/论坛候选标记 `needs_manual`。
- Settings 增加“需人工”筛选及黄色状态 Tag，队列将 `needs_manual` 识别为人工闸门。

## 2026-09-08 批量崩溃修复（3.1.5）

- [x] 定位全量队列内存/存储膨胀：Profile 大对象由“每任务一份”改为“每 Profile 一份”。
- [x] `activeBatchRun` 升级为紧凑 v3 快照；17,250 任务复测约 1.21MiB。
- [x] 侧栏任务列表改为 180 条滚动窗口，保留真实总进度。
- [x] 调度器增加单实例锁，修复建页失败悬挂和最后页签关闭后不收尾。
- [x] 后台批量日志持久化 400 条；侧栏可查看、重开恢复和复制。
- [x] Chrome 3.1.3 实机启动 2,962 任务未崩溃，侧栏显示实时进度和持久化日志；仅填表模式未点击第三方提交。
- [x] 实机发现并修复日志重复、停止后标题仍显示运行中、后台自动开侧栏被 Chrome 拒绝三项问题；升级 3.1.4。
- [x] 全部脚本语法、17 个 Node 测试文件、压力测算和 `git diff --check` 通过。
- [x] 重载 3.1.4 后确认日志去重、关闭/重开侧栏恢复且无新增自动开栏警告。
- [x] 旧批次缺终止事件时改从 `activeBatchRun.status` 回读停止/完成/等待人工状态；升级 3.1.5。
- [x] 重载 3.1.5 后确认旧批次标题显示“已停止”；临时验收地址已从执行队列软删除，队列数从 2962 降为 2961。

## 2026-09-08 批量暂停/停止与 Product Hunt 闸门修复

- [x] 扩展版本升级到 3.1.6。
- [x] 批量运行增加独立「暂停批量 / 继续批量」状态；暂停保留队列、已填写内容和待人工页签，继续后恢复调度。
- [x] 「停止」只关闭普通自动任务页，保留 `needs_login` / `needs_captcha` / `needs_otp` / `needs_manual` 及 Product Hunt 等人工页签和待人工列表；停止后的队列不再被 `onRemoved` 自动重启。
- [x] 页面就绪改为 `tab.status === "complete"`、内容脚本可探测、内容信号连续稳定两轮；短暂 loading / SPA 标题壳不会提前转人工，超时会留下带诊断信息的人工原因。
- [x] Product Hunt playbook 标为 `custom_launch`，禁止走普通目录「无验证码代点提交」，稳定就绪后以 `needs_manual` 停放并显示「Product Hunt 多步骤发布需人工完成」，不误写成“需登录”。
- [x] 新增 `tests/batch-controls.test.mjs`，覆盖暂停队列谓词、自动/人工页签选择、互斥按钮状态、Product Hunt 路由和内容就绪信号；全量 18 个 Node 测试通过。
- [x] 二轮 review 修复停止后 `contentReady` / CAPTCHA 回调复活、状态写入覆盖竞态、导航后固定等待、loading + 表单假就绪、OTP 重入队及无页签确认后人工卡片残留。
- [x] Chrome 3.1.7 真实仅填表小批量复测：暂停后队列不前进、继续恢复；停止关闭普通自动页并保留人工页签/待人工卡片；未点击第三方最终提交。
- [x] 2026-09-09 真实 Chrome 仅填表小批量验证暂停和停止：加载中队列可暂停，停止后普通自动页关闭，Product Hunt 页签及待人工卡片保留。
- [x] 实测发现并修复 3.1.6 遗留：「请稍候…」标题壳误判 ready，以及 Product Hunt 根路径和 `/posts/new` 重复建页；版本升至 3.1.7。
- [x] 3.1.7 补测继续修复 Neil Patel 根路径/`/submit` 重复任务、暂停/继续重复日志和停止同文案重复日志；Product Hunt 「请稍候…」真实页面观察超过 30 秒仍保持「正在打开」，不提前转人工。

## 2026-09-09 Product Hunt 专用自动化

- [x] 使用 `OldPhotoLive AI` 真实 Product Hunt 草稿勘验完整步骤；Main info、Topics、自定义 Maker/Solo、Freemium、可选步骤和 Launch checklist 控件已逐项确认。
- [x] 真实上传 512×512 Logo 与 4 张 1440×900 Gallery 图片，页面出现对应预览；Launch checklist Required 为 `100% Complete`。
- [x] 移除 Product Hunt 稳定后直接 `needs_manual` 的硬拦截，改为专用多步骤状态机、阶段恢复点和安全按钮白名单。
- [x] 「停止」只按实际人工门判断是否保留 Product Hunt 页签；自动执行中的 Product Hunt 不再被误当人工页签。
- [ ] 重载 3.2.5 后用“确定性优先 + 截图升级”自动重跑真实 Product Hunt 草稿，回读草稿 URL/回执并写入可信账本；随后用至少两个非 Product Hunt 的异构目录站验证迁移能力。
- 2026-09-09 实站复测已自动从 Main info 经 Images and media 推进到 Makers；确认 Chrome 多个同名 Product Hunt 页签会让侧栏短暂保留旧 `activeTabId`，3.2.2 已在点击填表时重新解析真实活动页签，并识别 `data-test="maker-syndred"` 已选 Maker。3.2.2 重载前不把后续步骤或提交记为通过。
- 3.2.2 重载后复测证明活动页签与状态提示修复生效，但 Makers 仍因无 ARIA 选中态被保留；3.2.3 改为直接核验 `data-test="maker-<handle>"` 身份，并让专用 `Product Hunt First Comment` 优先于通用评论模板。
- 3.2.5 Worker 已部署为 `8018ea06-3239-45ca-a46c-2a5eec39876d`；仍需重载 Chrome 扩展后进行真实验收。代码静态/回归检查通过不等于真实提交验收，未取得新提交回执前不得标记为通过。
- [x] 最终复审补齐主机级合并对旧成功记录、人工标记、软删除、重启后人工页签恢复及表格 Profile 限制的兼容。
- [x] 二次代码审查修复 4 个 P0：最终回执分支顺序、SPA `waiting` 循环重试、隐藏状态控件阶段识别、验证码恢复回到 Product Hunt 专用循环。
- [x] 修复持久 Gallery 预览误报、重复文件 ID、Maker/Topics 选中确认及隐藏法律控件检查；21/21 Node 测试通过。
- [x] 真实 Saaspo / RainbowPetAI 页面返回 `Thank you! Your submission has been received!`；由此补齐 Webflow `has been received` 回执识别测试。
- [ ] Saaspo 这次发生在回执正则修复前，扩展仍记为 `submitted_unconfirmed`；不得把页面成功倒填为可信账本，需用后续新动作验证自动入账。
- [x] Chrome 已重载并实机回读 `3.3.12`；Product Hunt 已从入口跑到草稿回执，下方 3.3.12 验收记录为最新结论。

## 2026-09-10 通用智能体与真实站点验收（3.3.12）

- [x] OldPhotoLive AI 在 Product Hunt 真实创建草稿；成功页回读草稿提示、网站、4 张 Gallery、Maker、Topics 和首评。最终状态是已创建草稿、尚未排期，不写成已公开 Launch。
- [x] 修复成功页读取 `Document.textContent` 为空导致的漏记；改读 `document.body`，并统一 Product Hunt 入口页/成功页为同一主机级账本 key。
- [x] 成功页会从完整 Profile 库匹配项目，即使当前活动 Profile 已切换也可恢复记录；侧栏已真实显示 `OldPhotoLive AI 已提交`、证据文案和成功页链接。
- [x] 重复回读相同回执时幂等，不再追加重复时间线。现场修复前已产生的两条相同动态均指向同一草稿，不代表提交两次。
- [x] 用户重载后实机验收：`chrome://extensions` 回读 ExternalLink `3.3.12`；Product Hunt 成功页仍显示未排期草稿、站点链接、4 张 Gallery、Launch Team 和 Maker 首评；侧栏刷新后仍为修复前的 2 条历史动态，未新增第 3 条。
- [x] FutureTools 真实提交页复测：订阅弹窗不再抢占主表单，检测从 1 个字段恢复为 11 个字段；Cloudflare Turnstile 正确进入验证码门。
- [x] AIPURE 真实页确认一次性 `$69.90` 付费；GeekWire 要求华盛顿/俄勒冈/爱达荷/BC 总部且含 reCAPTCHA；Devpost 未登录。均正确停/跳，未填写不适用资料、未付费、未伪记成功。
- [x] 收紧非 WordPress 评论识别：只有同时存在评论正文字段和提交按钮才算评论表单，普通 POST 搜索表单不再误报。
- [ ] 仍需继续用免费、无验证码且资料适用的目录站取得更多新回执；每个第三方最终提交动作必须按站点在动作前确认，登录、验证码、OTP、付费和法律确认仍不绕过。

## 2026-09-17 云端优先同步（3.6.0）

- [x] 高频业务文档改为字段级增量同步，Worker 基于 Neon 最新数据原子合并，不再要求浏览器携带整份旧修订覆盖保存。
- [x] 待同步字段队列持久化，扩展启动自动回读；冲突或本地待写数据仍受保护。
- [x] 手动回读成功后回到「外链库」并显示 8 秒成功提示；手动保存显示保存数量或“云端已是最新”。
- [x] Worker 已部署生产版本 `62d0d4c6-e760-4f0d-864d-b052396d1d0f`；线上无损 PATCH 返回 200、修订 `2 → 3`、内容不变、Profile 仍为 7 个。
- [x] 全量 Node 回归 42/42、Worker 测试、脚本语法和 diff 检查通过。
- [ ] 在 Ego 扩展管理页重新加载并确认 `3.6.0`，再目视验收成功提示；浏览器内部扩展页不能自动代点。

## 2026-09-17 外链库个人管理、Quick Open 与 Submify 同步（3.7.0）

- [x] 侧边栏支持收藏、启用/停用、按 Profile 分配外链。
- [x] 提交队列执行前过滤已停用和未分配给当前 Profile 的目标。
- [x] 支持 Quick Open 分批打开筛选外链，明确不填表、不提交。
- [x] 设置页支持从 Submify 插件当前公开接口增量同步，保留既有资料和提交记录。
- [x] 同步前回读云端、同步后从云端快照核对总数与全部来源 ID。
- [x] 新增高风险/付费目标默认停用。
- [x] 全量测试 44/44 通过；Ego 本地页面目视验收通过。
- [x] Ego 已加载 `3.7.0` 并真实点击 Submify 同步；页面显示来源 100 条、新增 0 条、云端 2,975 条、云端版本 12，且本机核对 100/100 条来源记录已存在。

## 2026-09-24 三产品外链提交验收（3.7.30）

- [ ] 用户最新目标为 15 个三款齐全站点；当前 13 个，仍差 2 个。42 个产品×站点真实回执覆盖 15 站，其中 2 站并未三款齐全。
- [x] 3.7.38 已在 Ego 重载并目视确认「需人工」站点标记；Viesearch 三款均进入免费邮件确认，插件未误记成功。
- [x] 15 个有真实回执的站点自动补 `可提交`；验证码、登录、付费、跳过和人工门槛继续保留。设置页增量回读后新标记存在，待上传与冲突均为 0。
- [ ] Viesearch 三封邮件待用户确认，新增人工标记待云端回读；四个验证码页、Sites Plus 规则勾选及 Startup88 非通用创始人邮箱待人工处理；AIGC List 页使 Ego 控制接口超时，无法继续本轮 UI 提交。
- [ ] 继续找两个站点完成三款并核对精确账本、云端回读与站点标记；不能把登录、草稿或仅排队算成功。
- [x] 38 个产品×站点真实提交回执，14 个不同站点，12 站三产品齐全；Forward Future 新增三条，均为待审核。
- [x] 现场修正 Forward Future 重复描述、错定价与错分类；3.7.30 已修对应通用填表路由，并让同步面板显示冲突和待上传文档名。
- [x] 全量 Node 回归、扩展脚本语法和 diff 检查通过；本机 JSON 备份已导出。
- [ ] Ego 重载 3.7.30 并在下一站预填复验；解决当前云端 1 类冲突，确认提交账本/时间线的 Neon 回读。
- [ ] 继续完成剩余 18 个站点各三款；登录、验证码、付费和法律条款停放待用户处理。
- [x] 3.7.30 Ego 重载后确认旧批次冲突键是 `activeBatchRun`；Forward Future 三款重新强制刷新均显示云端动态最新。
- [ ] 3.7.31 侧栏非阻塞修复待重载，用 Taify 普通表单验证检测、自动填表与云端记账。
- [x] 3.7.31 实站确认填表动作恢复；Taify 显露未绑定标签和截图 URL 错填，3.7.32 已修并通过定向测试。
- [ ] 3.7.32 重载复测 Taify；站内 Turnstile 由用户完成，当前不能计提交。
- [x] 3.7.32 实站复验 Taify 名称、邮箱、截图 URL 已正确填写；Turnstile 仍停放。
- [x] AISO Tools OldPhotoLive AI 免费提交并公开上线，产品链接为 nofollow；累计 39 组合、15 站、12 站三产品齐全。
- [x] 3.7.33 修 AISO 提交/成功/公开路径站点归并与 Pricing Details 误填；52/52 回归通过。
- [ ] Ego 重载 3.7.33，核实 AISO 精确 Profile 本地与云端记录；补交 Graffiti、JevPlay。
- [x] 3.7.33 重载后 OldPhoto 来源页正确归并；人工登记已上线公开页，云端回读最新。
- [ ] 3.7.34 清理 AISO 跨标签保留的旧产品草稿，待重载后实站复验 Graffiti/JevPlay。
- [x] 3.7.34 重载实站复验 Graffiti 跨 Profile 旧草稿清理；Graffiti 和 JevPlay 均在 AISO 免费档公开上线，外链为 nofollow。累计 41 组合、15 站、13 站三款齐全。
- [ ] 3.7.35 重载验收 AISO 成功页上线分类与升级动态去重；Graffiti 联系邮箱的 MX 问题需修 Profile 数据。

## 2026-09-25 原创投稿与整站闭环闸门（3.7.44 源码）

- [x] AI Tools Directory 官网/嵌入 Paperform 明确拒收 AI 生成或复制文案；插件在该站所有自动填表和最终提交入口停放。浏览器已预填非文案字段，原创描述与邮箱留空，未提交。
- [x] OldPhotoLive 首装备用 Profile 按当前官网定价改为免费额度和一次性积分包，移除过期订阅与无限使用说法；Neon 正本的后续更新见下方续办记录。
- [x] 普通表单自动提交新增法律同意闸门；成功回执后新增精确本地记录、云端记录与时间线回读。云端未确认时保留页签并防重复提交，稍后同步成功则有界自动续跑；延迟关页增加当前 Profile、归属与实际回执 URL 检查。
- [ ] 3.7.44 尚未在 Ego/Chrome 重载和实站验收；浏览器内部扩展页受安全策略限制。首轮未运行测试，用户要求复测后已启动回归；不能宣称任意站都能无人值守完成。
- 详细交接：`docs/整站自动提交与原创投稿交接-2026-09-25.md`。

### 2026-09-25 补做云端正本与复测

- [x] Neon `siteProfiles` 中 OldPhotoLive 的 13 个旧种子字段按修订 88 → 89 更新；云端 GET 回读 13/13 一致，其他 7 个 Profile 内容不变。
- [x] 初轮 52 项 Node 回归 43 项通过；补装 Worker 依赖、修旧测试桩并新增云端精确回读、防重复提交、归属关页和 AI Tools Directory 嵌入表单覆盖后，Desktop 主项目完整回归 52/52 通过。
- [ ] Ego 已安装扩展的本地 `siteProfiles` 仍停在修订 88，pending/conflict 皆 0；Ego 画面捕捉失败且内部扩展管理页不能由自动化接管。3.7.44 实机加载、云端回读到界面和真实站点闭环仍待验收。
- [x] 3.7.45 针对旧台式机启动风险加入“先云端回读再放行自动上传”保护：恢复待上传队列不立即重试，回读遇 pending/conflict 则保持暂停，用户明确回读覆盖或手动上传后才能解除；台式机须先退出旧版浏览器再拉代码。静态/模拟回归已补，实机加载与回读仍待验收。

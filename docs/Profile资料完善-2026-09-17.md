# VideoToArticleAI 与 AISpeakLearn Profile 资料完善

日期：2026-09-17

## 已完成

- 更新 `VideoToArticleAI` 的标题、联系邮箱、短/中/长描述、目标受众、价值主张、使用场景、关键词、功能描述、卖点、定价、禁止宣传内容和锚文本规则。
- 将视频限制从旧的 `100 MB / 10 分钟` 修正为当前的 `500 MB / 40 分钟`，并写明超过 `100 MB / 10 分钟` 时使用浏览器本地音频提取、截图改为手动选择。
- 新增 `AISpeakLearn` Profile，并补齐官网、重点推广页、Logo、4 张场景图、三档描述、受众、价值主张、使用场景、功能、卖点、定价状态、宣传边界和锚文本规则。
- AISpeakLearn 的口径明确为：匿名英语试用 3 轮、无需银行卡、受每日额度限制；登录后支持英语/日语/西班牙语练习；付费订阅仍未开放。
- 两份资料已在 Chrome 当前加载的 ExternalLink 3.5.1 本机缓存和侧边栏中显示；侧边栏已能切换到 AISpeakLearn。

## 资料边界

- VideoToArticleAI 不宣传 URL/YouTube 导入已开放，不宣传无限处理、自动公开发布或保证事实准确。
- AISpeakLearn 不宣传当前使用 GPT-Live，不宣传发音评分、无限免费、人类教师、保证流利或付费订阅已开放。
- AISpeakLearn 当前使用公开站点图片 URL；尚未迁移为 `cloud-media://` 私有 R2 媒体。

## 云端同步状态

- 点击“立即保存”时，云端返回 `siteProfiles` 已在其他设备更新的版本冲突。
- 扩展保留了本机修改，并自动下载了两份 JSON 备份：
  - `~/Downloads/externallink-backup-2026-09-17 (1).json`
  - `~/Downloads/externallink-backup-2026-09-17 (2).json`
- 备份已核对，包含更新后的 VideoToArticleAI 和新增的 AISpeakLearn（内部 ID 为 `site-mu4wlxu0`）。
- 当前不能把云端写入标记为完成；需要在解决 `siteProfiles` 冲突后再执行一次“立即保存”，并以云端回读作为完成证据。

## 浏览器约定

- 用户要求后续默认使用 ego 浏览器。
- 本次继续使用 Chrome，仅因为 ExternalLink 扩展、云端连接和现有缓存已加载在该浏览器中。

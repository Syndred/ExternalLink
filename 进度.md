# ExternalLink 项目进度

> 最后更新：2026-08-13
> 扩展版本：2.5.0（Manifest V3）
> 当前路线：混合 C；P2 视觉 Agent / 博客仿写暂停

## 1. 已确认工作流

用户先勾选一个或多个自家网站 Profile，再按外链站分组执行。每个外链站只打开一次，组内按勾选顺序逐个提交尚未成功的 Profile。

示例：

- 本轮选择 B、C。
- D 已有 `D × B = success`，因此 D 只提交 C；C 成功后写入账本。
- E 没有历史记录，因此同一页签依次提交 B、C。
- 下一轮 D 的 B、C 均已有成功记录，整个 D 分组跳过。

## 2. 当前架构与真相源

| 数据 | 真相源 | 说明 |
| --- | --- | --- |
| 自家网站资料 | `siteProfiles` | 使用稳定 Profile ID |
| 本轮多选 | `selectedSiteIds` | 记住最近选择 |
| 永久成功 | `submissionRecords` v2 | 唯一键 `destinationKey + profileId` |
| 外链站状态 | `siteAnnotations` | `paid/broken/skip/deleted` 永久排除；登录/验证码仅临时闸门 |
| 运行批次 | `activeBatchRun` | 只恢复 running / waiting_manual / paused |
| 外链和资料种子 | `Table.xlsx` / `table-library.json` | 不再作为运行记录真相源 |

永久成功只允许两种来源：

1. Agent 看见成功页、成功文案等明确证据。
2. 用户人工点击“确认成功”。

## 3. 本轮完成

### 数据模型与迁移

- [x] 新增版本化 `submissionRecords` v2。
- [x] 成功记录包含 `submittedAt`、`confirmedBy`、证据摘要和 Profile 信息。
- [x] 旧 `submittedProjects` 幂等迁移，旧字段暂不删除。
- [x] Table 历史记录按“外链站 × Profile”展开并幂等导入账本。
- [x] Table 项目初始化为稳定 Profile ID。
- [x] 提交账本 JSON 导入/导出；校验 Profile ID 并合并现有数据。

### 队列与调度

- [x] 每次开始都从 Profile、账本、分类和外链库重建，不复用旧 `tasks`。
- [x] 队列模型改为“外链站分组 → Profile 子任务”。
- [x] 已成功组合自动排除；空分组自动跳过。
- [x] 同站下一 Profile 复用页签并重新进入原提交入口。
- [x] 无法重置表单时进入待人工，不写成功。
- [x] 登录、验证码和待人工任务停放并释放并发位。
- [x] `paid/broken/skip/deleted` 终止该站剩余子任务。
- [x] 单项目失败不把整个外链站判坏。
- [x] 游标按稳定 destination key 推进，过滤当前项后不跳过下一站。

### UI 与安全

- [x] Side Panel 分为“执行 / 批量 / 待人工”。
- [x] 手动填表 Profile 单选与批量 Profile 多选分离。
- [x] 批量开始前显示外链站数、待提交组合数和历史成功数。
- [x] 运行状态显示“外链站 / 当前项目 / 本站进度 / 总进度”。
- [x] 日志与任务行带 Profile 名称。
- [x] 待人工列表支持继续处理与人工确认成功。
- [x] 外链库移至 Settings，支持查看、删除、置顶、分类筛选和各 Profile 状态。
- [x] Screenshot 1–4 统一为媒体列表。
- [x] 按钮和输入最小触控高度 44px；补齐键盘焦点和空状态。
- [x] 新增 `DESIGN.md`。
- [x] 网页、AI、Profile、URL、域名、备注和日志不拼入 `innerHTML`。

### 2026-07-30 表格与媒体同步

- [x] 当前 Google Sheet 与仓库 `Table.xlsx` 同步完成。
- [x] 新增并填完整 RainbowPetAI 子表；RspAi、OldPhotoLive 的无效占位邮箱已清理。
- [x] 5 个 Profile、59 条 canonical 表格外链和 12 个历史成功组合可由导入脚本稳定展开。
- [x] RainbowPetAI、RspAi、OldPhotoLive 已同步到 Chrome 扩展，保留稳定 Profile ID。
- [x] 3 个网站均补齐 Logo 与 Screenshot 1–4；本地 Logo 同时嵌入备份，扩展重装后可恢复。
- [x] 表单文件字段会区分 Logo 与 Screenshot 1–4，不再把同一张 Logo 填入所有截图位置。
- [x] 外链库历史状态使用完整账本键查询；SourceForge、SideProjectors 等实机显示正确。

### 2026-08-02 TheJOAI 表单兼容性修复

- [x] 普通字段扫描覆盖 `contenteditable` 富文本编辑器，并兼容 Quill 的非空状态与输入事件。
- [x] 主图和 Logo 支持跨域代理下载、格式转换、16:9 裁剪及站点最大尺寸限制。
- [x] 日期字段缺少资料时使用当天日期并在侧栏明确提醒核对。
- [x] 必选分类按 checkbox/radio 组处理，自动根据 Profile 标签选择匹配分类；选填空字段不再阻塞。
- [x] TheJOAI 首次 DOM 检查确认主图、描述、Launch Date、Categories 的必填约束，并在文件为空时阻止提交。
- [x] 首次预审发现文件输入为空并阻止提交；重新处理后 Main Image 与 Logo 均使用真实文件上传并完成最终提交。

### 2026-08-02 RainbowPetAI 外链提交与交接

- [x] TheJOAI：账户显示 `Submitted for Review`。
- [x] Launching Next：`SUBMITTED / In Queue`，回执 `i=143041`。
- [x] StartupBase：产品页显示 `Pending Review`，进入标准免费队列。
- [x] AITools.inc：Typeform 显示完成回执。
- [x] Uneed：免费排期至 2027-01-14。
- [x] FutureTools：用户人工确认已提交，按 `confirmedBy: manual` 入账。
- [x] SideProjectors：项目 ID `87446`，确认页显示审核中；根入口别名归并到 `/submit`。
- [x] PitchWall：账户显示 `Product Status: Under Review`，免费方案预计超过 30 天发布。
- [x] 新增可合并导入的增量账本 `data/submission-handoff-2026-08-02.json`。
- [x] 新增专用外链提交交接表，集中免费入口、今日记录、阻塞状态和 AI 操作规则。
- [x] 新增并安装 `$external-link-operator` Skill；自动从各项目 `public` 解析媒体，固化 Luna 分工和提交闸门。
- [x] GitHub：公开 `Syndred/pet-memorial-resources` 资源库，README 和 Public 页面已核验。
- [x] Hacker News：已登录 `syndred`，完成首条真实社区评论并回读公开评论 `item?id=49192618`；尚未提交 RainbowPetAI。
- [x] Indie Hackers：账号已登录并具备评论权限，完成首条真实社区评论；发帖权限仍待社区解锁。
- [x] Product Hunt：已创建 RainbowPetAI 发布草稿，必填检查 100%，Logo、三张图库、Maker、Free Options、首评和 `Pets / Social Impact` 标签已核验；尚未排期或上线。
- [ ] FiveTaco 缺少明确回执，保持待复核，不写成功账本。

### 2026-08-06 社区参与流程试跑

- [x] Luna 只读筛选候选和草稿，主代理复核实时页面并负责发布。
- [x] Hacker News：在产品取舍讨论中发布不带链接、不含品牌植入的实质性建议；公开评论为 `https://news.ycombinator.com/item?id=49192618`。
- [x] Indie Hackers：在 AI 工作流语义修复讨论中发布安全边界建议；公开评论含 `commentId=-OzKSLXOfRwBN3JXQJFm`。
- [x] Product Hunt：在 Kiro Crew 发布页提出生产变更人工批准、断点恢复和跨渠道密钥隔离问题；页面显示 `Syndred / 1m ago`，未点赞、未带自链。
- [x] 社区互动写入独立日志 `data/community-participation-log.json`，不计入 `submissionRecords`，也不写入外链成功表。

### 2026-08-06 Product Hunt 草稿

- [x] Cloudflare 验证后确认登录账号为 `@syndred`。
- [x] 草稿页面明确显示 `This product is a draft and is not scheduled for a launch yet.`。
- [x] 草稿产品页：`https://www.producthunt.com/products/rainbowpetai?launch=rainbowpetai`。
- [x] 未宣传 coming soon 的 AI 视频、肖像或讣告功能；Luna 复核后移除 `Artificial Intelligence` 标签。
- [x] 已正式上线；2026-08-10 产品页显示 `Launched this week` 和 `Launched in 2026`，并已写入成功账本。

## 4. 自动测试

### 2026-08-09 社区参与巡检

- [x] Hacker News：现有会话显示 `login`；结合最近两条评论均 `[flagged]`，当天暂停互动，未发布。
- [x] Indie Hackers：个人页回到社区首页且显示 `Join`；未重复登录、未评论，正式发帖权限无法核验。
- [x] Product Hunt：账号页触发 Cloudflare 安全验证；按闸门停止，未点击验证、未评论、未点赞。
- [x] 三个平台均无新的正式发布权限证据；普通互动成功账本和 RainbowPetAI 外链账本均未改动。

### 2026-08-10 社区参与巡检与 Product Hunt 上线核验

- [x] Hacker News：账号已登录，但最近两条评论仍 `[flagged]`；整个平台继续暂停，未发布。
- [x] Indie Hackers：完成外链 gap 帖全文与 11 条回复复核，但评论入口仍指向 `/sign-up`；未强行互动。
- [x] Product Hunt：在 Argos 页面发布一条关于本地运行、远程触发和数据边界的真实问题，公开显示 `Syndred / 1m ago`；未点赞、未带自链。
- [x] RainbowPetAI Product Hunt 产品页已公开上线，并按正式资源页写入 `submissionRecords`；这条正式上线不与普通社区评论混记。
- [ ] Google Sheet 与 `外链提交交接表.xlsx`：当前没有连接的表格会话，工作簿依赖加载超时，保持待同步，不伪造完成。

### 2026-08-11 社区参与巡检

- [x] Hacker News：账号已登录，但最近 24 小时已有一条用户侧评论，且更早两条评论仍为 `[flagged]`；当天未新增互动。基础 `/submit` 表单可见，但未测试 RainbowPetAI 域名级限制，正式提交权限不标记为恢复。
- [x] Indie Hackers：`/new-post` 已显示编辑器、`SUBMIT POST`、`SAVE AS DRAFT` 和 `PREVIEW`，正式发帖权限视为已解锁并仅作记录；候选帖评论和回复入口仍指向 `/sign-up`，当天未评论。
- [x] Product Hunt：在 oqoqo 讨论页发布一条关于远端权威 before/after 状态、代理自报成功和 `unable to verify` 独立计分的真实评论；公开显示 `Syndred / 1m ago`，未点赞、未带自链。
- [x] 普通社区互动继续只写 `data/community-participation-log.json`，未改 RainbowPetAI 外链成功账本、Google Sheet 或工作簿。

### 2026-08-12 社区参与巡检

- [x] Hacker News：账号已登录，8 月 10 日用户侧评论仍公开，但更早两条评论继续 `[flagged]`；整个平台继续暂停，当天未新增互动。
- [x] Indie Hackers：唯一相关候选无法稳定完成全文、回复和评论入口核验；Newest 页面被大量短时 IPTV 推广帖占据，当天选择不发。
- [x] Product Hunt：账号正常且无 CAPTCHA；Tines 3B 虽与代理安全相关，但已有 48 条讨论覆盖相近角度，分页回复无法完整去重，当天未评论、未点赞。
- [x] 三个平台正式发布权限均无新的可确认变化；普通社区日志之外的成功账本、Google Sheet 和工作簿未改动。

### 2026-08-13 社区参与巡检

- [x] Hacker News：账号已登录，但两条历史评论继续 `[flagged]`；整个平台暂停，当天未新增互动。
- [x] Indie Hackers：`/new-post` 正常显示 Syndred 发帖编辑器，正式发帖权限无变化；相关候选页普通评论入口仍指向 `/sign-up`，当天未评论。
- [x] Product Hunt：在 Unsloth Desktop 产品页发布一条关于 8GB GPU 下载前内存估算、KV cache/上下文开销、安全上下文长度和量化回退的真实问题；公开回读节点为 `comment-5782086`，未点赞、未带自链。
- [x] 普通社区互动只写 `data/community-participation-log.json`；RainbowPetAI 外链成功账本仍为 10 条，Google Sheet 和工作簿未改动。

| 测试 | 状态 |
| --- | --- |
| `tests/queue-workflow.test.mjs` | ✅ B/C → D/E、成功过滤、死路排除、迁移幂等 |
| `tests/profile-migration.test.mjs` | ✅ 旧 Profile 归并到稳定 ID、用户资料优先、重复迁移无副作用 |
| `tests/table-import.test.mjs` | ✅ 5 个项目、57 条外链、媒体 URL 与历史记录展开 |
| `tests/scheduler-workflow.test.mjs` | ✅ 分组顺序、停放释放并发、稳定游标 |
| `tests/backup-workflow.test.mjs` | ✅ 导出/导入数据保持、Profile ID 校验 |
| `tests/ui-workflow.test.mjs` | ✅ 三工作区、安全渲染、44px、Settings 功能入口 |
| `tests/extension-content.test.mjs` | ✅ 扩展静态回归断言 |
| `tests/local-agent.test.mjs` | ✅ local_agent 静态契约 |
| `tests/local-agent-unit.test.py` | ✅ 15 个单元测试 |
| `tests/external-link-operator-skill.test.mjs` | ✅ Skill 对账、媒体发现、alias 和幂等写入 |

## 5. 尚未完成的真实验收

- [x] Chrome 已安装 ExternalLink，并从旧版 2.3.1 重新加载为 2.5.0。
- [x] Computer Use 实机检查“执行 / 批量 / 待人工”三工作区。
- [x] 旧 Profile 已归并到稳定 ID；批量页只显示 5 个唯一资料且保留 RspAi 勾选。
- [x] 扩展重新加载后 9 个历史成功组合仍在，外链库状态显示正确。
- [x] RainbowPetAI、RspAi、OldPhotoLive 的 Logo 与 4 张截图在 Settings 实机可见。
- [ ] Chrome 手工复现 B/C → D/E。
- [ ] 检查侧边栏日志和待人工恢复。
- [ ] 检查浏览器重启后成功账本、分类和 Profile ID 不变。
- [ ] 在 TheJOAI 页面确认 ExternalLink 内容脚本已注入后，点击插件“填入网站资料”，人工确认主图、富文本计数器和分类显示；浏览器控制扩展目前未注入 ExternalLink，未代点最终提交。

为避免对 227 个真实外链项启动提交，本轮未点击“开始提交”。因此表格同步、资料媒体、账本导入、状态显示和版本重载已实机验证，但 B/C → D/E 的真实提交闭环仍不能标记完成。

## 6. 后续

1. 使用隔离测试外链或明确授权的真实目标完成 §5 剩余项。
2. 真实提交闭环通过后再启动 P2 难站视觉 Agent。
3. 之后再做博客评论观察/仿写；不改变当前账本和调度边界。

## 7. 提交记录

| Commit | 内容 |
| --- | --- |
| `a315936` | submissionRecords、分组队列和迁移 |
| `32ad4a2` | 同站顺序续跑、人工停放和稳定游标 |
| `0a31398` | 三工作区 UI、外链库、账本备份 |
| `10f32fb` | 测试、DESIGN、版本和交接 |
| `5724d14` | 旧 Profile 去重、稳定 ID 与账本同步迁移 |
| `14c83ce` | Google Sheet / Table 多项目、媒体和历史记录同步 |
| `6a67b79` | Logo / Screenshot 表单映射与外链库成功状态修复 |
| `f684e98` | 表格同步和 Chrome 实机验收交接 |

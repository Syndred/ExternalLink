# 新会话交接：JevPlay 插件独立验收

## 2026-09-26 21:13 / CUA 重置后故障仍在

- 本轮重新绑定前后确认 Ego、Chrome 仍在运行；Finder 可以捕捉，Chrome/Ego 均为 ScreenCaptureKit `-3811`。
- `cua_repl.js_reset()` 后成功重新枚举浏览器，但 Chrome/Ego 捕捉错误不变、Finder 正常。该操作只重置 CUA 的 JS 会话，不会重启 Ego，且本次没有修复。
- Chrome/Ego 原生句柄仍不可用，无法安全重启；不要再重复 REPL reset，除非出现 native pipe closed 等新证据。等待支持的原生绑定恢复或 CUA 服务恢复入口；定时续办保持暂停。

## 2026-09-26 21:09 / 中断原因复核，heartbeat 已暂停

- 故障证据：20:40 CUA 曾成功读取 Ego 插件侧栏和 Neon；20:53 Chrome 原生捕捉成功但 Ego 间歇失败；20:59 后 Chrome/Ego 原生绑定返回 ScreenCaptureKit `-3811`。普通 Chrome 标签 API、Finder 捕捉仍可用，进程列表显示 Chrome/Ego 在运行。
- Apple 将 `-3811` 说明为 ScreenCaptureKit 内部流启动失败，不是录屏权限拒绝。最近 20 分钟系统日志没有对应错误或矩形校验信息；日志窗口所有者被隐去，离屏坐标无法与目标应用关联。因此原生捕捉通路是已定位阻塞，底层触发原因仍未知。不能声称浏览器/扩展崩溃或已重启 Ego。
- Neon Cloudflare 验证另行阻断云端精确回读，和 `-3811` 无直接因果证据。插件验收因无法安全访问侧栏及 Neon 查重而停留在 0/10。
- 用户要求停止定时后，`externallink` 已设为 `PAUSED`。不要自行恢复；只有用户明确要求时再续办。

## 2026-09-26 21:04 / CUA Chrome 预检完成，插件侧栏尚不可用

- 本轮实际用 `cua_repl` 打开并读取 NoAdsGames 官方发布页。免费表单要求 HTTPS 游戏直链可在该站 iframe 中加载，并要求如实确认发布权利和规则；Jev 兼容性未验证。未输入、未确认、未提交，不算插件尝试或成功；不要将其加入固定十站。
- Finder 原生捕捉成功，Chrome 与 Ego `getApp` 报 ScreenCaptureKit `-3811`，Chrome 重绑再次失败。清单显示应用运行，但没有可靠句柄；没有重启 Ego，也未触及扩展工具栏/侧栏或声称实载版本已确认。
- Neon 标签仍在 Cloudflare 安全验证页，未绕过、未读到 Neon。保留 Neon 页与 Jev Code Breaker 续办页；已关闭 NoAdsGames 页并读回确认。当前公开标签清单不含分组状态，本轮分组未动。
- 当前新 Jev 独立验收仍 0/10，十站未冻结。只做 Jev；先实时 Neon 精确查重；禁止重投 Alieradox 和 AI Tools Ratings 的 Jev。恢复原生控制后动作前回读标题/URL/空间，逐步验证插件侧栏与实载版本。

## 2026-09-26 20:59 / 已在 Chrome 继续，待恢复工具栏控制

- 本轮 fetch 后 HEAD `9468646` 与远端一致；Computer Use 已在 Chrome 浏览器 ID `6` 打开并读到 Jev Code Breaker 页面，标记为续办页。Chrome 普通网页标签可直接操作。
- 原生捕捉当前 Chrome、Ego 都报 `-3811`，Finder 成功；上一轮 Chrome 原生捕捉成功一次。不能归为全局无权限。当前无稳定窗口句柄，未重启 Ego，插件工具栏/侧栏与实载版本尚未核实。
- 20:55–20:58 日志记录全屏区域 `[0,0,1470,956]`，也有 `x=-233,width=168,height=170` 的小捕捉区域；没有 `contentRect/sourceRect` 明确校验错误或权限拒绝，根因仍未知。
- 当前活跃清单：Ego 只有原新标签；Chrome 保留 Jev Code Breaker 与原新标签。历史 CAPTCHA/登录/条款页不在清单中；原生标签组无法读取，不要声称已关闭。无本轮插件尝试、站方回执或 Neon 实时回读，0/10。
- 后续继续用 Chrome 公开页预检；原生捕捉恢复后，先核对标题/URL/空间，再操作 ExternalLink 侧栏并读实载版本；任何提交先实时 Neon 精确查重。禁止重投 Alieradox、AI Tools Ratings 的 Jev。

## 2026-09-26 20:53 / Chrome 捕捉正常，Ego 间歇 -3811

- 本轮远端同步无变化，HEAD `185ea3f`；Computer Use 确实操作 Ego 普通网页。Chrome 原生窗口捕捉成功；Ego 原生捕捉在本轮先报 `-3811`、中间成功一次、随后又报 `-3811`。普通标签 API 工作正常，所以故障不等于不能操作浏览器。无稳定原生句柄，不要声称已重启 Ego。
- 只读系统日志在 20:45–20:52 记录 ScreenCaptureKit 流暂停和一次 AVFoundation `-11800` / 状态 `-12902`，未见本轮 `contentRect does not contain sourceRect`，也无权限拒绝实证；`-3811` 根因仍待定位。继续通过公开页面做可完成工作，原生捕捉只在需要侧栏时单次复测。
- PLAXZY 免费投稿要求账号登录，未继续。PLIX.GG 免费且无 SDK，但要求 Jev 游戏可 iframe；其通用联系表单需 URL/标题/描述/缩略图。本轮只读预检，没有插件调用、填表、发送或提交；PLIX 是否满足 iframe 和 Neon 精确去重均待核实。
- Bing、PLAXZY、PLIX.GG 三个已完成页均已关闭并由 CUA 清单回读确认。保留本任务 Google/DDG CAPTCHA、DeskLink 条款、AxoGamer/iDev 登录页和仍含这些现场的 ExternalLink 组；其他 Chrome 窗口/分组不动。
- 本轮新增独立 Jev 成功为 0，继续按 0/10；未做实时 Neon 读取。下一步仍须先查重，再决定候选；禁止重投已有回执的 Alieradox、AI Tools Ratings。

## 2026-09-26 20:40 / CUA 已实操，侧栏捕捉又报错

- 最新 fetch 后工作区干净，`HEAD=origin/main=ac8c55a`。直接 Computer Use 打开 Ego 的 Neon Console 并运行只读查询：生产 `default/submissionRecords` revision 123，读到 26 条 Jev `status=success` 键。完整清单、重复 alias、无新增尝试等见 [Jev 十站验收文档](Jev插件独立十站验收-2026-09-26.md)。Alieradox 和 AI Tools Ratings 的 Jev 仅在历史站方回执中，当前 26 行无其精确键；不得重投或人工伪造账本。
- 曾成功通过 CUA 原生绑定点击 ExternalLink 工具栏，侧栏显示 JevPlay Profile、2 个云媒体。随后 `getAXState/getApp` 重复出现 `-3812 invalidParameter`，而 Finder 捕捉成功、普通公开 CUA 标签仍工作。Ego 仍运行且没有可用原生退出句柄，不称作重启；恢复时优先重新绑定一次，避免无限重试。
- 普通 CUA 直接预检：Rando iframe 兼容性未证；DeskLink 要求同意内容规则；AxoGamer/iDev 要账号；Paizix/TapCraftBox 只接邮件；AIToolsDirectory.com 禁用 AI 文案。Google 与 DuckDuckGo 都出现 CAPTCHA，保留现场，不解决。已关掉无人工门槛的研究页与 Neon 页；仅保留待条款/登录/CAPTCHA 页及用户原新标签页。
- 本轮没有插件检测/填表/提交，站方回执无新增，Neon 只读回读而非写入。新十站目标仍 0/10 且队列未冻结。上一轮把原生侧栏失效扩大成“不能操作浏览器”是错误判断；普通标签路径应继续用于公开预检，但绝不能用人工网页操作冒充插件独立完成。

## 2026-09-26 20:33 续验补记

- Ego 窗口地址栏已有来源不明的输入 `b`，本轮不接管。CUA 独立页读取 CrazyGames 官方要求并打开开发者投稿入口，现场需登录，无投稿表单；未登录或提交。研究页关闭，原 Ego 页保留。仍无新插件闭环或 Neon 回读。

## 2026-09-26 20:28 直接标签页操作已验证

- CUA 公开标签接口能在 Ego 原生捕捉 `-3811` 时直接导航并读取 SlowDen，下一轮普通站点预检优先用这条路径。Ego 曾退出，原因未查明；本任务只用 CUA 重新打开并看到新标签页。随后原生又失败，插件侧栏未操作。不要把直接网页操作算成插件独立填表或成功。
- SlowDen 明确用 iframe 展示外链游戏，Jev 兼容性待证；本轮未填/未发送。已关完本任务页，Neon/实载仍待回读。原五分钟自动化已更新并回读 ACTIVE。

## 2026-09-26 20:23 续办补记

- 本轮 CUA 对照 Finder 可绑定，Ego 也曾恢复，但仍指向另一任务运行中的 SEO 窗口；随后 Ego 再报 `-3811`。未重启、未切换该任务空间或操作插件。Apple 官方 `sourceRect` 文档显示单窗口捕捉不参考该值，Sky 矩形错误还需目标窗口/采集配置证据。
- Bounty Board Arcade 需先建账号，外链游戏还须可 iframe 嵌入及封面、操作说明、100 字简介；未满足入队前提，未提交。所有本轮研究页已关闭回读；Neon 和实载仍待恢复。详见 `docs/Jev插件独立十站验收-2026-09-26.md` 顶部。

## 2026-09-26 20:19 捕捉中断复测

- 已 fetch 并核对 `origin/main`；本轮实际使用 Computer Use。首次 CUA `native pipe is closed`，经 `cua_repl.js_reset()` 后清单恢复；**这不是 Ego 重启**。Ego/Chrome 原生绑定继续间歇出现 `-3811/-3812`，且 Ego 可绑定时仍选中另一任务的 SEO 窗口，没有可安全操作本任务插件的可靠句柄。
- 同时段 `SkyComputerUseService` 日志三次显示 `SCStream validateStreamConfiguration: contentRect does not contain sourceRect`，可把 `-3812` 定位到捕捉配置矩形校验，但尚无 `sourceRect` 与目标窗口证据，不能归因权限或具体显示器。现有五分钟 heartbeat 已写入该诊断、REPL 重置及动作前后窗口/URL/空间回读规则，回读 ACTIVE。
- CUA 确认 Jev Code Breaker 公开游戏页可打开；HTTP HEAD 不足以证明可嵌入。浏览器自动安全审核拒绝 `data:text/html` iframe 实测并禁止绕行，该候选继续待证。没有绕到其他界面测试。
- 已关闭本轮 Jev/IAB 页与误开的 Chrome 空白页并回读，只余另一任务 SEO、Search Console 页；无本任务活动分组。**本轮无插件真实尝试、无新回执、无 Neon 实时回读；新十站仍 0/10。** 下一轮先确认 SEO 是否还占用原空间，再复测受支持的原生窗口绑定；不要重复投稿 Alieradox、AI Tools Ratings。

## 2026-09-26 晚间巡检新增结论

- `externallink` 现有五分钟任务已要求每轮主动诊断并修复中断，含 Computer Use 是否实际使用、`-3811/-3812`、窗口/空间漂移、插件、Neon、网络和登录门槛；修复后须复测中断步骤。不要只留状态汇报。
- 本机 ScreenCaptureKit SDK 常量确认 `-3811 = internalError`、`-3812 = invalidParameter`，`userDeclined = -3801`；`-3811` 不能直接归因录屏权限。Ego 原生绑定本轮间歇成功，未重启 Ego。另有 SEO 任务使用原 Ego 空间，已新建空白空间；ExternalLink 侧栏可打开并切 JevPlay，显示 Jev 云端媒体 2 个文件，但实载版本和精确 Neon 账本仍未验证。
- 捕捉错误可能发生在 UI 动作已经生效之后；本轮一次导航误落 SEO 空间，已关闭本任务误开的 SlowDen 页并回读，原 SEO 页未动。操作前后必须确认当前窗口标题、URL、空间，避免旧 AX 索引跨窗口使用。新空白空间保留供续办；本任务研究页均已关闭。
- CUA 可按独立空间的公开标签 ID 精确进入 SlowDen；原生 `getApp('ego lite')` 同时仍选中 SEO 窗口，不能据此操作本任务的插件侧栏。SlowDen 预检后独立标签已回到 `about:blank` 并标记续办，未在错误空间继续插件动作。
- 目前仍无实时 Neon 精确去重，也没有新的插件填表或提交，十站独立闭环仍为 0。详细证据见 `docs/Jev插件独立十站验收-2026-09-26.md` 顶部。

## 最新用户要求

- 只提交 JevPlay，暂停 OldPhotoLive、GraffitiName 外部提交。
- 持续优化插件，真实独立完成率超过 50% 才验收。新 Jev 十站固定样本至少 6/10；保留每次失败及人工辅助，不用其他产品历史成功混算。尚未固定完整队列，本轮新增独立成功为 0。
- 普通卡死时自行关闭并重开 Ego，核对回执防重投后继续；已有其他自动化使用该空间则另建空间。用完关闭本任务页签和分组，不动其他任务。
- 每 5 分钟检查，空闲且未达标时重新拉起。现有 heartbeat ID 为 `externallink`，ACTIVE，已改为 5 分钟。迁移时更新这个任务的目标会话，不创建重复自动化。
- 优先项目已有实现和主流开源方式；自行 review、测试、频繁提交推送，维护中文交接。不要反复让用户重载。

## 当前代码与验证

- 原仓库 `/Users/syndred/Desktop/projects/ExternalLink`；main 已推送 `bb009fe`（3.7.106 修复）与 `b09d34e`（进度）。开始先 fetch 并核对最新状态。
- 3.7.106 复用 `findBestSelectOption`，修自定义下拉 Free 被排列在前的 Freemium 部分匹配抢选。覆盖初次下拉和搜索后匹配。54/54 Node 测试通过；`node --check extension/content.js`、`git diff --check` 通过。
- 3.7.105 补 Radix 选项的 mouse pointerdown/pointerup 与选择值回读；3.7.104 补 Alieradox 短暂成功 toast 的来源限定识别；前版修表单归属、异步切页标注、多步字段和媒体映射。
- **源码 3.7.106 未实载验证**。不能由 manifest 推断浏览器版本；也不能将源码回归当成现场完成。
- 新会话可能在独立 worktree 中：浏览器可能仍加载原仓库 extension 路径。修改代码后必须核实真正加载路径，完成正确集成后再验收，不能把 worktree 修改误认为原加载目录已更新。

## 主流实现对照

- Bitwarden：先采集字段与表单归属。https://contributing.bitwarden.com/architecture/deep-dives/autofill/collecting-page-details/
- Automa：显式控件定位、输入事件和流程。https://github.com/AutomaApp/automa/blob/main/src/content/blocksHandler/handlerForms.js
- Playwright：可访问名称、动作前检查、动作后可见状态验证。
- Radix：Select 鼠标选择在 pointerup 提交。https://github.com/radix-ui/primitives/blob/main/packages/react/select/src/select.tsx
- 详细对照、历史矩阵在 `docs/插件自动投稿开源对照与十站验收-2026-09-26.md`；新范围在 `docs/Jev插件独立十站验收-2026-09-26.md`。

## 浏览器真实阻塞与边界

- Ego bundle ID `com.citrolabs.ego.lite`；本轮用 CUA 启动，应用清单显示运行，但反复绑定窗口失败于 ScreenCaptureKit -3811。Chrome 原生窗口同样失败。没有返回可用句柄，退出快捷键无法发出，**尚未成功完成关闭/重开**。
- CUA 公开 Chrome tab API 仍能打开、读写普通页面。上轮 browser 1 是本任务 Chrome；browser 3 有另一个 Jev 游戏自动化，不争用。新会话应刷新清单，不盲用旧 ID。
- 此前 `chrome://extensions/` 和 extension settings URL 被浏览器安全策略明确拒绝，并要求不通过备用界面、CDP、间接命令绕过。该拒绝跨重试仍有效；重启仅用于普通故障恢复。
- 所有 UI 使用 CUA 支持接口。不要用 shell/AppleScript、内部扩展调用绕过控制限制。不要绕过 CAPTCHA、登录、OTP 或条款门槛。
- 本轮 Startups Lab 预检页已关闭，清单确认不存在。上轮设置页 `512543996` 尚在，因既有内部页策略不能声称已清理；用户 X 页与扩展管理页不要擅动。

## 云端、Profile 和去重

- Neon/Cloudflare 是唯一云端事实源，浏览器 storage 是缓存；写入后必须云端回读。普通提交不能通过修改种子或灾备 JSON 冒充同步。
- 最新只读导出 `/Users/syndred/Downloads/externallink-backup-2026-09-26 (3).json`，导出时间 `2026-09-26T11:16:49.979Z`，当时 pending/conflict 都为 0 并已云端回读。只是该时刻快照，执行前刷新当前云端。
- `submissionRecords` 中 26 条 `::JevPlay` 精确键；不要用 sheet 老 Submit 字段判断成功。按 canonical destination + profile 去重。
- Jev 联系邮箱为 `syndredyoung@gmail.com`，官网 `https://jevplay.com`，Free。Graffiti 同 Gmail；OldPhoto support 邮箱本轮不再使用，因为暂停该产品。
- **Alieradox Jev、AI Tools Ratings Jev 已有明确站方回执，但精确云记录未完成；禁止再投。**
- TipSeason、AISO Tools、BestOfAI、Credible AI Tools、AI Tools Directory.site、ThatsMyAI、HumanOrNot、Insidr、Once、ListAI 等已有 Jev 记录，排除；完整名单以最新精确账本加历史回执文档为准。
- 旧三产品矩阵独立闭环 8/30，不能算新 Jev 比率。Alieradox OldPhoto/Graffiti 两条有站方与云端回执，但人工改价，不计独立成功。

## 候选预检，不等于已确认队列

下列为只读调研结果，仍须现场确认分类、免费入口和精确去重；未提交，不能计成功。

| 入口 | 当前证据 / 下一步 |
| --- | --- |
| https://aitoolsdirectory.com/submit-tool | 免费 AI 目录，外部 Paperform 字段与分类待核对；与已投 `.site` 不是同一站 |
| https://10015.io/product-finder/submit | 免费产品目录，先 URL 后抓取补资料，免费队列 3–4 个月；分类适配待核 |
| https://www.sites-plus.com/submit | 有 Games、Online Games 等分类，免费与提交条款尚未确认 |
| https://apprater.net/add/ | 应用目录含 Fun/Game，费用和 promotion 机制待核，备用 |
| https://www.aitoolnet.com/submit | START/Free 档，页面也有 Payment Platform 字段，实际免费路径待核；曾 403 |
| https://toolscout.ai/submit | 有 Gaming，明确要求 Sign in to continue，需已有授权登录态 |
| https://library.phygital.plus/tool-submission | 创作者/游戏开发工具库，Jev 是游戏而非游戏开发工具，不能因“游戏开发”就认定适配 |
| https://devpages.io/submit-a-tool | 开发工具目录，无 Games；Jev 适配未成立，不优先 |
| https://thereisanaitool.com/submit-tool/ | 免费表单，但分类仅 Chat/Writing/Image/Video/Voice/Coding/Search/Productivity/Music，**无 Jev 合适分类，不能硬选** |
| https://landing.mycloudmedia.co.uk/apps-and-websites-submit-ai-or-saas-tool/new-submission.html | 要姓氏、电话、公司、隐私同意、18 岁声明，不能编资料；费用未核 |

- Startups Lab `https://startupslab.site/submit-product`：主代理现场填 Jev 名称/URL 点 Generate Launch Listing 后立即弹 `Sign In to Submit Product`，未保存投稿、无回执，取消并关页。免费首页不代表无需登录。

## 接手后的执行顺序

1. 阅读 external-link-operator skill、最新 PROJECT_CONTEXT 和新 Jev 报告；fetch，保留别人的改动。不要重做已完成修复。
2. 恢复 Ego 普通窗口控制，查明实际插件加载路径/版本；受支持的界面不能操作时继续明确记录真实阻塞，不假称已重载或已重启。
3. 云端回读去重，按产品真实适配预检候选。先固定十站，再逐站由插件执行；失败仍在分母中，人工填写或人工改价另计。
4. 单站只有插件填表、最终发送、站方明确收到、Neon 精确账本回读都成立才计独立成功。审核/排期不等于公开或索引。
5. 修复真实失败原因，测试、自审、提交推送，更新中文文档；普通故障自主重开恢复，清理已用页签。旧会话不再同时操作浏览器，5 分钟续办应迁入新任务。

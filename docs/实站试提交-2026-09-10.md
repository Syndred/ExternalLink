# 真实站点试提交进度

用户已授权从外链库选几个站点做真实提交。Chrome 已核验加载 3.4.1。

## 当前结果

本轮实际提交 0，成功回执 0。以下是公开页面现场检查，不是插件提交验收通过。

| 站点 | 现场结果 | 后续 |
| --- | --- | --- |
| https://devpages.io/submit-a-tool | 免费人工审核表单；名称、描述、网址、分类、定价、邮箱；当前字段为空，未出现 ExternalLink 填充控件 | 保留页面，等待用户从插件启动填写 |
| https://futuretools.io/submit-a-tool | 公开表单可访问；名称、网址、描述、分类、定价、邮箱；当前字段为空，未出现 ExternalLink 填充控件 | 保留页面，待插件接管 |
| https://aitoolsdirectory.com/submit-tool | 跨域 iframe 表单；明确要求非 AI 原创描述 | 未填未交，不用模型生成文案试交 |
| https://startupbase.io/submissions/start | 当前返回 Page Not Found / Error 404 | 本轮不使用旧入口；未据此将整个站点写为永久失效 |

## 执行边界

公开网页可由浏览器工具操作，但此前扩展工作台 URL 的访问限制仍禁止绕过。本轮没有打开受限界面，没有从浏览器内部提取运行态或凭据。独立 CLI 未配置云端访问令牌，因此本地库快照只用于发现入口，不能证明云端尚未提交。

用户从 DevPages 页面手动选择测试项目并点击插件「填表」后，可继续观察真实填充、核对品牌资料、处理普通安全步骤，并以实际回执判断提交结果；验证码保留给用户。尚未完成跨批次或整夜实机验收。

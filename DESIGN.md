# ExternalLink UI 基础规则

> 适用范围：Chrome Side Panel 与 Settings。最后更新：2026-07-30。

## 颜色

- 主色：`#0284c7`；用于主要按钮、选中标签和焦点关联状态。
- 文字：浅色界面 `#0f172a`，深色界面 `#e2e8f0`。
- 成功：绿色；警告/待人工：琥珀色；失败/危险操作：红色。
- 颜色不能作为唯一状态提示；必须同时显示文字或图标。

## 间距与密度

- 基础间距单位为 4px；常用间距 8 / 12 / 16 / 24px。
- Side Panel 卡片内边距 12px，Settings 卡片内边距 16px。
- Side Panel 保持紧凑，但一行只承载一个主要判断或操作。

## 状态

- `pending`：等待；`running`：当前正在处理。
- `success`：只有明确成功证据或人工确认后显示。账本另用 `publicationStatus` 区分已提交 / 待审核 / 已上线，不把点提交当成已上线。
- `needs_login` / `needs_captcha` / `needs_manual`：待人工，不占批量并发位。
- `paid` / `broken` / `skip` / `deleted`：外链站级排除状态。

## 按钮与可访问性

- 可点击按钮、输入框、选择器最小高度 44px。
- 每个输入均需可见标签；图标按钮需 `title` 或可访问名称。
- 键盘焦点使用 3px 可见轮廓，不移除浏览器默认交互语义。
- 主按钮每个工作区最多一个；危险操作使用红色且操作前确认。

## 工作区

- 执行：当前外链站、当前手动填表 Profile、站点标记和当前页操作。
- 批量：多选本轮 Profile、预估组合、运行进度、带项目名日志。
- 待人工：停放原因、继续处理、人工确认成功。
- 外链库只在 Settings 管理，执行页只显示当前外链站分类。仅「外链库」页左右分栏（列表 + 同步）；网站资料和全局配置仍是顶部菜单。卡片一行两条，显示质量分和已提交记录，不列出「未提交」。

## 安全渲染

- 网页、AI、Profile、URL、域名、注解和日志内容一律通过 `textContent` 渲染。
- 不得将上述动态内容拼入 `innerHTML`；静态页内横幅模板不得包含动态插值。

# ExternalLink 2.8.0 升级报告（2026-08-28）

## 结果

本轮已完成质量评分、已发布链接监控、三候选评论工作室、媒体上传预检和 Google Sheet 变化提醒。代码、本机 Agent、Google Sheet 读回和静态页面视觉均已验收；Chrome 扩展运行缓存只差用户重载扩展后执行一次“预览同步 → 应用同步”。

## 数据对账

- Google Sheet：6 个 Profile、2,905 条外链、29 条提交记录、0 冲突。
- 6 个 Profile 共 36 个 `Local media folder / LOGO / Screenshot 1–4` 单元格已从过期相对路径改为 `/Users/syndred/Desktop/projects/media/{Profile}/...`，官方 API 回读一致。
- 本机媒体库：6 个 Profile、31 个可上传文件。
- Sheet 是人工维护源；`chrome.storage.local` 继续作为扩展运行缓存和本地成功账本。定时检查只提醒，不自动覆盖。

## 新增能力

1. 外链机会质量分：0–100 分、四档 Tag、DR/DA/流量/Spam 标签、质量筛选和排序、最低分闸门。
2. 发布外链监控：检查公开结果页是否仍包含目标网站链接并识别 rel；仅在已存活链接转为异常时提醒。
3. 评论工作室：一次生成 3 条候选，选择、编辑、字数限制和上一版恢复；生成与填入分开。
4. 媒体预检：提交前查看本地 Logo/截图元数据和缩略图，提交后查看每个文件字段真实上传结果。
5. Sheet 变化提醒：30 分钟到每天的定时只读检查，检测到变化后要求人工预览并应用。

## 验证证据

- 全部 `tests/*.test.mjs` 通过。
- `tests/local-agent-unit.test.py`：42 项通过。
- `background.js / content.js / settings.js / sidepanel.js` 语法检查通过。
- `git diff --check` 通过。
- Settings 外链库真实渲染：无横向溢出，工具栏间距 10px，按钮行间距 12px。
- 500px 窄侧栏真实截图：工具栏改为两列，四个按钮完整显示；评论头部换行，生成按钮不裁切。该响应式规则覆盖常见 390–500px 侧栏。
- 本机 Agent 新进程：健康检查、Google 授权、媒体列表和 Sheet 预览均成功。

## 尚需一次人工运行态验收

1. 打开 `chrome://extensions`，重载 ExternalLink 2.8.0。
2. 打开扩展 Settings → 外链库。
3. 点击“预览同步”，确认 6 / 2,905 / 29 / 0 后点击“应用同步”。
4. 打开侧栏，确认质量 Tag、三候选评论和媒体预检可见。

未完成以上四步前，不把 2026-08-28 Sheet 数据标记为已经进入扩展运行缓存。

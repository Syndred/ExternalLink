# JevPlay 插件独立十站验收

## 19:49 续验：候选费用与登录门槛

- fetch 后仍为 `d4cb04e`，无远端新改动；Ego 原生绑定再次为 `-3811`，未获得退出或重启句柄，未操作安全拒绝的内部页。
- AIToolnet `https://www.aitoolnet.com/submit` 本次公开页可打开，含 Gaming 分类及产品 Free 定价选项，但目录费用仅见 PRO $9.9 / BUSINESS $49.9，必填 Payment Platform，最终按钮为 `Submit Pay $9.9`。交接候选中的 START/Free 说法不符合本次现场，不能将产品 Free 定价当成免费投稿；本轮未填表、未付款、未投稿。
- ToolScout `https://toolscout.ai/submit` 明确免费，但要求 `Sign in to continue`；登录页支持 Google / Email，继续前明确接受 Terms of Service。未继续登录或接受条款，未投稿。
- 共用本任务新建页签 `512544013`，已关闭并回读确认清理；不计插件尝试，独立闭环无新增。
- 修复本地审计空数据误报：`audit-state.mjs` 明确 `source=local_seed_backup`、`liveCloudVerified=false`，无成功证据 `ok=false / reason=no_evidence`；Jev 实际 CLI 退出码 2，RainbowPetAI 既有本地账本仍退出 0。独立 fixture 回归覆盖无记录、仅失败记录、成功账本与别名匹配，全部通过；语法、diff 和 Skill 校验通过。原综合 Skill 测试因 worktree 外 `rainbowPetAi/public` 缺失而失败，未声称全量通过；本轮未改扩展运行代码或版本。

## 19:44 新任务接手与四站现场预检

- 新任务 `01a0dd85-e567-7273-a836-7a179de56ddb`，worktree `/Users/syndred/.codex/worktrees/67be/ExternalLink`；开始 fetch 后 HEAD 与 origin/main 均为 `06df6fe`。现有 `externallink` heartbeat 已迁入本任务，配置回读为 ACTIVE、每 5 分钟，没有新增自动化。
- CUA 刷新清单后，Ego 原生绑定仍返回 ScreenCaptureKit `-3811`，没有取得控制句柄，未完成退出重开或插件重载。Chrome 公开标签接口可用；未访问此前被安全拒绝的扩展内部页，也未通过其他接口绕过。实载版本和加载路径仍未证实。
- 以下均为公开页面门槛预检，未调用插件填表，未点击最终投稿，不能计入独立成功或已固定十站；暂未完成最新云端去重。

| 候选 | 本轮直接可见证据 | 下一步 |
| --- | --- | --- |
| Sites Plus | `/submit` 选择 Recreation 后，第二步明确提供 `Regular - free`；第三步含 TinyMCE 描述、默认勾选邮件订阅、必选 `Submission Rules Agreement` | 分类仍需细化；最终投稿前处理协议确认，不能自动沿用默认订阅 |
| AI Tools Directory `.com` | `/submit-tool` 明确写 AI 生成或复制文案会被 discarded | 当前源码已有该域名和 Paperform 的人工闸门；从插件独立投稿候选中排除，不重复开发或尝试 |
| 10015 | 输入 Jev 官网并点 `Submit URL` 后出现 `Sign In / Register`、`You need to sign in or register to use this feature.`；页面注明免费队列 3–4 个月、免费链接 nofollow | 当前会话需登录；没有抓取后表单或投稿回执，不以首页免费说明认定可直接投稿 |
| AppRater | `/add/` 重定向首页，`#submit` 表单含 App name、Platform、App or store URL、What makes it useful、姓名、邮箱，可选 tags/image/X；页面已有 Web 游戏案例；最终按钮旁写提交即同意 Terms | 更新旧入口认知；条款待动作时确认，付费 leaderboard 不能误当投稿必付费 |

- 四站共用本任务新建标签 `512544009`，已关闭并回读标签清单确认移除；原有 X、扩展管理/设置及其他 Jev 游戏任务页未动。
- 本轮新增插件独立闭环 **0**，目标仍为固定十站至少 **6/10**；没有以预检、文档或静态测试冒充真实完成。
- 只读审计确认接手时原仓库和 worktree 均为 `06df6fe`，两处 manifest 哈希一致、版本 `3.7.106`；这仅证明磁盘版本一致，不证明浏览器加载路径或已重载。Worker 的 `/v1/revisions` 与 `/v1/state/submissionRecords` 可用于实时读回，但需设备访问密钥，未授权请求返回 401；原仓库 `.env` 无 `EXTERNALLINK_CLOUD_*`，不能为取密钥绕过内部页限制。
- 最近可用导出仍为 `2026-09-26T11:16:49.979Z` 的 `(3).json`、26 条 Jev 精确记录，属于历史快照。旧 `audit-state.mjs --profile JevPlay` 只查种子和灾备，输出 `0/0 ok` 不能代表 Neon 为空或已去重；不据此启动新投稿。Alieradox 与 AI Tools Ratings 的既有回执继续优先防重投。

## 2026-09-26 / Jev 单站范围与 3.7.106 精确选项优先

- 用户最新范围覆盖旧三产品计划：只投稿 JevPlay，OldPhotoLive 与 GraffitiName 暂停外部提交。既有三产品矩阵 8/30 仅保留为历史，不能混入新 Jev 样本。新十站先排除已有回执，固定队列后记录每一真实尝试、失败和人工辅助；独立填表、提交、站方回执及 Neon 精确回读至少 6/10 才达到超过 50%。本轮尚无新增真实提交，不能报告达标。
- 普通卡死恢复流程已写入现有 `externallink` 续办任务：保存目的站、Profile、是否已点提交和证据，关闭并重新打开 Ego；恢复后先核对回执和精确去重，再继续未提交步骤。已有其他自动化工作空间则使用独立空间；用完关闭本任务页签与分组。安全策略拒绝、登录、CAPTCHA 和条款门槛不靠重启绕过。不能关闭其他任务正在使用的 Chrome 窗口。
- 本轮通过原生接口启动 Ego，应用清单确认运行，但绑定窗口连续失败于 ScreenCaptureKit `-3811`，未取得可调用的窗口句柄，因此尚不能执行退出快捷键或操作插件；Chrome 原生窗口也同样失败。不得将启动应用写成已完成重启，更不得将源码测试计为实站成功。此前扩展管理页/设置页存在明确安全拒绝，不使用备用界面规避。
- 复查自定义下拉发现 `Free` 会先命中排列在前的 `Freemium` 包含匹配。3.7.106 复用项目既有 `findBestSelectOption` 的精确标签优先排序，覆盖首次展开和输入搜索后匹配；动态回归验证 Free 选项收到选择事件，Freemium 不被误选。54/54 测试、语法和 diff 检查通过，源码尚未实载复验。前版主流实现参考仍为 Bitwarden 字段归属、Automa 控件流程、Playwright 可访问名称与动作后验证、Radix pointerup 提交选择。

## 队列与计分

候选预检尚不计提交；固定队列前需要核对云端精确去重。第一次插件填表启动即计真实尝试，填写错误、提交失败、人工接管、缺少站方回执或云端回读都留在分母中，不替换删除。只有重复或产品不适用等在尝试前已确认的候选才能排除。超过 50% 要求至少 6/10，全部十站须有处理结果。

## 待恢复后第一步

使用受支持的 Ego 原生界面恢复普通窗口控制；禁止重投 Alieradox 和 AI Tools Ratings 的 Jev。确认插件实载版本后，在新候选真实页面验证字段、Free 定价、提交和云端闭环；不要把手动填写替代插件独立测试。

## 本轮公开页预检

- 2026-09-26 19:35，Startups Lab `https://startupslab.site/submit-product`：最近一次云端回读导出无该站精确账本。公开页手动填 JevPlay 名称和官网后点击 `Generate Launch Listing`，立即弹 `Sign In to Submit Product`，要求登录验证 maker ownership 并保存草稿。未提交、未创建成功记录；取消并关闭预检页。此为门槛预检，不计插件独立尝试。恢复后若有已授权登录态可再次评估。
- 源码 3.7.106 已随 `bb009fe` 推送 `origin/main`，工作区干净；插件实载版本与现场回归仍未验证。

# 外链提交报告 2026-08-26（晚间中断）

## 2026-08-27 晨续跑

用户已在 Cursor 内置浏览器完成 Google 登录。本轮用该浏览器继续 Video，一次一个工作页，验证码页不关。

### 已入账（Video 现 17/30）

- `bai.tools`：免费档提交成功，确认页 `https://bai.tools/finish`（Submission Successful，审核最多约 30 天）。
- `toolscout.ai`：Google 账号选择器登录后免费提交，确认 `Submission received` / `In review` / Placement: Free。
- `once.tools`：无需登录，确认 `Thank you for submitting VideoToArticleAI.`
- `insidr.ai`：确认 `Your submission was successful.`
- `navtools.ai`：用户过 Turnstile 后确认已提交，`confirmedBy: manual`。
- `humanornot.co`：确认 `Thank you! Your submission has been received!`
- `10015.io`：Google 登录后免费提交，确认 `Thanks for submitting your product! "VideoToArticleAI" has been submitted successfully.` 未买 $9.99 加急。
- `library.phygital.plus`：确认 `Thank you for your submission!`
- `nextpedia.io`：确认 `Your submission was successful.`（免费、无可见验证码）

### 仍停放 / 跳过

- aitoolnet.com：Cloudflare，未再开页。
- faind.ai / trackbes.com / launched.io / allstartups.info / futureagitools.com/submit-a-site：失效或 404。
- aitoolsdirectory.com：Paperform iframe，Cursor 浏览器填不了。
- toolpilot.ai：免费档强制对方页脚外链，跳过。
- ai-hunter.io：表单报 `An error occurred while processing the form`，未入账。
- aidirectory.org：reCAPTCHA；电话现已有 `+8615766379321`，验证码档有空再试。
- saashub.com：用户已过 Cloudflare，但 `/submit` 是「提交到其他 108 个目录」的营销页，不是本站收录表，跳过、不算成功。
- aishenqi.net：表单已填（名称/网址/简介/分类 AI写作神器/标签/昵称 Syndred），提交被安全检查拦住；页签留给用户过验证后点「提交审核」。
- thataicollection.com：$19 付费。
- nextgentools.me：Airtable iframe，Cursor 填不了。
- simplelister.com：登录且写明未来 9 个月只收付费。
- aimojo.io：最低 $79。
- chatgate.ai / critiqs.ai / payoncealternatives.com / blpdirectory.info / raidai：404、过期域名或打不开。
- saaspo.com：Cloudflare 直接拦截，不是可过的验证码页。

RainbowPet / OldPhoto 尚未开始。

## 结论

今晚云端 Agent 在给 **VideoToArticleAI** 批量开免费目录页，大约开了 **81 个 Chrome 页签** 后把本机卡死。Chrome 约 **22:54** 重启。当前实机只剩 2 个页签（新标签页 + Wappalyzer），上一轮提交页全部丢失。

**Google Sheet `Submission Records` 今晚没有新增。** VideoToArticleAI 仍只有 2026-08-24 已确认的 7 条。口头声称的 Viesearch 成功未回写，页签也已丢失，**不能入账**。

## 23:24 续跑（目标改为每项目 30 条）

用户要求 Video / RainbowPet / OldPhoto **各交 30 条**。本轮只开 1 个页签，做完即关。

### 已入账

- VideoToArticleAI × `viesearch.com`：实机再次提交，接口返回 `This website has already been submitted and is currently under review.` 已写入 `Submission Records`。Video 确认成功现为 **8 / 30**。

### 本轮停放（不算成功）

| 站 | 原因 |
| --- | --- |
| NavTools | 免费 Basic $0 已填好，Cloudflare Turnstile 卡住 |
| Navs | 必须登录，且要截图 |
| Aimyflow | 免费表单，React 校验收不到填值，未交出去 |
| BAI.tools | 提交后跳转 Google 登录 |
| AnyFP | 免费多步表单，Logo/截图必传 |
| Postmake | 付费，最低 $79 |
| DokeyAI | 弹出 Google 登录；免费档还要在页脚加对方外链 |

RainbowPet / OldPhoto 本轮尚未开始。

### 当前计数

| Profile | Sheet 确认成功 | 目标 |
| --- | ---: | ---: |
| VideoToArticleAI | 8 | 30 |
| RainbowPetAI | 3（Sheet；交接 JSON 另有历史 10 条未全部回写） | 30 |
| OldPhotoLive | 5 | 30 |

## 上一轮实际停在哪

目标：先交 Video，再 RainbowPet / OldPhoto；至少 10 条；付费跳过；验证码页先留着。

云端会话 [`外链提交自动化`](bc-e4563053-06ec-4043-b2e3-ce06e76b19fb) 最后状态（约 23:06）：

- 已连上私有表格，外链库仍是 2,902 条。
- 插件当时仍停在 RainbowPetAI，准备切到 VideoToArticleAI。
- 正在补 NavTools、Aimyflow、Navs、BAI.tools 等已打开表单，并从哥伦布榜单继续开新入口。
- 自称 Viesearch 免费等待名单已成功，并打算把「已确认 2 条」回填表格后再做到 10 条。
- 这些回填在 Sheet 里没有发生。

## 表格对账（本机 OAuth 实读）

| 项 | 数量 |
| --- | --- |
| Link Submit | 2,902 |
| Submission Records | 19（全部为旧迁移种子） |
| VideoToArticleAI 成功 | 7（Launching Next / StartupBase / TheJOAI / SideProjectors / PitchWall / StartupStash / AITools.inc） |
| 今晚新增成功 | **0** |

Viesearch、NavTools、Aimyflow、Navs、BAI.tools 均未写入 `Submission Records`，Link Submit 也没有对应成功标记。

## 卡死原因

会话文件 `Tabs_13432229656061230`（22:55，约 1.4MB）里有大量同时打开的 submit 页，例如 viesearch、navtools.ai、aimyflow.com、navs.site、bai.tools，以及 aitoolnet / fazier / hunt0 / listedai / showmebest / theresanaiforthat 等。验证码页「先别关」被执行成「全部留着」，并发页签把 Chrome 拖死。

## 操作约束（已更新）

- 同一时间只处理 **1 个**提交页签。
- 做完、跳过、确认付费或失败后 **立刻关闭**该页。
- 验证码最多保留 **1–2** 个待人工页，禁止堆积。
- 不要点 Chrome「恢复上次会话」，会把约 80 个页签重新打开。
- 没有可见成功证据，不写账本、不改 Sheet。

## 下一步

1. 从 VideoToArticleAI 未成功的免费目录继续，目标仍是至少 10 条确认成功。
2. 若要认 Viesearch，必须重新打开该站并看到等待名单/回执后再写 `Submission Records`。
3. RainbowPetAI / OldPhotoLive 等 Video 达到目标后再做。

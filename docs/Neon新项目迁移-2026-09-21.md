# ExternalLink Neon 新项目迁移（2026-09-21）

## 结果

ExternalLink 已从网络传输额度耗尽的旧 Neon 项目迁移到新项目：

- 项目：`ExternalLink`
- Project ID：`dark-night-20420985`
- Branch：`production`
- Region：AWS US East 2 (Ohio)
- Worker 地址保持不变：`https://externallink-cloud.syndred.workers.dev`
- 工作区保持不变：`default`
- APP 设备访问密钥保持不变；数据库连接串只保存在 Cloudflare Worker Secret，未写入仓库或文档。

Cloudflare Worker 的 `DATABASE_URL` 已轮换到新项目，生产 `/v1/health` 已从旧库 402 恢复为 HTTP 200。

## 数据来源与保护

用户确认这台 Ego Profile 是最新数据源。迁移前已备份扩展 LevelDB，并导出不含 Worker/数据库密钥的状态迁移包：

- `/Users/syndred/Desktop/ExternalLink-Neon迁移备份-2026-09-21/`
- 目录权限：仅当前用户可读写。
- 首次迁移遇到新库已被运行中的扩展写入 `linkMonitorResults`，Worker 冲突保护返回 409，没有覆盖数据。
- 随后重新抓取切换后的最新本机快照，确认无 pending/conflict 后，以这台机器为唯一真相源执行明确替换。

## 生产回读

- 状态文档：20 类，20 个修订。
- Profile：7 个。
- 外链库：2,975 条。
- 提交记录：30 条。
- 时间线：77 组、222 条事件。
- 本机迁移快照与云端 20 类文档深度比较一致；PostgreSQL JSONB 键顺序差异不计为内容差异。

## 媒体恢复

- 当前本机媒体源：43 个文件，6 个 Logo、37 张截图。
- 已重新上传/登记到原 Cloudflare R2 桶，R2 桶本身未更换。
- 43/43 逐个从生产 Worker 下载，SHA-256 与字节数全部一致，总计 33,103,634 字节。
- 修复迁移脚本对中文文件名 Header 的兼容问题。
- 将旧媒体目录 `FreeLanguage` 映射到稳定 Profile `site-mu4wlxu0`（显示名 `AISpeakLearn`），资产 ID 保持不变。
- 修复一个旧悬空引用 `RainbowPetAI-screenshot-3.png`，统一指向现存 `RainbowPetAI-screenshot-3.jpg`。
- 最终云端媒体记录 43 条，Profile/外链库/活动批次中的媒体引用均有对应记录，缺失引用为 0。

## 验证

- Worker `/v1/health`：HTTP 200。
- Worker `/v1/snapshot`：20 类文档及上述业务数量回读一致。
- Worker `/v1/media`：43 条；43/43 下载哈希和大小一致。
- `node --test tests/*.test.mjs`：44/44 通过。
- `node --check tools/migrate-media-to-r2.mjs`：通过。
- `git diff --check`：通过。

## 另一台电脑

使用 `/Users/syndred/Desktop/ExternalLink-台式机云端配置.txt` 中现有 Worker 地址、设备密钥和 `default` 工作区即可。另一台电脑必须先拉取 `main`、重载 `3.7.1`，然后只执行“连接云端”和“从云端回读”；不要点击“首次迁移到云端”，也不要先点“立即保存”。


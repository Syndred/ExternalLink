# 外链提交验收与误填保护

日期：2026-09-24

## 本轮目标与实际结果

原目标是按已有真实成功记录的站点优先，为 JevPlay、OldPhotoLive AI、Graffiti Name AI 各提交至少 30 个站点，并验收插件是否自动填写、提交和记账。

首次试跑了 3 个 Graffiti Name AI 组合。三个候选站均未找到可由确定性规则处理的目录表单，插件转交模型后收到 `Insufficient Balance`，随后按无人值守组合上限暂停。当时没有站点实际提交成功，也没有验证码。JevPlay 和 OldPhotoLive AI 尚未进入试跑；三产品各 30 站目标仍待完成。

Future Tools 首页是 AI 新闻订阅页，不是产品提交表单。插件误将配置邮箱填进了 newsletter 邮箱框；已清空该字段，未点 `Subscribe to Newsletter` 或其他订阅按钮。备份回读未见本轮三个域名对应的提交记录或时间线事件，因此没有虚报成功。

## 源码修复

- email-only 表单不再作为产品提交表单的识别证据。
- 识别 newsletter / subscribe 等营销语义的单邮箱表单，并从表单范围、填表候选中排除。
- 侧栏自动填表和批次后台自动填表均要求页面被识别为 directory、submission、profile 或 forum；未知页面即使有输入框也不自动填。
- 扩展 Manifest 从 3.7.4 经账本保护和实站误填修复升至 3.7.12；Ego 管理页已回读 3.7.12。
- 增加 newsletter、目录表单、联系表单的行为回归用例。
- 补上模型服务商额度错误的明确处理：Worker 将 `Insufficient Balance` / HTTP 402 映射为 `AI_PROVIDER_BALANCE_EXHAUSTED` 和 503；插件保留错误码并显示中文原因，不再把它混成普通云端错误。Worker 已部署，版本 `d6e24727-7cf8-47f0-981b-1edafdafdd05`。
- 队列不再把 `confirmedBy: migration` 的旧表格占位记录视为真实成功；它们没有本次提交回执，目标 Profile 可以继续排队。
- 账本缺少目标 URL、Profile ID 或精确目标键时直接报错；本地记录写入后核对 `destinationKey::profileId`。自动提交和人工确认都只在真实账本记录返回后标记成功，人工确认写入失败时保留页签与确认凭据，以便重试。
- 10015 第二步实站暴露三处填表缺陷：产品网页 URL 被填进图片 URL、整串标签被填进 React Select 搜索框、未选中的必填定价/分类/标签却显示「可提交」。3.7.6 只允许真实公开图片链接进入图片 URL；React Select 逐项匹配实际选项，不再任取第一项或留下未选中的搜索词；根据选中 chip 和字段星号计算必填状态。错误表单已重载清空，尚未最终提交。

## 新 Key 接入

- 用户于 2026-09-24 新建并充值 DeepSeek `outlink` Key。通过剪贴板读取后，只传给 `wrangler secret put DEEPSEEK_API_KEY` 的标准输入；未写入仓库、本机 `.env` 或进度文档。旧 `.env` 中泄露的 Key 已删除。
- Worker Secret 更新成功；Cloudflare 当前部署版本 `4d293a19-d070-4f0a-abbd-0119736000a7`。新 Key 直接调用 DeepSeek `deepseek-v4-pro` 返回 HTTP 200。此项只证明密钥和账户可用，插件到 Worker 的实际调用仍需实机验收。

## 验证

- `node tests/content-submission-detection.test.mjs`
- `node tests/content-comment-detection.test.mjs`
- `node tests/extension-content.test.mjs`
- `node tests/sidepanel-behavior.test.mjs`
- 仓库 README 中列出的 10 个 Node 测试脚本全部通过；另有上面两项检测测试通过，共 12/12。
- `node --check` 检查 `extension/content.js`、`extension/sidepanel.js`、`extension/background.js` 均通过。
- `git diff --check` 通过。
- `tests/ai-provider-errors.test.mjs` 验证额度耗尽与 429 的 Worker 响应；全量 Node 测试 47/47 通过。
- 3.7.6 的全量 Node 回归 48/48 通过；`node --check`（扩展及 Worker）和 `git diff --check` 通过。Ego 实机验收结果待补录。
- 3.7.11 的全量 Node 回归 49/49 通过，`node --check extension/content.js` 与 `git diff --check` 通过。Insidr、Human or Not 的自动预填、人工点击提交后的自动回执登记及云端回读已现场验证；插件批次自动点击提交仍未通过验收。
- 3.7.12 的全量 Node 回归 50/50 通过；Ego 实站回读 NextPedia 时间线由 20 条重复/异常日期动态压为 5 条实际可区分动态，历史 Excel 日期序号 `46261` 显示为 2026/08/27，三款本轮提交分别显示 2026/09/24 18:28–18:30。NextPedia 重载后短描述使用短文案，正文按 2–3 句填入，未再次提交。

## 待办和边界

- 已通过侧栏导出旧批次报告至 `~/Downloads/externallink-run-mudxzt97-本轮报告.md`，回读为回执 0、待人工 4、失败 0、跳过 0。该 8815 组合的旧批次已停止，侧栏确认状态为「已停止」，4 项待人工仍保留，其中 3 项属于 Graffiti Name AI，另 1 项是既有 RainbowPetAI 待办。
- 试跑期间云同步显示 1 项冲突、2 个修订版本不一致；此前已导出本机备份到 `~/Downloads/externallink-backup-2026-09-23.json`。停止旧批次产生了本机状态变更；尚未强制云端回读或重试上传，冲突仍需核对。
- Ego 已回读扩展 3.7.12 启用。10015 首步由插件填入 JevPlay URL，点击后站点要求登录，已按登录门槛停放，第二步控件修复尚未能实站复测。
- Once Tools 由插件在清空表单后重新填入 JevPlay 的名称、联系邮箱、官网、标语、描述和价格；提交页显示明确回执 `Thank you for submitting JevPlay`。插件没有自动登记：原因是人工提交监听把 Profile 推广深链 `/games` 与表单填写的官网根路径按整串 URL 比较，未能归属同一产品。已通过侧栏「登记外链动态」依据现场回执补记 `once.tools::JevPlay`，侧栏随后回读「云端动态已核对，当前记录是最新的」；这是人工补记，不算自动记账通过。
- 3.7.7 将提交监听改为严格同主机名匹配（允许 `www` 和深链差异，拒绝相似恶意主机）；预检 `humanornot.co` 时又发现其只有邮箱和文本 URL，原监听会因没有 textarea 或 `input[type=url]` 而漏掉。3.7.8 在文本 URL 字段明确标为 URL/website/link 时也监听，排除隐藏追踪字段，回归通过。需重载并用下一个 Profile 实站验证自动记账。旧 `activeBatchRun` 仍有跨设备冲突，已停止旧批次；本次 `submissionRecords`/`submissionTimeline` 则已完成云端回读。
- Once Tools 首页明确定位为「只需一次付款的软件」，三款当前产品不适合继续投该站；JevPlay 已有的真实提交回执仅记为待审核，不能当作已收录外链。
- 对历史站点先只做预填检查：Viesearch 的旧学习映射把官网误填进标题/描述，分类搜索框留着整串标签；Insidr 的 CMS 内部字段名为 `email`，可见标签却是 `Tag / Tool category`，旧逻辑误填联系邮箱；NextPedia 的订阅弹窗抢占主表单并收到姓名和邮箱，未点击订阅或目录提交。3.7.9 让明确可见的标题、描述、分类标签压过旧映射，未选中的分类搜索框留空，且不让无收录字段的弹窗替换真正提交表单；待重载实站复测。
- Human or Not 的免费表单对 JevPlay、Graffiti Name AI、OldPhotoLive AI 均收到 `Thank you! Your submission has been received!` 回执。JevPlay 在旧版未自动记账，已人工补记；后两款由 3.7.11 自动登记并完成云端回读。该站要求 AI 功能，三款均按真实功能描述提交；仅代表待审核。
- Insidr 三款均收到 `Your submission was successful.`：JevPlay 在旧版提交后人工补记；OldPhotoLive AI、Graffiti Name AI 经 3.7.11 正确预填、人工点击提交后自动记账，侧栏云端回读均显示最新。旧版漏记因通用回执正则没有识别该文案且大小写敏感，3.7.11 已修正。
- 切换 Profile 时，Insidr 表单曾保留 JevPlay 的描述，旧学习映射还可能回放原始文本。3.7.11 让 textarea 使用当前 Profile 的描述，只回用当前 Profile 字段值；发现表单主网址属于另一产品时，先清空该表单旧字段，再重新填写。现场复测 OldPhotoLive AI、Graffiti Name AI 三字段均正确。
- NextPedia 三款也均由插件填写名称、网址，人工按该站要求把长短描述调整为精简内容后点击 `Send`；三次均见 `Your submission was successful.`，插件自动登记，云端刷新显示最新。3.7.12 已修短描述与“2–3 句”字段路由，重载后在实站预填中看到短文案和三句正文。
- 第一轮真实提交为 10 个产品×站点组合：Insidr 3、Human or Not 3、NextPedia 3、Once Tools 1；其中三个站点完成三产品覆盖。自动记账通过 7 个新组合，3 个旧版组合靠人工补记。侧栏旧时间线重复动态及 Excel 日期序号已在 3.7.12 的展示层去重/正确解析；迁移时也不再新增与现有提交回执等价的事件。云端既有重复事件仍保留，不能把原始动态条数当成提交数。
- Startup Stash 的 Graffiti Name AI 已走完 11 题 Typeform，广告选 `No`，结束页显示 `Thank you for applying to get listed on StartupStash! We will get back to you as early as possible :)`。跨域 iframe 使插件没有自动捕获这次提交；通过侧栏补记精确 Profile 与回执后，动态显示 `云端动态已核对，当前记录是最新的`。此项计为已提交待审核、不计为自动记账通过。当时累计 11 个产品×站点组合，仍只有 3 个三产品完整站点。iframe 监听和 `confirmedBy` 来源语义正在修复。
- Viesearch 的 Graffiti Name AI 已选择免费等待队列，页面提示 `Please check your email to confirm your free submission`，邮件确认未完成，不计成功。StartupBase 的 Graffiti Name AI 完成产品资料和媒体，但页上仍标 `Draft`，免费发布队列涉及站内徽章，暂不计成功。FiveTaco 进入创建账户及条款门槛，Sites Plus 需要同意 Submission Rules，Launching Next 有 `What is 2+3?` 验证题，AILib 有 reCAPTCHA 与个人信息处理声明，均留待人工处理；AI Wiki Tools 的嵌入式表单连接失败。这些站点没有被记作成功。
- `2f8ab90` 将队列成功跳过条件收紧为 `status: success`、`confirmedBy: agent/manual`、非空且非迁移种子的证据同时成立；旧表格种子、空证据和不明来源仍可排队，相关 7 组测试通过并已推送。新版代码尚待 Ego 扩展重载，不能把源码通过当成实机行为。
- 历史成功站点的 projects 白名单不含这三个 Profile；旧表格迁移记录没有真实回执，不能算本轮的优先成功目标。
- 模型密钥已恢复可用；验证码、OTP、付费和法律确认仍交由用户处理。先按有真实成功回执的目录排序，再逐站确认 Profile 白名单、免费状态和页面回执；不能将历史迁移种子凑入成功数。

## 19:06 实站续跑与同步异常

- Ego 已实际重载并回读扩展 3.7.13、3.7.14。`9a28362` 给跨域 Typeform iframe 增加帧级提交与回执监听；`2aaa077` 在侧栏「填表」时刷新已打开的帧并重建监听。两次定向代码测试通过。3.7.14 的独立 Typeform 来源站映射尚待实站确认。
- Startup Stash 的 OldPhotoLive AI 按 11 题提交，广告选 `No`，看到同一 `Thank you for applying...` 结束页。扩展 3.7.13 重载前已存在的 iframe 没有自动记账；按可见回执经侧栏补记，备注明确为操作代理补记。侧栏能看到 `OldPhotoLive AI 已提交`，但云端回读仍显示「本地外链动态正在等待云端保存」，不能称云端已确认。
- TipSeason 免费表单对 OldPhotoLive AI、Graffiti Name AI 分别提交，均跳转 `/ai-tools/submit-free/success`，页面显示 `Your AI Tool Is In The Standard Queue`、`Submission Received`。两次扩展均提示自动保存提交回执和动态，证明本地自动记账链路触发；两条云端回读尚未确认。该站免费标准队列显示等待 180 天以上，收录概率低，不能算已发布外链。
- TipSeason 还暴露自动填表字段错配：OldPhotoLive 邮箱留空，短描述仅 3 词，Primary Use Case 截断，CDN 地址误入可选社媒 URL；Graffiti 的人工修正又被异步填表覆盖，短描述变为 5 词导致第一次提交被站点校验拒绝。两次都在最终提交前逐项修正并核对业务邮箱、官网、分类、定价和广告 `No`，最终获得真实成功页。修复需防止错误字段路由与异步覆盖。
- 设置页在 19:04 显示云端已连接 `default`，但 1 类数据冲突、另 2 类修订不同。「重试上传本机修改」返回「本机有冲突数据，上传未执行」。没有强制回读或丢弃本地修改；需先按键定位冲突并安全合并，再对 Startup Stash 和 TipSeason 的新记录做云端回读。
- 已有真实提交回执的组合暂为 14：Insidr 3、Human or Not 3、NextPedia 3、Once Tools 1、Startup Stash 2、TipSeason 2；仍仅 3 个站点完成三产品覆盖。云端已确认的是此前 11 个组合；新增 3 个组合（Startup Stash OldPhoto、TipSeason 两款）只确认站点回执和本地记录。AI Tools Inc Graffiti、DevPages OldPhoto、AI Infinity OldPhoto 表单已备至提交前，未计成功。
- MyCloudMedia 表单要求电话且提交即确认接受隐私条款，留待用户；SiteLike 输入 OldPhoto 域名后出现 Cloudflare 安全验证，留待用户。DevPages 主要面向开发工具，三产品适配需谨慎；AI Marketing Directory 面向营销工具。候选不因首页免费或历史他人 Profile 回执而直接计入成功。

## 19:13 新回执与跨域表单复测

- AI Infinity 的 Google Form 已提交 OldPhotoLive AI，回执明确写 `Thank you for submitting a new AI tool. We appreciate your contribution to our community and will review your submission shortly.`。提交前复核名称、Image Editing 分类、描述与官网；侧栏当时仍停在 Graffiti Profile，并把 `docs.google.com` 识别为已删除站点，因此未自动记成 `AI Infinity × OldPhotoLive`。需补精确站点账本，并解决外部表单来源与 Profile 归属。
- Startup Stash 的 JevPlay 已走完 11 题 Typeform，广告选 `No`；个人 Gmail 被站点拒绝为非工作邮箱，改用同一负责人已登记的业务邮箱后通过。结束页显示 `Thank you for applying to get listed on StartupStash! We will get back to you as early as possible :)`。3.7.14 的「填表」把空白跨域问卷误报为「全部 个字段已填写」，问卷由人工逐项填完。19:14 本地 JSON 备份回读发现插件其实在 19:12:52 自动写入 `startupstash.com/add-listing::JevPlay` 且保存了真实感谢回执；侧栏按 `startupstash.com` 取数未显示它，误导操作代理于 19:13 又补记 `startupstash.com::JevPlay`。这是一条真实投递、两条不同目标键记录；需安全归一并防重，不能计两次成功。两条云端状态尚待核对。
- 截至此刻有 16 个产品×站点真实回执、7 个不同站点；Insidr、Human or Not、NextPedia、Startup Stash 四站三产品齐全。TipSeason 两款为免费标准审核队列，AI Infinity 和 Startup Stash 为待审核，不能宣称已发布外链。最近记录的云端同步冲突仍未解除，站点回执、本地账本与 Neon 回读分别验收。

## 19:18 Phygital 双产品回执与跨标签错填

- Phygital+ AI Library 的 OldPhotoLive AI 与 Graffiti Name AI 分别在独立标签提交；两次页面都明确显示 `Thank you for your submission!`。OldPhoto 表单 URL 为 `https://oldphotoliveai.com`、业务联系邮箱为 `support@oldphotoliveai.com`；Graffiti 表单 URL 为 `https://graffitinameai.com`、业务联系邮箱为 `support@graffitinameai.com`。两次仅证明收到审核申请，不能当作已上线外链。
- Graffiti 标签切换正确 Profile 后点 3.7.14 的「填表」，侧栏提示「当前表单 全部 个字段已填写，可提交」，实际当前标签的 URL、描述、联系字段全部为空；旧 OldPhoto 成功标签随后被异步写入 Graffiti URL 和描述，联系字段变空，成功提示消失。相同 URL 多标签时疑似填表消息错误路由或广播，必须按 tabId 隔离并防异步覆盖；修复前暂停对同 URL 的插件自动填表验收。
- 两款均已通过「登记外链动态」补入 `已提交` 与页面原文回执，本地时间线可见，但站点卡片仍为 `待提交`；精确站点/Profile 提交记录和 Neon 端均待核对，不能宣称已云端保存。
- 截至此刻共 18 个产品×站点真实回执、8 个不同站点；Insidr、Human or Not、NextPedia、Startup Stash 四站三产品齐全。Phygital 只投视觉设计适配的两款，不把 JevPlay 硬投到其图像/设计/艺术目录。

## 19:33 修复版与 JevPlay 第五站

- Ego 已重载 3.7.15、3.7.16；切换标签后旧页「已填」提示已清空。TipSeason JevPlay 在 3.7.16 点击「填表」，插件写入名称、长描述、Primary Use Case 和网址，但邮箱留空，短描述只有 4 词并被站点校验拒绝，类别未选，报价与广告选项未定；最终由操作代理逐项校正为 JevPlay 官网、23 词短描述、Other 类别、Free、广告 No 和 Profile 联系邮箱，再提交。免费成功页 URL `/ai-tools/submit-free/success`，标题 `Submission Received`，页面明确 `Your AI Tool Is In The Standard Queue`。这是收到审核申请，站方同时提示等待 180 天以上，不能算已上线。
- 插件在站点回执后显示「JevPlay 已提交，回执和外链动态已保存」；19:32 导出的本地 JSON 含 `tipseason.com/ai-tools/submit-free::JevPlay`，`status=success`、`confirmedBy=agent` 和回执文本。Phygital 两款人工记录也在同一备份的精确路径/Profile 键下。Startup Stash JevPlay 自动路径键和人工域名键仍为同一真实提交的两条记录，不重复计数。
- 19:32 设置页「轻量核对版本」显示本机已保存数据与云端一致，待上传 0 类、冲突 0 类；上次上传 19:30:55，晚于 JevPlay 的本地 `submittedAt` 19:30:53。手动「回读云端更新」显示云端没有新更新。这证明当前文档修订号对齐，但仍需用界面精确 Profile 回显验收每个新条目。先前 1 类冲突在扩展重载/自动同步后已不显示，未执行强制覆盖或丢弃本地修改。
- 本轮累计 19 个产品×站点真实回执、8 个站点；Insidr、Human or Not、NextPedia、Startup Stash、TipSeason 五站完成三产品。新发现的显示缺陷：TipSeason 与 Phygital 成功记录以提交路径为键，侧栏按主域名取数仍显示 `待提交`/旧动态；已扩展已知单站主域名别名并增加记账事件后队列刷新，待重载实站验证。

## 优先站点预检

- `10015.io/product-finder/submit`：真实免费提交入口；第一步只填产品 URL，随后拉取站点资料供复核。免费收录约需 3–4 个月，发布后为 nofollow 链接；付费快速审核不选。
- `once.tools/submit`：当前能看到姓名、邮箱、工具名、URL、标语、描述、价格等普通提交字段，适合作为重载后第一轮插件填表验收。
- `insidr.ai/submit-tools/`：当前能看到 Message、Link、Tag 与提交按钮，提交后仍需查回执。
- `bai.tools/submit-ai-tools`：免费档明确写「Submit without backlink for free」，因此不把免费提交算作取得外链；付费档不执行。
- 已有可信回执的旧提交路由多被 `projects` 白名单限定为 VideoToArticleAI 等旧 Profile，三目标 Profile 不在白名单。重载后需先解决目标路由分配，再执行 3 产品队列；不能因根路径有开放条目而把不同提交路由当成同一条。
- 第一优先、旧库回执和 30 个补充候选的完整顺序见 `docs/三产品外链候选清单-2026-09-24.md`；它们是待预检目标，不是提交结果。

# 外链提交验收与误填保护｜2026-09-25

## 当日续办实站结果（本文件下方“未点击/未提交”为更早快照）

- 用户明确授权处理 The Next AI、Launching Next、Free AI Tools Directory、TAIFY 的验证及免费申请。本轮在 Ego 完成 The Next AI、ListAi、Launching Next、Free AI Tools Directory、BestOfAI 五个来源站的三产品免费申请或既有待审核核验；每个站点都分别核对三个产品，不能把表单填写或付费推广页当作最终回执。
- **Viesearch**：JevPlay 邮件确认后进入 `https://viesearch.com/28svb/plan`，OldPhotoLive AI 邮件确认后进入 `https://viesearch.com/28sve/plan`。两页均展示 BASIC `Free $0 forever`，Express/Featured 为可选付费档。两款各选 `Join the Waiting List` → `Wait in Line`，站方显示 `Plan Selected! ... Your submission is now in the review queue.`；分别按来源页与 Profile 登记 `待审核`，侧栏显示 3 个 Profile、4 条动态并提示云端最新。**Viesearch 不应标 `付费`**。Graffiti Name AI 的确认信不在当前 Gmail，会话使用的业务邮箱是 `support@graffitinameai.com`；未能确认，未计该产品成功。
- **The Next AI**：JevPlay、OldPhotoLive AI、Graffiti Name AI 各选免费 Basic Listing，核对各自名称、官网、类别、文案、Logo 和邮箱，完成对应 Quick Check。三页均出现 `Tool Submitted!`，承诺人工审核；插件按三款分别登记 `待审核`，来源页 `https://www.thenextai.com/submit-ai-tool/` 显示 **3 个 Profile、3 条动态**，刷新后提示云端最新。尚未见公开详情页。
- **ListAi**：`https://listai.cc/submit` 明示标准档永久免费且不需账号。Graffiti Name AI 与 JevPlay 各收到 `Submission Received!`；OldPhotoLive AI 复核时站方显示 `This tool has already been submitted and is pending review`，故未创建重复申请，原始提交日期未知。三款分别据准确回执登记 `待审核`；来源页显示 **3 个 Profile、3 条动态**并完成云端回读。尚未见公开详情页。
- **Launching Next**：三款各用独立空白表单填写，完成 `2+3` Quick Check、取消默认新闻邮件订阅；营销预算未代填。免费提交分别产生 JevPlay `https://www.launchingnext.com/thanks/?i=153624`、OldPhotoLive AI `...?i=153625`、Graffiti Name AI `...?i=153626`，页面均显示 `SUBMITTED`、`Status: In Queue (Estimated Wait: 4 Months)`。下一页的 $99 Fast-Track 只是可选加速，未购买。按三款登记 `待审核`，侧栏合计 **5 个 Profile、6 条动态**（含历史其他产品），云端回读最新。免费排队不证明上线或已获得 backlink。
- **Free AI Tools Directory**：`https://free-ai-tools-directory.com/submit-request/` 的 Graffiti Name AI、OldPhotoLive AI、JevPlay 各自完成资料与 reCAPTCHA，三次最终页面均显示 `Thanks for contacting us! We will get in touch with you shortly.`。按三个 Profile 分别登记 `待审核`，来源页显示 **3 个 Profile、3 条动态**并完成云端回读。该站与下方禁止 AI 文案的 `aitoolsdirectory.com` 是不同站点；回执只证明申请已送达。
- **BestOfAI**：`https://bestofai.com/tool/add` 初始要求登录；使用已有 Google 登录后，OldPhotoLive AI、JevPlay、Graffiti Name AI 分别用准确官网、分类、标签与描述提交。三次赞助页均明确显示 `<产品名> has been submitted and is awaiting approval`，`Your Tools` 中对应条目均为 `Awaiting Approval / Not Sponsored`；工具条目分别是 `/tool/oldphotolive-ai`、`/tool/jevplay`、`/tool/graffiti-name-ai`。未购买赞助。按三款分别登记 `待审核`，侧栏显示 **4 个 Profile、4 条动态**（含历史 VideoToArticleAI），刷新后提示云端最新。工具条目存在不等于公开发布。
- **TAIFY**：OldPhotoLive AI 表单核对图片 URL、分类与 tagline 后点击 `Submit for review`，连续两次显示 `That verification didn't go through. Please try again.`；页面没有可继续操作的验证控件。仅在插件登记一条 `笔记` 并标 `验证码`，**不计成功**，没有继续盲目重投。
- 9 月 24 日全局账本快照为 13 个三产品完整站点。本轮新增以上五个三产品来源站并分别完成来源站云端动态回读；按该历史基线且其记录未变化，15 站目标已超过。完整 Neon 账本本轮仍不能安全读取，**当前全局精确总数未核实**。新增五站均是**已申请/待审核**，非公开收录。

## 站点取舍与数据边界

- `aitoolsdirectory.com/submit-tool` 明文拒收 AI 生成或复制的介绍，未投递，插件标 `跳过=1`；`aitoolnet.com/submit` 只见 PRO `$9.9` / Business `$49.9` 付费入口，插件标 `付费=1`，未付款；`bestofai.com/tool/add` 需登录，插件标 `需登录=1`，登录后已完成上方三产品申请。三个标记均在对应提交页重载后回读仍为 1；没有从受保护设置页读取完整云端标记文档。
- `freestartupdirectories.com/submit` 有 `$0` 档，但按钮下写明提交即同意 Terms of Use / Directory Guidelines；本轮仅预检、未接受条款或提交。`listai.cc` 表单只要求确认产品真实及有权提交，未出现同样的条款接受文案。
- 本轮人工登记使用浏览器侧栏的站点/Profile 表单，以页面回执 URL 和原文备注固化证据；记录只证明站方收到或正在审核，不证明最终公开链接、索引或 dofollow。完整统计仍须按 Neon `destinationKey::profileId` 读取全账本。

## 早间预检快照（后续结果见文首）

- 本轮目标仍是 15 个站点分别具备 JevPlay、OldPhotoLive AI、Graffiti Name AI 的准确成功记录与回执。9 月 24 日的 42 个产品×站点组合、15 个有回执站点、13 个三款齐全站点是历史快照；本轮没有读到完整 Neon 账本，因此当前数量保持**未知**，不能写成 13/15。
- 启动时检查 `origin/main` 无新提交，本地与远端均为 `f751eaa`，工作区原本干净。
- Ego 当前 AIGCLIST 标签及四个验证码页（The Next AI、Free AI Tools Directory、Launching Next、TAIFY）均仍保留。临时 Viesearch 标签已关闭；Gmail 搜索结果页保留供用户处理。

## Viesearch 云端与页面复核

- 在 `https://viesearch.com/submit` 检查到公开普通表单，含 URL、标题、描述、分类、邮箱；页面没有单独的站内邮件确认入口，只有 Sign In / Join。Viesearch 邮件正文中的确认 URL 使用站方 `/confirm/<token>` 路径。未打开或记录任何一次性 token 链接。
- Ego 侧栏对 `viesearch.com` 执行刷新后显示「云端动态已核对，当前记录是最新的」；可见数据为 1 个 Profile、2 条动态，时间均指向 2026-08-26 的 VideoToArticleAI 历史记录，其中一条状态「待审核」，原文 `This website has already been submitted and is currently under review.`，另一条为表格提交动作但未核验。没有看到本轮三款目标的成功记录。
- 同一侧栏此时只显示「可提交」标记，不显示「需人工」。此前 9 月 24 日记录的本地人工标记没有在本轮显示为已云端保存，不能声称标记已同步。
- 打开表单后扩展自动填入 Graffiti Name AI 网站资料和 `support@graffitinameai.com`，状态显示 5 个字段已填写。未提交表单；该页面只是待提交状态，不能计数。

## 其他单站云端复核与候选门槛

- **AITools Inc**：侧栏刷新显示 3 个 Profile、3 条动态，JevPlay / OldPhotoLive AI / Graffiti Name AI 分别有 2026-09-24 的免费 Typeform `Thanks!` 回执。均是已提交审核申请，未证明上线；本轮没有重复投递。
- **TipSeason**：侧栏刷新显示 4 个 Profile、4 条动态；三款目标分别显示 `Submission Received`，日期为 2026-09-24；另有一条 VideoToArticleAI 表格动作未核验。三款记录都已存在，本轮没有重复投递。
- **Startup Stash**：侧栏刷新显示 4 个 Profile、6 条动态；JevPlay、OldPhotoLive AI、Graffiti Name AI 均有 2026-09-24 的 `Thank you for applying to get listed on StartupStash!` 回执。JevPlay 有两条时间线事件；不据此将该站重复计数。本轮没有重复投递。
- **AI Tools Directory** (`/submit-tool`)：表单标明只接受原创人工文案，明确写明 AI 生成或复制内容会被删除；必填短描述、长描述和差异说明。本轮未填写资料，也未提交。
- **Aitoolnet** (`/submit`)：当前可见的提交按钮为 `Submit Pay $9.9`，另有 $49.9 方案，未发现免费提交流程；未提交。
- **BestOfAI** (`/tool/add`)：页面返回 `401 Something went wrong / You must be logged in`；未登录或提交。
- 以上均为单站证据，不构成本轮全局去重总数；三款审核申请也不等于已发布。

## 邮箱复核与交接

- 已登录的 Gmail 浏览器会话是 `syndredyoung@gmail.com`。全文件夹搜索 `from:hello@viesearch.com` 找到 9 月 24 日一封未读会话，内含两封确认邮件：JevPlay 与 OldPhotoLive AI；另有 8 月 26 日旧的 VideoToArticleAI 邮件。搜索 Graffiti Name AI 未找到 Viesearch 确认邮件。邮件要求确认后才进入标准审核队列，尚不等于目录回执或公开发布。
- JevPlay 与 OldPhotoLive AI 的确认链接未点击，搜索结果仍未读，并已将 Gmail 页留给用户操作。Graffiti 邮件未出现在当前 Gmail；资料显示其提交邮箱为 `support@graffitinameai.com`，需由用户检查对应邮箱或站方是否提供重发方式，不要为此重复提交。
- 扩展清单 `extension/manifest.json` 没有 `identity` 权限；`cloud/worker/README.md` 明确部署不创建 Google OAuth。项目没有专用邮箱 API / 确认链接读取流程；当前邮箱检查通过已登录 Gmail 页面完成，不代表插件具备邮箱连接能力。清单存在 `<all_urls>` 页面权限，但本轮未用扩展脚本读取邮箱或访问确认链接。
- 技能 `skills/external-link-operator/references/operating-policy.md` 将邮件验证列为暂停门槛；用户需自行点击两封邮件里的 Viesearch 确认链接。一次性 token 不进入本文件或其他日志。

## 云端完整数量的阻塞

- 完整统计需要读取 Neon `submissionRecords` 与 `submissionTimeline`，按规范化站点主机和精确 `destinationKey::profileId` 去重。
- 浏览器策略拒绝本轮打开扩展内部设置页。策略同时禁止改用侧栏按钮、CDP、命令行或其他浏览器表面去达成相同设置页访问，因此没有读取本机设置里的 Worker token，也没有调用 Worker 受保护的状态接口。
- 当前可用的侧栏刷新仅提供当前站点动态，不能导出全局提交账本；`table-library.json`、旧候选表和旧审计脚本是种子/历史资料，不能代替 Neon 当前数据。
- 因此本轮不报告当前三产品完整站点数。恢复完整审计需要用户从扩展内正常打开设置并提供工作区导出/回读结果，或在安全策略允许的正常界面完成核对后再重算。

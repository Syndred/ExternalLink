# JevPlay 插件独立十站验收

## 2026-09-26 20:53 / CUA 对照捕捉与新目录预检

- 先同步远端并读取验收/交接文档，分支 HEAD `185ea3f` 与远端一致、工作区干净。Computer Use 已实际用于普通 Ego 标签导航和页面读取。
- 原生窗口对照：Chrome 捕捉成功并显示“新标签页”；Ego 第一次 `-3811`，稍后成功绑定并读到本任务 Google 人机验证页，再次绑定仍 `-3811`。这证明当前不是 Chrome 原生捕捉全面故障，而是 Ego 捕捉间歇恢复；普通标签 API 正常。未取得稳定 Ego 句柄，未重启。
- 只读查 20:45–20:52 的 `SkyComputerUseService` 日志，看到 ScreenCaptureKit 流初始化/启动后暂停，及一次 AVFoundation `-11800`（底层状态 `-12902`）；没有新 `contentRect/sourceRect` 矩形不匹配记录，不能把它确定为本轮 `-3811` 根因，也没有录屏权限拒绝证据。恢复动作仍是公开标签继续工作，原生重绑偶尔成功。
- PLAXZY 官方投稿说明：免费，但必须 Google/邮箱登录；排除当前可直接独立提交候选。PLIX.GG 官方页：免费、无 SDK，联系表单或邮件接受游戏 URL、标题、描述和缩略图，且游戏须可在 iframe 中运行。未测试 Jev iframe；未读当前 Neon 精确账本，故暂为未去重候选，不冻结入队。
- 已关掉本轮 Bing 搜索、PLAXZY、PLIX.GG 页，并从 CUA 标签清单回读确认消失。保留 Google/DuckDuckGo CAPTCHA、DeskLink 条款、AxoGamer/iDev 登录页；现有 `🔎 ExternalLink 云端回读` 标签组含人工门槛现场，继续保留；其他 Chrome 标签/关闭组未碰。
- 本轮插件调用/实际填表尝试 0、站方提交与回执 0、Neon 实时读取 0。新 Jev 十站仍未固定，独立闭环 0/10；Alieradox 与 AI Tools Ratings 的 Jev 回执继续防重投。

## 2026-09-26 20:40 / 直接 Computer Use 操作与 Neon 精确账本回读

- 本轮先 `git fetch origin`，工作区干净，`HEAD=origin/main=ac8c55a`。用 `cua.getState()` 确认 Ego 运行及公开标签，再通过 CUA 在 Ego 打开 Neon Console。编辑器中的旧保存文本包含建表 SQL；执行前用 `setValue` 完整替换为单条只读 `SELECT`，回读确认后才点击 Run。Neon 为项目 `dark-night-20420985`、生产分支 `br-quiet-scene-b4gw8ygn`、`neondb`。
- Neon `externallink_workspace_documents` 精确读到 `workspace_id=default, document_key=submissionRecords, revision=123, updated_at=2026-09-26T11:25:56.337494Z`。按 `key LIKE '%::JevPlay'` 查询得到 26 行，均为 `status=success`。这些是已有 Jev 账本项，不混入新的固定十站分子；其中 Startup Stash 同时存在 `startupstash.com/add-listing::JevPlay` 与 `startupstash.com::JevPlay`，按同站只算一项。其他已存在的精确键为：
  `aiinfinity-meetpatel.notion.site/AI-Infinity-AI-Tools-Directory-0da673c487124ea2b6f8ebe59b75a231::JevPlay`、`aimarketing.directory/submit::JevPlay`、`aisotools.com::JevPlay`、`aisuperhub.io/ai-tools/submit/free::JevPlay`、`aitools.inc/submit::JevPlay`、`aitoolsdirectory.site/submit.html::JevPlay`、`bestofai.com/tool/add::JevPlay`、`credibleaitools.com/submit-tool::JevPlay`、`forwardfuture.com/tools/submit::JevPlay`、`free-ai-tools-directory.com/submit-request::JevPlay`、`humanornot.co/submit-tool::JevPlay`、`insidr.ai/submit-tools::JevPlay`、`launchingnext.com::JevPlay`、`launchpedia.co/submit::JevPlay`、`listai.cc/submit::JevPlay`、`nextpedia.io/submit-tool::JevPlay`、`once.tools/submit::JevPlay`、`producthunt.com::JevPlay`、`startupcollections.com/submit-product::JevPlay`、`thatsmy.ai/submit-tool::JevPlay`、`theaigeneration.com/add::JevPlay`、`thenextai.com/submit-ai-tool::JevPlay`、`tipseason.com/ai-tools/submit-free::JevPlay`、`viesearch.com::JevPlay`。
- 侧栏可见时通过工具栏按钮真实打开 ExternalLink；加载后显示 Profile `JevPlay` 和“云端媒体已就绪：2 个文件”。这证明插件运行 UI 在本轮至少被访问一次，但实载源码版本未读到，也没有按站点点击“检测”“填表”或“提交”。
- 之后原生 `getAXState()` 与 `getApp('com.citrolabs.ego.lite')` 多次报 `SCStreamErrorDomain -3812 (invalidParameter)`；`getApp('com.apple.finder')` 成功。普通公开标签仍可用 `getTab/createBrowserTab` 打开和读取，故原生捕捉失败不能再被当作网页标签全面停工理由。Ego 仍在运行，失败时无可用原生控制句柄，未重启。此前停工并非 Computer Use 没有任何可用路径，而是错误地把侧栏原生控制故障推广到了普通标签工作。
- 新候选现场：Rando 免费但要求真正能嵌入的游戏直达 URL；Jev `/games/code-breaker` 页面可打开，但 iframe 兼容性未证。DeskLink 表单要求“提交即同意内容规则”，AxoGamer 和 iDev 要求注册；Paizix、TapCraftBox 仅收邮件，不满足插件表单闭环；AIToolsDirectory.com 明确说 AI 生成/复制描述将被丢弃，未填。Google 和 DuckDuckGo 搜索各自出现 CAPTCHA，均已留给人工处理，没有尝试解题。
- 已通过 CUA 关闭本轮 Rando、Jev 游戏详情、AIToolsDirectory.com、Paizix、TapCraftBox、iDev 404 和 Neon 查询页并回读标签清单；保留 Google、DuckDuckGo CAPTCHA 与 DeskLink/AxoGamer/iDev 人工门槛页，保留用户原新标签页。Neon 页的活动标签分组是否随关页一并消失暂不能在原生 `-3812` 下确认；恢复后先检查空分组。
- 无新插件实际填表、站方回执或账本写入，新的固定 Jev 十站队列仍未冻结，独立闭环 **0/10**。Alieradox Jev 与 AI Tools Ratings Jev 的历史站方回执未出现在这 26 条 Neon 精确记录中，按用户要求严禁重投，也不补造记录。历史三产品矩阵结果不混算。

## 2026-09-26 20:33 / 公开标签续验与 CrazyGames 登录门槛

- 本轮 fetch 后 `HEAD` 与 `origin/main` 同为 `d14cd2b`，无未提交代码。CUA 清单起初显示 Ego 运行但无可枚举页签；原生 `getApp` 一次成功绑定新标签页，地址栏已有输入中的 `b` 及 Bilibili 建议，来源不明，为避免争用没有点击插件按钮、切空间或修改该页。
- 直接通过 CUA 独立公开标签读取 [CrazyGames 官方投稿要求](https://docs.crazygames.com/requirements/intro/)：Basic Launch 无需平台 SDK，但需游戏技术、视觉与元数据要求；Full Launch 则需 SDK。官方 `Submit a game` 指向 `https://developer.crazygames.com/games`，现场截图显示 Google 或邮箱/密码的 **Log In** 门槛，没有可用的投稿表单。未登录、注册、上传或提交；不能把该站计为当前可独立投稿候选。
- 本轮独立研究标签已关闭并回读，Ego 的原有新标签页保留，不碰其地址栏输入；没有本任务待清标签或新分组。未调用插件、没有回执和 Neon 回读，十站独立闭环无新增。

## 2026-09-26 20:28 / 直接 Computer Use 标签页恢复

- 用户要求跳过原生捕捉瓶颈，直接用 Computer Use 操作浏览器。本轮先 fetch，`HEAD`/`origin/main` 均为 `9eb483d`。CUA 在独立 IAB 标签和 Ego 公开标签都成功打开并读取 SlowDen 投稿表单，证实 **`-3811` 不会阻断普通网页标签操作**。
- CUA 清单一度显示 Ego 从运行变为已退出、原 SEO 页签消失；本任务没有执行退出动作。随后调用受支持的 `cua.getApp('com.citrolabs.ego.lite')`，取得 Ego 新标签页原生窗口，明确是本任务**重新打开** Ego，而不是声称本任务关闭过它。之后原生重绑又报 `-3811`，没有成功点击 ExternalLink 工具栏或操作侧栏。
- 按新 Ego 标签 ID `1931183027` 用 `cua.getTab(...,{browser:'4'})` 直接导航 `https://slowden.com/submit/` 并回读完整表单；可直接操作普通页面，不依赖 macOS 原生捕捉。该页明确游戏会在 iframe 中运行，除已有权利确认与必选投稿体验评分外，Jev 的 iframe 可用性仍须验证。没有填表、点击发送或获得站方回执，不计插件真实尝试。
- 已将原 `externallink` 五分钟 heartbeat 调整为：优先用 CUA 公开标签接口继续候选预检，原生错误不应停工；仅在需要插件工具栏/侧栏时恢复可靠原生句柄。明确公开标签接口不能操作浏览器工具栏或侧栏，也不能用手工网页填表冒充插件独立完成。配置回读 ACTIVE、每五分钟、任务 ID 不变。
- 本轮 IAB 与 Ego 的 SlowDen 页均已关闭，CUA 清单回读无本任务页签或分组，Ego 进程仍在但无可枚举标签。未使用被安全拒绝的扩展管理页/设置页；实时 Neon 精确账本与插件实载仍缺，新 Jev 十站未冻结，独立闭环仍为 0。

## 2026-09-26 20:23 / 原生绑定复测与游戏目录预检

- 开始 fetch，`HEAD`/`origin/main` 均为 `f063422`。本轮 CUA 先读到 Ego 仍运行，SEO 与 Search Console 是另一任务页签；首次 Ego 原生绑定报 `-3811`。对照绑定 Finder 桌面成功，再绑 Ego 成功，但实际窗口标题/URL 仍是另一任务 `seo.web.cafe`，其生成操作尚在进行。没有操作该任务侧栏或切空间；随后再绑 Ego 又报 `-3811`。这证明捕捉间歇可恢复，**没有证明本任务独立窗口可稳定绑定**，也未关闭重开 Ego。
- [Apple `SCStreamConfiguration.sourceRect`](https://developer.apple.com/documentation/screencapturekit/scstreamconfiguration/sourcerect) 官方说明：该值指定捕捉来源区域；若单窗口捕捉，系统直接取窗口完整边界而不参考它。结合上轮 `contentRect does not contain sourceRect` 日志，下一步应在受支持的 CUA 层区分显示器裁剪与窗口捕捉配置；目前拿不到 Sky 的目标窗口/`sourceRect`，不能将故障归因 Ego 或权限，也不能改显示器设置干扰其他任务。
- CUA 公开页预检 [Bounty Board Arcade 投稿页](https://www.bountyboard.gg/arcade/submit)：免费、支持直接 HTTPS 游戏 URL，但明确须先建 studio 账号，提供 16:9 封面、操作说明、至少 100 字介绍；外链游戏通过 iframe 嵌入且站方会在投稿前检查。Jev 的可嵌入性未证，未注册/登录/填表，暂不能列为插件独立十站队列。其攻略中的 CrazyGames/Poki/GamePix 等是平台发行/SDK 流程，不等于普通目录一键投稿。
- 本轮 Apple、搜索及 Bounty Board 四张研究页经 CUA 关闭并回读；仅剩另一任务 SEO、Search Console 页，无本任务新分组。仍无实时 Neon 精确去重或浏览器实载版本，未开始插件真实尝试，独立成功仍为 0。

## 2026-09-26 20:19 / 捕捉流矩形校验失败与受支持恢复复测

- 本轮先 fetch，`HEAD` 与 `origin/main` 均为 `33dbe02`，工作树无旧改动。回读现有 `externallink` 自动化为 ACTIVE、每五分钟、仍指向当前任务；规则已补充：CUA `native pipe` 关闭时重置 REPL 并重新初始化，对 `-3812` 查询捕捉日志，复测动作前后核对窗口、URL 和空间，不能把 REPL 重置写成 Ego 重启。
- Computer Use `cua.getState()` 首次返回浏览器清单后报 `native pipe is closed`；用受支持的 `cua_repl.js_reset()` 重新初始化，`getState()` 成功。这恢复的是 **Computer Use 会话**，Ego 没有退出或重启。
- 本轮 Ego 原生绑定先报 `-3811`，之后一度可绑定，但落在另一任务正在运行的 `seo.web.cafe` 窗口；Chrome 原生绑定先成功、随后动作/回读报 `-3812`。没有在错误空间操作插件，也没有中断另一任务。
- 用 `/usr/bin/log show` 只读检查同一时段的系统日志，`SkyComputerUseService` 三次报 `SCStream validateStreamConfiguration: contentRect does not contain sourceRect`。这比 `-3812 = invalidParameter` 更具体：**采集配置的矩形校验失败**；日志未给出目标 `sourceRect` 或对应窗口，不能断定由某一显示器、Ego 或权限引起。另一普通应用偶尔可绑定，故不能把故障概括为全局权限拒绝。
- CUA 公开页确认 `https://jevplay.com/games/code-breaker` 可直接打开游戏；只读 HEAD 为 HTTP 200、未见 CSP/X-Frame-Options，但这**不能证明 iframe 可嵌入**，Rando 仍不满足已核实入队条件。尝试打开用于 iframe 实测的 `data:text/html` 页面遭浏览器自动安全审核拒绝，且明确禁止换入口规避；已停止该项测试，没有经本地页面、原始 CDP 或其他界面重试。
- 本任务的 Jev 公开页、IAB 空白页及本轮误开的 Chrome 空白页已用 CUA 关闭并回读；清单仅剩另一任务的 SEO 和 Search Console 页，无本任务活跃分组。没有调用插件填表、发送或写账本；浏览器实载版本、实时 Neon 精确去重仍未证实，新 Jev 十站尚未冻结，独立闭环仍为 **0/10**。下一轮在不占用 SEO 空间的前提下先复测原生绑定；若捕捉矩形错误重现，保留日志与窗口清单并继续合规预检。

## 2026-09-26 / 巡检中断排障：原生捕捉与空间漂移

- 已更新原有 `externallink` 五分钟 heartbeat：每轮核查是否真的用了 Computer Use、为何中断，要求采取受支持的修复并复测；覆盖 `-3811`、`-3812`、窗口/空间漂移、插件实载、Neon、网络、登录等，不能只报状态。配置保持 ACTIVE 且指向本任务。
- 本轮 `cua.getApp('com.citrolabs.ego.lite')` 首次仍报 `SCStreamErrorDomain -3811`。本机 `swift import ScreenCaptureKit` 读取官方 SDK 常量：`SCStreamError.Code.internalError.rawValue == -3811`、`invalidParameter.rawValue == -3812`、`userDeclined.rawValue == -3801`。Apple [Error Constants](https://developer.apple.com/documentation/screencapturekit/error-constants) 将 `internalError` 描述为框架内部失败，将 `userDeclined` 单列为录屏权限被拒；因此**不能仅凭 -3811 宣称是权限问题**。
- 用 CUA 绑定 Chrome 原生窗口成功，再绑定 Ego 原生窗口也成功，说明该故障本轮是间歇性的、不是永久无法枚举 Ego。**没有退出或重开 Ego**。Ego 其他任务正在 `seo.web.cafe`，本轮通过原生“打开空间”菜单新建空白空间；ExternalLink 侧栏可打开，Profile 已切到 JevPlay，侧栏显示 Jev 云端媒体 `2 个文件`。这只证实侧栏可见和媒体状态，不能证明插件源码版本、Neon 精确投稿账本或投稿成功。
- 随后原生捕捉转为 `-3812`。一次多动作调用在捕捉报错前部分完成，结果 SlowDen 预检页落到 SEO 任务所在空间，而非预期独立空白空间。已通过 CUA 公开标签接口定位并关闭本任务误开的 SlowDen 页，回读仅剩原 SEO 页和新建的空白空间。**后续必须先回读窗口标题、URL、空间，单步动作后立即回读；遇到捕捉错误不能假设所有动作失败，也不能沿用旧索引。**
- 官方错误码只能确定框架报告的类别，不能单独定位是 Ego、macOS 捕捉流还是窗口绑定状态。下一轮先核实 CUA 目标窗口，再尝试稳定原生绑定；如其他任务仍占用原空间，只在独立空间操作。若原生再次失败，可继续普通公开页面预检、源码/测试工作，但不得用备用接口访问此前被拒的扩展内部页。未完成实时 Neon 去重，**本轮没有调用插件填表或最终提交，独立成功仍为 0**。
- 补充复测：CUA 公开标签接口可精确绑定独立空间页签 `512544022`（当轮浏览器 `5`），在该页签打开 SlowDen 并读回成功；同一时刻原生 `cua.getApp('ego lite')` 却仍选中 `seo.web.cafe` 窗口。这证明**普通网页预检可在独立标签继续，但原生插件侧栏动作不能凭 getApp 推断目标就是独立空间**。SlowDen 预检完已将该页签改回 `about:blank`，回读其他任务 SEO 页未变；空白页标记为续办现场。本轮没有进行插件动作。

## 2026-09-26 / 五分钟续办：云端只读门槛复核

- 本轮先 fetch，`origin/main` 与工作树均为 `4dfef6d`，无远端新改动；回读现有 heartbeat 仍 ACTIVE、每五分钟、指向本任务，上轮确已使用 Computer Use 操作并清理六张候选页。
- Computer Use 再次绑定 Ego 原生窗口，仍是 ScreenCaptureKit `-3811`，没有可用窗口句柄，未执行退出重开或扩展内部页操作。
- 通过 Computer Use 普通公开标签打开 Neon 官方 `https://console.neon.tech/` 试图只读核对 canonical 账本，页面停在 Cloudflare“正在进行安全验证”；未点击、求解或绕过验证，未读到数据库。该页已关闭。本轮 `cua.getState()` 回读仅剩用户原有的 Chrome 设置页，没有本任务已完成页签或分组。
- 这一步没有产生实时 Neon 记录或新投稿，不能凭本机 19:16 快照固定十站并启动插件尝试；独立闭环仍为 0，新目标仍未达 6/10。

## 2026-09-26 / Computer Use 续验与定时任务核对

- 用户要求由当前任务继续，并指定网页操作使用 Computer Use。本轮通过 `cua_repl` 公开 Chrome 标签接口打开、读取和关闭候选页；Ego 原生绑定重试仍返回 ScreenCaptureKit `-3811`，没有取得退出/重启窗口句柄，未声称已重启。此前扩展内部页的安全拒绝未绕过，因此插件实载版本和当前 Neon 精确回读仍未完成。
- 现有 `externallink` heartbeat 已更新并回读：ACTIVE、每五分钟、目标仍是当前任务。每轮检查上一轮是否实际使用 Computer Use、是否因此中断，普通故障用受支持界面重试或在不影响其他任务时重启 Ego，并回读清理本任务已完成的页签/分组；保留 Jev 限定、去重、6/10 和 Neon 证据规则。没有新建重复任务。
- SlowDen `https://slowden.com/submit/` 直接显示免费已完成浏览器游戏表单；需游戏名称、可玩链接、描述、邮箱、权利确认及必选的投稿体验评分，表示提交后立即上线再人工审核。可作为候选，但需验证 Jev 游戏是否可嵌入及当前云端去重，尚未触发插件填表。
- Rando `https://rando.gg/submit` 免费接收可嵌入 iframe 的 HTML5 游戏，要求直接游戏 URL，不是普通介绍页；官网明确说 iframe 不传 SEO 链接权重。Jev 的实际可嵌入性未测，不将其计为已合格队列或成功。
- Valeri `https://valeri.tech/submit` 免费但需登录；现场游戏表单 Platform 仅 iOS、Android、iOS & Android，Web build 只是可选项。Jev 仅网页版，预检判不适配，排除。
- Find Next Play 官方说明接受网页游戏、免费；现场点击 Submit Game 跳 `/login?redirect=/submit`，并加载 reCAPTCHA，当前未登录，未继续。
- Indie Games Developer `https://devcentral.indiegamesdeveloper.com/submit-your-game-free-form/` 免费表单要求 Press Kit URL 和主要商店页面 URL；Jev 现有网站不等于商店页，先排除，不能编造资料。
- 2026-09-26 19:16 的本机备份快照对 SlowDen、Rando、Valeri、Find Next Play 均无 Jev 精确记录，但这是旧快照，不等于当前 Neon 无记录。Google 对 `site:slowden.com/games/ JevPlay` 未显示结果，也不能单独证明站内没有条目。提交前仍须实时精确去重。
- 本轮新开的六个候选/去重页签已逐一关闭，`cua.getState()` 回读仅余用户原有 `chrome://settings/autofill` 标签；没有本任务活动分组。没有新增站方提交，十站独立目标仍未达标。

## 19:54 续验：普通窗口恢复，发现已有公开条目

- fetch 后与远端一致，基线 `853d327`。Ego 原生绑定本轮成功，刷新后点击、打开空间、普通网页导航及关页均可用；没有执行退出/重开。先新建独立空间，界面由 `1 Space` 变为 `2 Spaces`，避免使用其他 Jev 游戏任务的原空间。未操作既有安全拒绝的扩展管理页/设置页，实载和 Neon 仍待合规验证。
- 从 Google 公开结果进入 `https://techbasedirectory.com/product/jevplay`，真实产品页显示 JevPlay 标题、Code Breaker / Word Ladder / Dungeon Crawler / Snake 介绍，`View Product Website` 链接为 `https://jevplay.com/?utm_source=TechBaseDirectory`。此为直接站方公开条目证据；页面还显示 `Claim This Product`，本轮未认领、修改或提交。
- 将 TechBaseDirectory 纳入已存在条目的防重投范围。它不是本轮插件独立提交，不能计入新十站分子；未读取 Neon 当前精确键，不声称已云端登记。
- 目录详情页和搜索页已逐一关闭，窗口正文回到新标签页；没有关闭原有 X、设置/管理页或其他任务标签。独立空间保留复用，不新增任务分组。

## 19:49 续验：候选费用与登录门槛

- fetch 后仍为 `d4cb04e`，无远端新改动；Ego 原生绑定再次为 `-3811`，未获得退出或重启句柄，未操作安全拒绝的内部页。
- AIToolnet `https://www.aitoolnet.com/submit` 本次公开页可打开，含 Gaming 分类及产品 Free 定价选项，但目录费用仅见 PRO $9.9 / BUSINESS $49.9，必填 Payment Platform，最终按钮为 `Submit Pay $9.9`。交接候选中的 START/Free 说法不符合本次现场，不能将产品 Free 定价当成免费投稿；本轮未填表、未付款、未投稿。
- ToolScout `https://toolscout.ai/submit` 明确免费，但要求 `Sign in to continue`；登录页支持 Google / Email，继续前明确接受 Terms of Service。未继续登录或接受条款，未投稿。
- 共用本任务新建页签 `512544013`，已关闭并回读确认清理；不计插件尝试，独立闭环无新增。
- 修复本地审计空数据误报：`audit-state.mjs` 明确 `source=local_seed_backup`、`liveCloudVerified=false`，无成功证据 `ok=false / reason=no_evidence`；Jev 实际 CLI 退出码 2，RainbowPetAI 既有本地账本仍退出 0。独立 fixture 回归覆盖无记录、仅失败记录、成功账本与别名匹配，全部通过；语法、diff 和 Skill 校验通过。原综合 Skill 测试因 worktree 外 `rainbowPetAi/public` 缺失而失败，未声称全量通过；本轮未改扩展运行代码或版本。

## 19:44 新任务接手与四站现场预检

- 新任务 `01a0dd85-e567-7273-a836-7a179de56ddb`，worktree `/Users/syndred/.codex/worktrees/67be/ExternalLink`；开始 fetch 后 HEAD 与 origin/main 均为 `06df6fe`。现有 `externallink` heartbeat 已迁入本任务，配置回读为 ACTIVE、每 5 分钟，没有新增自动化。
- CUA 刷新清单后，Ego 原生绑定仍返回 ScreenCaptureKit `-3811`，没有取得控制句柄，未完成退出重开或插件重载。Chrome 公开标签接口可用；未访问此前被安全拒绝的扩展内部页，也未通过其他接口绕过。实载版本和加载路径仍未证实。
- 以下均为公开页面门槛预检，未调用插件填表，未点击最终投稿，不能计入独立成功或已固定十站；暂未完成最新云端去重。

| 候选 | 本轮直接可见证据 | 下一步 |
| --- | --- | --- |
| Sites Plus | `/submit` 选择 Recreation 后，第二步明确提供 `Regular - free`；第三步含 TinyMCE 描述、默认勾选邮件订阅、必选 `Submission Rules Agreement` | 分类仍需细化；最终投稿前处理协议确认，不能自动沿用默认订阅 |
| AI Tools Directory `.com` | `/submit-tool` 明确写 AI 生成或复制文案会被 discarded | 当前源码已有该域名和 Paperform 的人工闸门；从插件独立投稿候选中排除，不重复开发或尝试 |
| 10015 | 输入 Jev 官网并点 `Submit URL` 后出现 `Sign In / Register`、`You need to sign in or register to use this feature.`；页面注明免费队列 3–4 个月、免费链接 nofollow | 当前会话需登录；没有抓取后表单或投稿回执，不以首页免费说明认定可直接投稿 |
| AppRater | `/add/` 重定向首页，`#submit` 表单含 App name、Platform、App or store URL、What makes it useful、姓名、邮箱，可选 tags/image/X；页面已有 Web 游戏案例；最终按钮旁写提交即同意 Terms | 更新旧入口认知；条款待动作时确认，付费 leaderboard 不能误当投稿必付费 |

- 四站共用本任务新建标签 `512544009`，已关闭并回读标签清单确认移除；原有 X、扩展管理/设置及其他 Jev 游戏任务页未动。
- 本轮新增插件独立闭环 **0**，目标仍为固定十站至少 **6/10**；没有以预检、文档或静态测试冒充真实完成。
- 只读审计确认接手时原仓库和 worktree 均为 `06df6fe`，两处 manifest 哈希一致、版本 `3.7.106`；这仅证明磁盘版本一致，不证明浏览器加载路径或已重载。Worker 的 `/v1/revisions` 与 `/v1/state/submissionRecords` 可用于实时读回，但需设备访问密钥，未授权请求返回 401；原仓库 `.env` 无 `EXTERNALLINK_CLOUD_*`，不能为取密钥绕过内部页限制。
- 最近可用导出仍为 `2026-09-26T11:16:49.979Z` 的 `(3).json`、26 条 Jev 精确记录，属于历史快照。旧 `audit-state.mjs --profile JevPlay` 只查种子和灾备，输出 `0/0 ok` 不能代表 Neon 为空或已去重；不据此启动新投稿。Alieradox 与 AI Tools Ratings 的既有回执继续优先防重投。

## 2026-09-26 / Jev 单站范围与 3.7.106 精确选项优先

- 用户最新范围覆盖旧三产品计划：只投稿 JevPlay，OldPhotoLive 与 GraffitiName 暂停外部提交。既有三产品矩阵 8/30 仅保留为历史，不能混入新 Jev 样本。新十站先排除已有回执，固定队列后记录每一真实尝试、失败和人工辅助；独立填表、提交、站方回执及 Neon 精确回读至少 6/10 才达到超过 50%。本轮尚无新增真实提交，不能报告达标。
- 普通卡死恢复流程已写入现有 `externallink` 续办任务：保存目的站、Profile、是否已点提交和证据，关闭并重新打开 Ego；恢复后先核对回执和精确去重，再继续未提交步骤。已有其他自动化工作空间则使用独立空间；用完关闭本任务页签与分组。安全策略拒绝、登录、CAPTCHA 和条款门槛不靠重启绕过。不能关闭其他任务正在使用的 Chrome 窗口。
- 本轮通过原生接口启动 Ego，应用清单确认运行，但绑定窗口连续失败于 ScreenCaptureKit `-3811`，未取得可调用的窗口句柄，因此尚不能执行退出快捷键或操作插件；Chrome 原生窗口也同样失败。不得将启动应用写成已完成重启，更不得将源码测试计为实站成功。此前扩展管理页/设置页存在明确安全拒绝，不使用备用界面规避。
- 复查自定义下拉发现 `Free` 会先命中排列在前的 `Freemium` 包含匹配。3.7.106 复用项目既有 `findBestSelectOption` 的精确标签优先排序，覆盖首次展开和输入搜索后匹配；动态回归验证 Free 选项收到选择事件，Freemium 不被误选。54/54 测试、语法和 diff 检查通过，源码尚未实载复验。前版主流实现参考仍为 Bitwarden 字段归属、Automa 控件流程、Playwright 可访问名称与动作后验证、Radix pointerup 提交选择。

## 队列与计分

候选预检尚不计提交；固定队列前需要核对云端精确去重。第一次插件填表启动即计真实尝试，填写错误、提交失败、人工接管、缺少站方回执或云端回读都留在分母中，不替换删除。只有重复或产品不适用等在尝试前已确认的候选才能排除。超过 50% 要求至少 6/10，全部十站须有处理结果。

## 待恢复后第一步

使用受支持的 Ego 原生界面恢复普通窗口控制；禁止重投 Alieradox 和 AI Tools Ratings 的 Jev。确认插件实载版本后，在新候选真实页面验证字段、Free 定价、提交和云端闭环；不要把手动填写替代插件独立测试。

## 本轮公开页预检

- 2026-09-26 19:35，Startups Lab `https://startupslab.site/submit-product`：最近一次云端回读导出无该站精确账本。公开页手动填 JevPlay 名称和官网后点击 `Generate Launch Listing`，立即弹 `Sign In to Submit Product`，要求登录验证 maker ownership 并保存草稿。未提交、未创建成功记录；取消并关闭预检页。此为门槛预检，不计插件独立尝试。恢复后若有已授权登录态可再次评估。
- 源码 3.7.106 已随 `bb009fe` 推送 `origin/main`，工作区干净；插件实载版本与现场回归仍未验证。

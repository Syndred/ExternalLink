# 外链通用表单实站验收（2026-09-10）

## 目标

普通表单走确定性填写；异步多步骤、自定义组件、富文本、上传预览等异常才升级模型。验证码、登录、OTP、付费和明确法律确认停给人工。成功必须读取站点回执、结果页或公开链接后才写账本。

## 实站结果

- ListAi：真实页面返回 `Submission Received!`，并提示 24–48 小时发布。发现同 URL 文档替换后旧脚本仍读旧文档，3.3.13 后改为提交后重新读取当前文档证据。
- Hype Star：无登录、无邮箱、免费、多步骤。3.3.14 能从 URL 页进入 `REVIEW DRAFT`，但下一步异步出现后被误判人工；3.3.15 已能识别并保持运行；3.3.16 真实页面已自动选择 `AI & Agent Tools`。继续复验发现按钮样式的 `List a project — free` 推广链接参与了提交候选排序并重新打开首步；3.3.18 通用排除链接式入口，只允许真实表单按钮参与代点。
- ShareYourStartup：真实页出现 Cloudflare Turnstile，正确归入验证码人工门，没有伪记成功。
- Product Hunt：OldPhotoLive AI 已真实创建草稿并回读产品页、图库、团队、标签和首评；状态是已创建草稿，未排期公开 Launch。

## 当前边界

3.3.18 代码和 21 套 Node 测试已通过。Hype Star 的最终提交回执仍需浏览器实际加载 3.3.18 后复验；在得到该证据前不得宣称全库所有站点均已成功。
